// Generated by `pnpm build:widget`. Do not edit by hand — edit lean/widget/src instead.
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// lean/widget/src/index.tsx
import * as React from "react";

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/external.js
var external_exports = {};
__export(external_exports, {
  BRAND: () => BRAND,
  DIRTY: () => DIRTY,
  EMPTY_PATH: () => EMPTY_PATH,
  INVALID: () => INVALID,
  NEVER: () => NEVER,
  OK: () => OK,
  ParseStatus: () => ParseStatus,
  Schema: () => ZodType,
  ZodAny: () => ZodAny,
  ZodArray: () => ZodArray,
  ZodBigInt: () => ZodBigInt,
  ZodBoolean: () => ZodBoolean,
  ZodBranded: () => ZodBranded,
  ZodCatch: () => ZodCatch,
  ZodDate: () => ZodDate,
  ZodDefault: () => ZodDefault,
  ZodDiscriminatedUnion: () => ZodDiscriminatedUnion,
  ZodEffects: () => ZodEffects,
  ZodEnum: () => ZodEnum,
  ZodError: () => ZodError,
  ZodFirstPartyTypeKind: () => ZodFirstPartyTypeKind,
  ZodFunction: () => ZodFunction,
  ZodIntersection: () => ZodIntersection,
  ZodIssueCode: () => ZodIssueCode,
  ZodLazy: () => ZodLazy,
  ZodLiteral: () => ZodLiteral,
  ZodMap: () => ZodMap,
  ZodNaN: () => ZodNaN,
  ZodNativeEnum: () => ZodNativeEnum,
  ZodNever: () => ZodNever,
  ZodNull: () => ZodNull,
  ZodNullable: () => ZodNullable,
  ZodNumber: () => ZodNumber,
  ZodObject: () => ZodObject,
  ZodOptional: () => ZodOptional,
  ZodParsedType: () => ZodParsedType,
  ZodPipeline: () => ZodPipeline,
  ZodPromise: () => ZodPromise,
  ZodReadonly: () => ZodReadonly,
  ZodRecord: () => ZodRecord,
  ZodSchema: () => ZodType,
  ZodSet: () => ZodSet,
  ZodString: () => ZodString,
  ZodSymbol: () => ZodSymbol,
  ZodTransformer: () => ZodEffects,
  ZodTuple: () => ZodTuple,
  ZodType: () => ZodType,
  ZodUndefined: () => ZodUndefined,
  ZodUnion: () => ZodUnion,
  ZodUnknown: () => ZodUnknown,
  ZodVoid: () => ZodVoid,
  addIssueToContext: () => addIssueToContext,
  any: () => anyType,
  array: () => arrayType,
  bigint: () => bigIntType,
  boolean: () => booleanType,
  coerce: () => coerce,
  custom: () => custom,
  date: () => dateType,
  datetimeRegex: () => datetimeRegex,
  defaultErrorMap: () => en_default,
  discriminatedUnion: () => discriminatedUnionType,
  effect: () => effectsType,
  enum: () => enumType,
  function: () => functionType,
  getErrorMap: () => getErrorMap,
  getParsedType: () => getParsedType,
  instanceof: () => instanceOfType,
  intersection: () => intersectionType,
  isAborted: () => isAborted,
  isAsync: () => isAsync,
  isDirty: () => isDirty,
  isValid: () => isValid,
  late: () => late,
  lazy: () => lazyType,
  literal: () => literalType,
  makeIssue: () => makeIssue,
  map: () => mapType,
  nan: () => nanType,
  nativeEnum: () => nativeEnumType,
  never: () => neverType,
  null: () => nullType,
  nullable: () => nullableType,
  number: () => numberType,
  object: () => objectType,
  objectUtil: () => objectUtil,
  oboolean: () => oboolean,
  onumber: () => onumber,
  optional: () => optionalType,
  ostring: () => ostring,
  pipeline: () => pipelineType,
  preprocess: () => preprocessType,
  promise: () => promiseType,
  quotelessJson: () => quotelessJson,
  record: () => recordType,
  set: () => setType,
  setErrorMap: () => setErrorMap,
  strictObject: () => strictObjectType,
  string: () => stringType,
  symbol: () => symbolType,
  transformer: () => effectsType,
  tuple: () => tupleType,
  undefined: () => undefinedType,
  union: () => unionType,
  unknown: () => unknownType,
  util: () => util,
  void: () => voidType
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/util.js
var util;
(function(util2) {
  util2.assertEqual = (_) => {
  };
  function assertIs(_arg) {
  }
  util2.assertIs = assertIs;
  function assertNever(_x) {
    throw new Error();
  }
  util2.assertNever = assertNever;
  util2.arrayToEnum = (items) => {
    const obj = {};
    for (const item of items) {
      obj[item] = item;
    }
    return obj;
  };
  util2.getValidEnumValues = (obj) => {
    const validKeys = util2.objectKeys(obj).filter((k) => typeof obj[obj[k]] !== "number");
    const filtered = {};
    for (const k of validKeys) {
      filtered[k] = obj[k];
    }
    return util2.objectValues(filtered);
  };
  util2.objectValues = (obj) => {
    return util2.objectKeys(obj).map(function(e) {
      return obj[e];
    });
  };
  util2.objectKeys = typeof Object.keys === "function" ? (obj) => Object.keys(obj) : (object) => {
    const keys = [];
    for (const key in object) {
      if (Object.prototype.hasOwnProperty.call(object, key)) {
        keys.push(key);
      }
    }
    return keys;
  };
  util2.find = (arr, checker) => {
    for (const item of arr) {
      if (checker(item))
        return item;
    }
    return void 0;
  };
  util2.isInteger = typeof Number.isInteger === "function" ? (val) => Number.isInteger(val) : (val) => typeof val === "number" && Number.isFinite(val) && Math.floor(val) === val;
  function joinValues(array, separator = " | ") {
    return array.map((val) => typeof val === "string" ? `'${val}'` : val).join(separator);
  }
  util2.joinValues = joinValues;
  util2.jsonStringifyReplacer = (_, value) => {
    if (typeof value === "bigint") {
      return value.toString();
    }
    return value;
  };
})(util || (util = {}));
var objectUtil;
(function(objectUtil2) {
  objectUtil2.mergeShapes = (first, second) => {
    return {
      ...first,
      ...second
      // second overwrites first
    };
  };
})(objectUtil || (objectUtil = {}));
var ZodParsedType = util.arrayToEnum([
  "string",
  "nan",
  "number",
  "integer",
  "float",
  "boolean",
  "date",
  "bigint",
  "symbol",
  "function",
  "undefined",
  "null",
  "array",
  "object",
  "unknown",
  "promise",
  "void",
  "never",
  "map",
  "set"
]);
var getParsedType = (data) => {
  const t = typeof data;
  switch (t) {
    case "undefined":
      return ZodParsedType.undefined;
    case "string":
      return ZodParsedType.string;
    case "number":
      return Number.isNaN(data) ? ZodParsedType.nan : ZodParsedType.number;
    case "boolean":
      return ZodParsedType.boolean;
    case "function":
      return ZodParsedType.function;
    case "bigint":
      return ZodParsedType.bigint;
    case "symbol":
      return ZodParsedType.symbol;
    case "object":
      if (Array.isArray(data)) {
        return ZodParsedType.array;
      }
      if (data === null) {
        return ZodParsedType.null;
      }
      if (data.then && typeof data.then === "function" && data.catch && typeof data.catch === "function") {
        return ZodParsedType.promise;
      }
      if (typeof Map !== "undefined" && data instanceof Map) {
        return ZodParsedType.map;
      }
      if (typeof Set !== "undefined" && data instanceof Set) {
        return ZodParsedType.set;
      }
      if (typeof Date !== "undefined" && data instanceof Date) {
        return ZodParsedType.date;
      }
      return ZodParsedType.object;
    default:
      return ZodParsedType.unknown;
  }
};

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/ZodError.js
var ZodIssueCode = util.arrayToEnum([
  "invalid_type",
  "invalid_literal",
  "custom",
  "invalid_union",
  "invalid_union_discriminator",
  "invalid_enum_value",
  "unrecognized_keys",
  "invalid_arguments",
  "invalid_return_type",
  "invalid_date",
  "invalid_string",
  "too_small",
  "too_big",
  "invalid_intersection_types",
  "not_multiple_of",
  "not_finite"
]);
var quotelessJson = (obj) => {
  const json = JSON.stringify(obj, null, 2);
  return json.replace(/"([^"]+)":/g, "$1:");
};
var ZodError = class _ZodError extends Error {
  get errors() {
    return this.issues;
  }
  constructor(issues) {
    super();
    this.issues = [];
    this.addIssue = (sub) => {
      this.issues = [...this.issues, sub];
    };
    this.addIssues = (subs = []) => {
      this.issues = [...this.issues, ...subs];
    };
    const actualProto = new.target.prototype;
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(this, actualProto);
    } else {
      this.__proto__ = actualProto;
    }
    this.name = "ZodError";
    this.issues = issues;
  }
  format(_mapper) {
    const mapper = _mapper || function(issue) {
      return issue.message;
    };
    const fieldErrors = { _errors: [] };
    const processError = (error) => {
      for (const issue of error.issues) {
        if (issue.code === "invalid_union") {
          issue.unionErrors.map(processError);
        } else if (issue.code === "invalid_return_type") {
          processError(issue.returnTypeError);
        } else if (issue.code === "invalid_arguments") {
          processError(issue.argumentsError);
        } else if (issue.path.length === 0) {
          fieldErrors._errors.push(mapper(issue));
        } else {
          let curr = fieldErrors;
          let i = 0;
          while (i < issue.path.length) {
            const el2 = issue.path[i];
            const terminal = i === issue.path.length - 1;
            if (!terminal) {
              curr[el2] = curr[el2] || { _errors: [] };
            } else {
              curr[el2] = curr[el2] || { _errors: [] };
              curr[el2]._errors.push(mapper(issue));
            }
            curr = curr[el2];
            i++;
          }
        }
      }
    };
    processError(this);
    return fieldErrors;
  }
  static assert(value) {
    if (!(value instanceof _ZodError)) {
      throw new Error(`Not a ZodError: ${value}`);
    }
  }
  toString() {
    return this.message;
  }
  get message() {
    return JSON.stringify(this.issues, util.jsonStringifyReplacer, 2);
  }
  get isEmpty() {
    return this.issues.length === 0;
  }
  flatten(mapper = (issue) => issue.message) {
    const fieldErrors = {};
    const formErrors = [];
    for (const sub of this.issues) {
      if (sub.path.length > 0) {
        const firstEl = sub.path[0];
        fieldErrors[firstEl] = fieldErrors[firstEl] || [];
        fieldErrors[firstEl].push(mapper(sub));
      } else {
        formErrors.push(mapper(sub));
      }
    }
    return { formErrors, fieldErrors };
  }
  get formErrors() {
    return this.flatten();
  }
};
ZodError.create = (issues) => {
  const error = new ZodError(issues);
  return error;
};

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/locales/en.js
var errorMap = (issue, _ctx) => {
  let message;
  switch (issue.code) {
    case ZodIssueCode.invalid_type:
      if (issue.received === ZodParsedType.undefined) {
        message = "Required";
      } else {
        message = `Expected ${issue.expected}, received ${issue.received}`;
      }
      break;
    case ZodIssueCode.invalid_literal:
      message = `Invalid literal value, expected ${JSON.stringify(issue.expected, util.jsonStringifyReplacer)}`;
      break;
    case ZodIssueCode.unrecognized_keys:
      message = `Unrecognized key(s) in object: ${util.joinValues(issue.keys, ", ")}`;
      break;
    case ZodIssueCode.invalid_union:
      message = `Invalid input`;
      break;
    case ZodIssueCode.invalid_union_discriminator:
      message = `Invalid discriminator value. Expected ${util.joinValues(issue.options)}`;
      break;
    case ZodIssueCode.invalid_enum_value:
      message = `Invalid enum value. Expected ${util.joinValues(issue.options)}, received '${issue.received}'`;
      break;
    case ZodIssueCode.invalid_arguments:
      message = `Invalid function arguments`;
      break;
    case ZodIssueCode.invalid_return_type:
      message = `Invalid function return type`;
      break;
    case ZodIssueCode.invalid_date:
      message = `Invalid date`;
      break;
    case ZodIssueCode.invalid_string:
      if (typeof issue.validation === "object") {
        if ("includes" in issue.validation) {
          message = `Invalid input: must include "${issue.validation.includes}"`;
          if (typeof issue.validation.position === "number") {
            message = `${message} at one or more positions greater than or equal to ${issue.validation.position}`;
          }
        } else if ("startsWith" in issue.validation) {
          message = `Invalid input: must start with "${issue.validation.startsWith}"`;
        } else if ("endsWith" in issue.validation) {
          message = `Invalid input: must end with "${issue.validation.endsWith}"`;
        } else {
          util.assertNever(issue.validation);
        }
      } else if (issue.validation !== "regex") {
        message = `Invalid ${issue.validation}`;
      } else {
        message = "Invalid";
      }
      break;
    case ZodIssueCode.too_small:
      if (issue.type === "array")
        message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `more than`} ${issue.minimum} element(s)`;
      else if (issue.type === "string")
        message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `over`} ${issue.minimum} character(s)`;
      else if (issue.type === "number")
        message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
      else if (issue.type === "bigint")
        message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
      else if (issue.type === "date")
        message = `Date must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(issue.minimum))}`;
      else
        message = "Invalid input";
      break;
    case ZodIssueCode.too_big:
      if (issue.type === "array")
        message = `Array must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `less than`} ${issue.maximum} element(s)`;
      else if (issue.type === "string")
        message = `String must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `under`} ${issue.maximum} character(s)`;
      else if (issue.type === "number")
        message = `Number must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
      else if (issue.type === "bigint")
        message = `BigInt must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
      else if (issue.type === "date")
        message = `Date must be ${issue.exact ? `exactly` : issue.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(issue.maximum))}`;
      else
        message = "Invalid input";
      break;
    case ZodIssueCode.custom:
      message = `Invalid input`;
      break;
    case ZodIssueCode.invalid_intersection_types:
      message = `Intersection results could not be merged`;
      break;
    case ZodIssueCode.not_multiple_of:
      message = `Number must be a multiple of ${issue.multipleOf}`;
      break;
    case ZodIssueCode.not_finite:
      message = "Number must be finite";
      break;
    default:
      message = _ctx.defaultError;
      util.assertNever(issue);
  }
  return { message };
};
var en_default = errorMap;

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/errors.js
var overrideErrorMap = en_default;
function setErrorMap(map) {
  overrideErrorMap = map;
}
function getErrorMap() {
  return overrideErrorMap;
}

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/parseUtil.js
var makeIssue = (params) => {
  const { data, path: path2, errorMaps, issueData } = params;
  const fullPath = [...path2, ...issueData.path || []];
  const fullIssue = {
    ...issueData,
    path: fullPath
  };
  if (issueData.message !== void 0) {
    return {
      ...issueData,
      path: fullPath,
      message: issueData.message
    };
  }
  let errorMessage = "";
  const maps = errorMaps.filter((m) => !!m).slice().reverse();
  for (const map of maps) {
    errorMessage = map(fullIssue, { data, defaultError: errorMessage }).message;
  }
  return {
    ...issueData,
    path: fullPath,
    message: errorMessage
  };
};
var EMPTY_PATH = [];
function addIssueToContext(ctx, issueData) {
  const overrideMap = getErrorMap();
  const issue = makeIssue({
    issueData,
    data: ctx.data,
    path: ctx.path,
    errorMaps: [
      ctx.common.contextualErrorMap,
      // contextual error map is first priority
      ctx.schemaErrorMap,
      // then schema-bound map if available
      overrideMap,
      // then global override map
      overrideMap === en_default ? void 0 : en_default
      // then global default map
    ].filter((x) => !!x)
  });
  ctx.common.issues.push(issue);
}
var ParseStatus = class _ParseStatus {
  constructor() {
    this.value = "valid";
  }
  dirty() {
    if (this.value === "valid")
      this.value = "dirty";
  }
  abort() {
    if (this.value !== "aborted")
      this.value = "aborted";
  }
  static mergeArray(status, results) {
    const arrayValue = [];
    for (const s of results) {
      if (s.status === "aborted")
        return INVALID;
      if (s.status === "dirty")
        status.dirty();
      arrayValue.push(s.value);
    }
    return { status: status.value, value: arrayValue };
  }
  static async mergeObjectAsync(status, pairs) {
    const syncPairs = [];
    for (const pair of pairs) {
      const key = await pair.key;
      const value = await pair.value;
      syncPairs.push({
        key,
        value
      });
    }
    return _ParseStatus.mergeObjectSync(status, syncPairs);
  }
  static mergeObjectSync(status, pairs) {
    const finalObject = {};
    for (const pair of pairs) {
      const { key, value } = pair;
      if (key.status === "aborted")
        return INVALID;
      if (value.status === "aborted")
        return INVALID;
      if (key.status === "dirty")
        status.dirty();
      if (value.status === "dirty")
        status.dirty();
      if (key.value !== "__proto__" && (typeof value.value !== "undefined" || pair.alwaysSet)) {
        finalObject[key.value] = value.value;
      }
    }
    return { status: status.value, value: finalObject };
  }
};
var INVALID = Object.freeze({
  status: "aborted"
});
var DIRTY = (value) => ({ status: "dirty", value });
var OK = (value) => ({ status: "valid", value });
var isAborted = (x) => x.status === "aborted";
var isDirty = (x) => x.status === "dirty";
var isValid = (x) => x.status === "valid";
var isAsync = (x) => typeof Promise !== "undefined" && x instanceof Promise;

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/errorUtil.js
var errorUtil;
(function(errorUtil2) {
  errorUtil2.errToObj = (message) => typeof message === "string" ? { message } : message || {};
  errorUtil2.toString = (message) => typeof message === "string" ? message : message?.message;
})(errorUtil || (errorUtil = {}));

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/types.js
var ParseInputLazyPath = class {
  constructor(parent, value, path2, key) {
    this._cachedPath = [];
    this.parent = parent;
    this.data = value;
    this._path = path2;
    this._key = key;
  }
  get path() {
    if (!this._cachedPath.length) {
      if (Array.isArray(this._key)) {
        this._cachedPath.push(...this._path, ...this._key);
      } else {
        this._cachedPath.push(...this._path, this._key);
      }
    }
    return this._cachedPath;
  }
};
var handleResult = (ctx, result) => {
  if (isValid(result)) {
    return { success: true, data: result.value };
  } else {
    if (!ctx.common.issues.length) {
      throw new Error("Validation failed but no issues detected.");
    }
    return {
      success: false,
      get error() {
        if (this._error)
          return this._error;
        const error = new ZodError(ctx.common.issues);
        this._error = error;
        return this._error;
      }
    };
  }
};
function processCreateParams(params) {
  if (!params)
    return {};
  const { errorMap: errorMap2, invalid_type_error, required_error, description } = params;
  if (errorMap2 && (invalid_type_error || required_error)) {
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  }
  if (errorMap2)
    return { errorMap: errorMap2, description };
  const customMap = (iss, ctx) => {
    const { message } = params;
    if (iss.code === "invalid_enum_value") {
      return { message: message ?? ctx.defaultError };
    }
    if (typeof ctx.data === "undefined") {
      return { message: message ?? required_error ?? ctx.defaultError };
    }
    if (iss.code !== "invalid_type")
      return { message: ctx.defaultError };
    return { message: message ?? invalid_type_error ?? ctx.defaultError };
  };
  return { errorMap: customMap, description };
}
var ZodType = class {
  get description() {
    return this._def.description;
  }
  _getType(input) {
    return getParsedType(input.data);
  }
  _getOrReturnCtx(input, ctx) {
    return ctx || {
      common: input.parent.common,
      data: input.data,
      parsedType: getParsedType(input.data),
      schemaErrorMap: this._def.errorMap,
      path: input.path,
      parent: input.parent
    };
  }
  _processInputParams(input) {
    return {
      status: new ParseStatus(),
      ctx: {
        common: input.parent.common,
        data: input.data,
        parsedType: getParsedType(input.data),
        schemaErrorMap: this._def.errorMap,
        path: input.path,
        parent: input.parent
      }
    };
  }
  _parseSync(input) {
    const result = this._parse(input);
    if (isAsync(result)) {
      throw new Error("Synchronous parse encountered promise.");
    }
    return result;
  }
  _parseAsync(input) {
    const result = this._parse(input);
    return Promise.resolve(result);
  }
  parse(data, params) {
    const result = this.safeParse(data, params);
    if (result.success)
      return result.data;
    throw result.error;
  }
  safeParse(data, params) {
    const ctx = {
      common: {
        issues: [],
        async: params?.async ?? false,
        contextualErrorMap: params?.errorMap
      },
      path: params?.path || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    const result = this._parseSync({ data, path: ctx.path, parent: ctx });
    return handleResult(ctx, result);
  }
  "~validate"(data) {
    const ctx = {
      common: {
        issues: [],
        async: !!this["~standard"].async
      },
      path: [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    if (!this["~standard"].async) {
      try {
        const result = this._parseSync({ data, path: [], parent: ctx });
        return isValid(result) ? {
          value: result.value
        } : {
          issues: ctx.common.issues
        };
      } catch (err) {
        if (err?.message?.toLowerCase()?.includes("encountered")) {
          this["~standard"].async = true;
        }
        ctx.common = {
          issues: [],
          async: true
        };
      }
    }
    return this._parseAsync({ data, path: [], parent: ctx }).then((result) => isValid(result) ? {
      value: result.value
    } : {
      issues: ctx.common.issues
    });
  }
  async parseAsync(data, params) {
    const result = await this.safeParseAsync(data, params);
    if (result.success)
      return result.data;
    throw result.error;
  }
  async safeParseAsync(data, params) {
    const ctx = {
      common: {
        issues: [],
        contextualErrorMap: params?.errorMap,
        async: true
      },
      path: params?.path || [],
      schemaErrorMap: this._def.errorMap,
      parent: null,
      data,
      parsedType: getParsedType(data)
    };
    const maybeAsyncResult = this._parse({ data, path: ctx.path, parent: ctx });
    const result = await (isAsync(maybeAsyncResult) ? maybeAsyncResult : Promise.resolve(maybeAsyncResult));
    return handleResult(ctx, result);
  }
  refine(check, message) {
    const getIssueProperties = (val) => {
      if (typeof message === "string" || typeof message === "undefined") {
        return { message };
      } else if (typeof message === "function") {
        return message(val);
      } else {
        return message;
      }
    };
    return this._refinement((val, ctx) => {
      const result = check(val);
      const setError = () => ctx.addIssue({
        code: ZodIssueCode.custom,
        ...getIssueProperties(val)
      });
      if (typeof Promise !== "undefined" && result instanceof Promise) {
        return result.then((data) => {
          if (!data) {
            setError();
            return false;
          } else {
            return true;
          }
        });
      }
      if (!result) {
        setError();
        return false;
      } else {
        return true;
      }
    });
  }
  refinement(check, refinementData) {
    return this._refinement((val, ctx) => {
      if (!check(val)) {
        ctx.addIssue(typeof refinementData === "function" ? refinementData(val, ctx) : refinementData);
        return false;
      } else {
        return true;
      }
    });
  }
  _refinement(refinement) {
    return new ZodEffects({
      schema: this,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect: { type: "refinement", refinement }
    });
  }
  superRefine(refinement) {
    return this._refinement(refinement);
  }
  constructor(def) {
    this.spa = this.safeParseAsync;
    this._def = def;
    this.parse = this.parse.bind(this);
    this.safeParse = this.safeParse.bind(this);
    this.parseAsync = this.parseAsync.bind(this);
    this.safeParseAsync = this.safeParseAsync.bind(this);
    this.spa = this.spa.bind(this);
    this.refine = this.refine.bind(this);
    this.refinement = this.refinement.bind(this);
    this.superRefine = this.superRefine.bind(this);
    this.optional = this.optional.bind(this);
    this.nullable = this.nullable.bind(this);
    this.nullish = this.nullish.bind(this);
    this.array = this.array.bind(this);
    this.promise = this.promise.bind(this);
    this.or = this.or.bind(this);
    this.and = this.and.bind(this);
    this.transform = this.transform.bind(this);
    this.brand = this.brand.bind(this);
    this.default = this.default.bind(this);
    this.catch = this.catch.bind(this);
    this.describe = this.describe.bind(this);
    this.pipe = this.pipe.bind(this);
    this.readonly = this.readonly.bind(this);
    this.isNullable = this.isNullable.bind(this);
    this.isOptional = this.isOptional.bind(this);
    this["~standard"] = {
      version: 1,
      vendor: "zod",
      validate: (data) => this["~validate"](data)
    };
  }
  optional() {
    return ZodOptional.create(this, this._def);
  }
  nullable() {
    return ZodNullable.create(this, this._def);
  }
  nullish() {
    return this.nullable().optional();
  }
  array() {
    return ZodArray.create(this);
  }
  promise() {
    return ZodPromise.create(this, this._def);
  }
  or(option) {
    return ZodUnion.create([this, option], this._def);
  }
  and(incoming) {
    return ZodIntersection.create(this, incoming, this._def);
  }
  transform(transform) {
    return new ZodEffects({
      ...processCreateParams(this._def),
      schema: this,
      typeName: ZodFirstPartyTypeKind.ZodEffects,
      effect: { type: "transform", transform }
    });
  }
  default(def) {
    const defaultValueFunc = typeof def === "function" ? def : () => def;
    return new ZodDefault({
      ...processCreateParams(this._def),
      innerType: this,
      defaultValue: defaultValueFunc,
      typeName: ZodFirstPartyTypeKind.ZodDefault
    });
  }
  brand() {
    return new ZodBranded({
      typeName: ZodFirstPartyTypeKind.ZodBranded,
      type: this,
      ...processCreateParams(this._def)
    });
  }
  catch(def) {
    const catchValueFunc = typeof def === "function" ? def : () => def;
    return new ZodCatch({
      ...processCreateParams(this._def),
      innerType: this,
      catchValue: catchValueFunc,
      typeName: ZodFirstPartyTypeKind.ZodCatch
    });
  }
  describe(description) {
    const This = this.constructor;
    return new This({
      ...this._def,
      description
    });
  }
  pipe(target) {
    return ZodPipeline.create(this, target);
  }
  readonly() {
    return ZodReadonly.create(this);
  }
  isOptional() {
    return this.safeParse(void 0).success;
  }
  isNullable() {
    return this.safeParse(null).success;
  }
};
var cuidRegex = /^c[^\s-]{8,}$/i;
var cuid2Regex = /^[0-9a-z]+$/;
var ulidRegex = /^[0-9A-HJKMNP-TV-Z]{26}$/i;
var uuidRegex = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i;
var nanoidRegex = /^[a-z0-9_-]{21}$/i;
var jwtRegex = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/;
var durationRegex = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/;
var emailRegex = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i;
var _emojiRegex = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`;
var emojiRegex;
var ipv4Regex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/;
var ipv4CidrRegex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/;
var ipv6Regex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/;
var ipv6CidrRegex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/;
var base64Regex = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/;
var base64urlRegex = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/;
var dateRegexSource = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`;
var dateRegex = new RegExp(`^${dateRegexSource}$`);
function timeRegexSource(args) {
  let secondsRegexSource = `[0-5]\\d`;
  if (args.precision) {
    secondsRegexSource = `${secondsRegexSource}\\.\\d{${args.precision}}`;
  } else if (args.precision == null) {
    secondsRegexSource = `${secondsRegexSource}(\\.\\d+)?`;
  }
  const secondsQuantifier = args.precision ? "+" : "?";
  return `([01]\\d|2[0-3]):[0-5]\\d(:${secondsRegexSource})${secondsQuantifier}`;
}
function timeRegex(args) {
  return new RegExp(`^${timeRegexSource(args)}$`);
}
function datetimeRegex(args) {
  let regex = `${dateRegexSource}T${timeRegexSource(args)}`;
  const opts = [];
  opts.push(args.local ? `Z?` : `Z`);
  if (args.offset)
    opts.push(`([+-]\\d{2}:?\\d{2})`);
  regex = `${regex}(${opts.join("|")})`;
  return new RegExp(`^${regex}$`);
}
function isValidIP(ip, version) {
  if ((version === "v4" || !version) && ipv4Regex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6Regex.test(ip)) {
    return true;
  }
  return false;
}
function isValidJWT(jwt, alg) {
  if (!jwtRegex.test(jwt))
    return false;
  try {
    const [header] = jwt.split(".");
    if (!header)
      return false;
    const base64 = header.replace(/-/g, "+").replace(/_/g, "/").padEnd(header.length + (4 - header.length % 4) % 4, "=");
    const decoded = JSON.parse(atob(base64));
    if (typeof decoded !== "object" || decoded === null)
      return false;
    if ("typ" in decoded && decoded?.typ !== "JWT")
      return false;
    if (!decoded.alg)
      return false;
    if (alg && decoded.alg !== alg)
      return false;
    return true;
  } catch {
    return false;
  }
}
function isValidCidr(ip, version) {
  if ((version === "v4" || !version) && ipv4CidrRegex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6CidrRegex.test(ip)) {
    return true;
  }
  return false;
}
var ZodString = class _ZodString extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = String(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.string) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.string,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    const status = new ParseStatus();
    let ctx = void 0;
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        if (input.data.length < check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "string",
            inclusive: true,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        if (input.data.length > check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "string",
            inclusive: true,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "length") {
        const tooBig = input.data.length > check.value;
        const tooSmall = input.data.length < check.value;
        if (tooBig || tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          if (tooBig) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: check.value,
              type: "string",
              inclusive: true,
              exact: true,
              message: check.message
            });
          } else if (tooSmall) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: check.value,
              type: "string",
              inclusive: true,
              exact: true,
              message: check.message
            });
          }
          status.dirty();
        }
      } else if (check.kind === "email") {
        if (!emailRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "email",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "emoji") {
        if (!emojiRegex) {
          emojiRegex = new RegExp(_emojiRegex, "u");
        }
        if (!emojiRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "emoji",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "uuid") {
        if (!uuidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "uuid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "nanoid") {
        if (!nanoidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "nanoid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cuid") {
        if (!cuidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cuid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cuid2") {
        if (!cuid2Regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cuid2",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "ulid") {
        if (!ulidRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "ulid",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "url") {
        try {
          new URL(input.data);
        } catch {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "regex") {
        check.regex.lastIndex = 0;
        const testResult = check.regex.test(input.data);
        if (!testResult) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "regex",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "trim") {
        input.data = input.data.trim();
      } else if (check.kind === "includes") {
        if (!input.data.includes(check.value, check.position)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { includes: check.value, position: check.position },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "toLowerCase") {
        input.data = input.data.toLowerCase();
      } else if (check.kind === "toUpperCase") {
        input.data = input.data.toUpperCase();
      } else if (check.kind === "startsWith") {
        if (!input.data.startsWith(check.value)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { startsWith: check.value },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "endsWith") {
        if (!input.data.endsWith(check.value)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: { endsWith: check.value },
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "datetime") {
        const regex = datetimeRegex(check);
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "datetime",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "date") {
        const regex = dateRegex;
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "date",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "time") {
        const regex = timeRegex(check);
        if (!regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_string,
            validation: "time",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "duration") {
        if (!durationRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "duration",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "ip") {
        if (!isValidIP(input.data, check.version)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "ip",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "jwt") {
        if (!isValidJWT(input.data, check.alg)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "jwt",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "cidr") {
        if (!isValidCidr(input.data, check.version)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "cidr",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "base64") {
        if (!base64Regex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "base64",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "base64url") {
        if (!base64urlRegex.test(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            validation: "base64url",
            code: ZodIssueCode.invalid_string,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  _regex(regex, validation, message) {
    return this.refinement((data) => regex.test(data), {
      validation,
      code: ZodIssueCode.invalid_string,
      ...errorUtil.errToObj(message)
    });
  }
  _addCheck(check) {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  email(message) {
    return this._addCheck({ kind: "email", ...errorUtil.errToObj(message) });
  }
  url(message) {
    return this._addCheck({ kind: "url", ...errorUtil.errToObj(message) });
  }
  emoji(message) {
    return this._addCheck({ kind: "emoji", ...errorUtil.errToObj(message) });
  }
  uuid(message) {
    return this._addCheck({ kind: "uuid", ...errorUtil.errToObj(message) });
  }
  nanoid(message) {
    return this._addCheck({ kind: "nanoid", ...errorUtil.errToObj(message) });
  }
  cuid(message) {
    return this._addCheck({ kind: "cuid", ...errorUtil.errToObj(message) });
  }
  cuid2(message) {
    return this._addCheck({ kind: "cuid2", ...errorUtil.errToObj(message) });
  }
  ulid(message) {
    return this._addCheck({ kind: "ulid", ...errorUtil.errToObj(message) });
  }
  base64(message) {
    return this._addCheck({ kind: "base64", ...errorUtil.errToObj(message) });
  }
  base64url(message) {
    return this._addCheck({
      kind: "base64url",
      ...errorUtil.errToObj(message)
    });
  }
  jwt(options) {
    return this._addCheck({ kind: "jwt", ...errorUtil.errToObj(options) });
  }
  ip(options) {
    return this._addCheck({ kind: "ip", ...errorUtil.errToObj(options) });
  }
  cidr(options) {
    return this._addCheck({ kind: "cidr", ...errorUtil.errToObj(options) });
  }
  datetime(options) {
    if (typeof options === "string") {
      return this._addCheck({
        kind: "datetime",
        precision: null,
        offset: false,
        local: false,
        message: options
      });
    }
    return this._addCheck({
      kind: "datetime",
      precision: typeof options?.precision === "undefined" ? null : options?.precision,
      offset: options?.offset ?? false,
      local: options?.local ?? false,
      ...errorUtil.errToObj(options?.message)
    });
  }
  date(message) {
    return this._addCheck({ kind: "date", message });
  }
  time(options) {
    if (typeof options === "string") {
      return this._addCheck({
        kind: "time",
        precision: null,
        message: options
      });
    }
    return this._addCheck({
      kind: "time",
      precision: typeof options?.precision === "undefined" ? null : options?.precision,
      ...errorUtil.errToObj(options?.message)
    });
  }
  duration(message) {
    return this._addCheck({ kind: "duration", ...errorUtil.errToObj(message) });
  }
  regex(regex, message) {
    return this._addCheck({
      kind: "regex",
      regex,
      ...errorUtil.errToObj(message)
    });
  }
  includes(value, options) {
    return this._addCheck({
      kind: "includes",
      value,
      position: options?.position,
      ...errorUtil.errToObj(options?.message)
    });
  }
  startsWith(value, message) {
    return this._addCheck({
      kind: "startsWith",
      value,
      ...errorUtil.errToObj(message)
    });
  }
  endsWith(value, message) {
    return this._addCheck({
      kind: "endsWith",
      value,
      ...errorUtil.errToObj(message)
    });
  }
  min(minLength, message) {
    return this._addCheck({
      kind: "min",
      value: minLength,
      ...errorUtil.errToObj(message)
    });
  }
  max(maxLength, message) {
    return this._addCheck({
      kind: "max",
      value: maxLength,
      ...errorUtil.errToObj(message)
    });
  }
  length(len, message) {
    return this._addCheck({
      kind: "length",
      value: len,
      ...errorUtil.errToObj(message)
    });
  }
  /**
   * Equivalent to `.min(1)`
   */
  nonempty(message) {
    return this.min(1, errorUtil.errToObj(message));
  }
  trim() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "trim" }]
    });
  }
  toLowerCase() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "toLowerCase" }]
    });
  }
  toUpperCase() {
    return new _ZodString({
      ...this._def,
      checks: [...this._def.checks, { kind: "toUpperCase" }]
    });
  }
  get isDatetime() {
    return !!this._def.checks.find((ch) => ch.kind === "datetime");
  }
  get isDate() {
    return !!this._def.checks.find((ch) => ch.kind === "date");
  }
  get isTime() {
    return !!this._def.checks.find((ch) => ch.kind === "time");
  }
  get isDuration() {
    return !!this._def.checks.find((ch) => ch.kind === "duration");
  }
  get isEmail() {
    return !!this._def.checks.find((ch) => ch.kind === "email");
  }
  get isURL() {
    return !!this._def.checks.find((ch) => ch.kind === "url");
  }
  get isEmoji() {
    return !!this._def.checks.find((ch) => ch.kind === "emoji");
  }
  get isUUID() {
    return !!this._def.checks.find((ch) => ch.kind === "uuid");
  }
  get isNANOID() {
    return !!this._def.checks.find((ch) => ch.kind === "nanoid");
  }
  get isCUID() {
    return !!this._def.checks.find((ch) => ch.kind === "cuid");
  }
  get isCUID2() {
    return !!this._def.checks.find((ch) => ch.kind === "cuid2");
  }
  get isULID() {
    return !!this._def.checks.find((ch) => ch.kind === "ulid");
  }
  get isIP() {
    return !!this._def.checks.find((ch) => ch.kind === "ip");
  }
  get isCIDR() {
    return !!this._def.checks.find((ch) => ch.kind === "cidr");
  }
  get isBase64() {
    return !!this._def.checks.find((ch) => ch.kind === "base64");
  }
  get isBase64url() {
    return !!this._def.checks.find((ch) => ch.kind === "base64url");
  }
  get minLength() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxLength() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
};
ZodString.create = (params) => {
  return new ZodString({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodString,
    coerce: params?.coerce ?? false,
    ...processCreateParams(params)
  });
};
function floatSafeRemainder(val, step) {
  const valDecCount = (val.toString().split(".")[1] || "").length;
  const stepDecCount = (step.toString().split(".")[1] || "").length;
  const decCount = valDecCount > stepDecCount ? valDecCount : stepDecCount;
  const valInt = Number.parseInt(val.toFixed(decCount).replace(".", ""));
  const stepInt = Number.parseInt(step.toFixed(decCount).replace(".", ""));
  return valInt % stepInt / 10 ** decCount;
}
var ZodNumber = class _ZodNumber extends ZodType {
  constructor() {
    super(...arguments);
    this.min = this.gte;
    this.max = this.lte;
    this.step = this.multipleOf;
  }
  _parse(input) {
    if (this._def.coerce) {
      input.data = Number(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.number) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.number,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    let ctx = void 0;
    const status = new ParseStatus();
    for (const check of this._def.checks) {
      if (check.kind === "int") {
        if (!util.isInteger(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: "integer",
            received: "float",
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "min") {
        const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
        if (tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
        if (tooBig) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: check.value,
            type: "number",
            inclusive: check.inclusive,
            exact: false,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "multipleOf") {
        if (floatSafeRemainder(input.data, check.value) !== 0) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "finite") {
        if (!Number.isFinite(input.data)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_finite,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  gte(value, message) {
    return this.setLimit("min", value, true, errorUtil.toString(message));
  }
  gt(value, message) {
    return this.setLimit("min", value, false, errorUtil.toString(message));
  }
  lte(value, message) {
    return this.setLimit("max", value, true, errorUtil.toString(message));
  }
  lt(value, message) {
    return this.setLimit("max", value, false, errorUtil.toString(message));
  }
  setLimit(kind, value, inclusive, message) {
    return new _ZodNumber({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind,
          value,
          inclusive,
          message: errorUtil.toString(message)
        }
      ]
    });
  }
  _addCheck(check) {
    return new _ZodNumber({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  int(message) {
    return this._addCheck({
      kind: "int",
      message: errorUtil.toString(message)
    });
  }
  positive(message) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  negative(message) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  nonpositive(message) {
    return this._addCheck({
      kind: "max",
      value: 0,
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  nonnegative(message) {
    return this._addCheck({
      kind: "min",
      value: 0,
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  multipleOf(value, message) {
    return this._addCheck({
      kind: "multipleOf",
      value,
      message: errorUtil.toString(message)
    });
  }
  finite(message) {
    return this._addCheck({
      kind: "finite",
      message: errorUtil.toString(message)
    });
  }
  safe(message) {
    return this._addCheck({
      kind: "min",
      inclusive: true,
      value: Number.MIN_SAFE_INTEGER,
      message: errorUtil.toString(message)
    })._addCheck({
      kind: "max",
      inclusive: true,
      value: Number.MAX_SAFE_INTEGER,
      message: errorUtil.toString(message)
    });
  }
  get minValue() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxValue() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
  get isInt() {
    return !!this._def.checks.find((ch) => ch.kind === "int" || ch.kind === "multipleOf" && util.isInteger(ch.value));
  }
  get isFinite() {
    let max = null;
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "finite" || ch.kind === "int" || ch.kind === "multipleOf") {
        return true;
      } else if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      } else if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return Number.isFinite(min) && Number.isFinite(max);
  }
};
ZodNumber.create = (params) => {
  return new ZodNumber({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodNumber,
    coerce: params?.coerce || false,
    ...processCreateParams(params)
  });
};
var ZodBigInt = class _ZodBigInt extends ZodType {
  constructor() {
    super(...arguments);
    this.min = this.gte;
    this.max = this.lte;
  }
  _parse(input) {
    if (this._def.coerce) {
      try {
        input.data = BigInt(input.data);
      } catch {
        return this._getInvalidInput(input);
      }
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.bigint) {
      return this._getInvalidInput(input);
    }
    let ctx = void 0;
    const status = new ParseStatus();
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
        if (tooSmall) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            type: "bigint",
            minimum: check.value,
            inclusive: check.inclusive,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
        if (tooBig) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            type: "bigint",
            maximum: check.value,
            inclusive: check.inclusive,
            message: check.message
          });
          status.dirty();
        }
      } else if (check.kind === "multipleOf") {
        if (input.data % check.value !== BigInt(0)) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.not_multiple_of,
            multipleOf: check.value,
            message: check.message
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return { status: status.value, value: input.data };
  }
  _getInvalidInput(input) {
    const ctx = this._getOrReturnCtx(input);
    addIssueToContext(ctx, {
      code: ZodIssueCode.invalid_type,
      expected: ZodParsedType.bigint,
      received: ctx.parsedType
    });
    return INVALID;
  }
  gte(value, message) {
    return this.setLimit("min", value, true, errorUtil.toString(message));
  }
  gt(value, message) {
    return this.setLimit("min", value, false, errorUtil.toString(message));
  }
  lte(value, message) {
    return this.setLimit("max", value, true, errorUtil.toString(message));
  }
  lt(value, message) {
    return this.setLimit("max", value, false, errorUtil.toString(message));
  }
  setLimit(kind, value, inclusive, message) {
    return new _ZodBigInt({
      ...this._def,
      checks: [
        ...this._def.checks,
        {
          kind,
          value,
          inclusive,
          message: errorUtil.toString(message)
        }
      ]
    });
  }
  _addCheck(check) {
    return new _ZodBigInt({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  positive(message) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  negative(message) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: false,
      message: errorUtil.toString(message)
    });
  }
  nonpositive(message) {
    return this._addCheck({
      kind: "max",
      value: BigInt(0),
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  nonnegative(message) {
    return this._addCheck({
      kind: "min",
      value: BigInt(0),
      inclusive: true,
      message: errorUtil.toString(message)
    });
  }
  multipleOf(value, message) {
    return this._addCheck({
      kind: "multipleOf",
      value,
      message: errorUtil.toString(message)
    });
  }
  get minValue() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min;
  }
  get maxValue() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max;
  }
};
ZodBigInt.create = (params) => {
  return new ZodBigInt({
    checks: [],
    typeName: ZodFirstPartyTypeKind.ZodBigInt,
    coerce: params?.coerce ?? false,
    ...processCreateParams(params)
  });
};
var ZodBoolean = class extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = Boolean(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.boolean) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.boolean,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodBoolean.create = (params) => {
  return new ZodBoolean({
    typeName: ZodFirstPartyTypeKind.ZodBoolean,
    coerce: params?.coerce || false,
    ...processCreateParams(params)
  });
};
var ZodDate = class _ZodDate extends ZodType {
  _parse(input) {
    if (this._def.coerce) {
      input.data = new Date(input.data);
    }
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.date) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.date,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    if (Number.isNaN(input.data.getTime())) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_date
      });
      return INVALID;
    }
    const status = new ParseStatus();
    let ctx = void 0;
    for (const check of this._def.checks) {
      if (check.kind === "min") {
        if (input.data.getTime() < check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            message: check.message,
            inclusive: true,
            exact: false,
            minimum: check.value,
            type: "date"
          });
          status.dirty();
        }
      } else if (check.kind === "max") {
        if (input.data.getTime() > check.value) {
          ctx = this._getOrReturnCtx(input, ctx);
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            message: check.message,
            inclusive: true,
            exact: false,
            maximum: check.value,
            type: "date"
          });
          status.dirty();
        }
      } else {
        util.assertNever(check);
      }
    }
    return {
      status: status.value,
      value: new Date(input.data.getTime())
    };
  }
  _addCheck(check) {
    return new _ZodDate({
      ...this._def,
      checks: [...this._def.checks, check]
    });
  }
  min(minDate, message) {
    return this._addCheck({
      kind: "min",
      value: minDate.getTime(),
      message: errorUtil.toString(message)
    });
  }
  max(maxDate, message) {
    return this._addCheck({
      kind: "max",
      value: maxDate.getTime(),
      message: errorUtil.toString(message)
    });
  }
  get minDate() {
    let min = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "min") {
        if (min === null || ch.value > min)
          min = ch.value;
      }
    }
    return min != null ? new Date(min) : null;
  }
  get maxDate() {
    let max = null;
    for (const ch of this._def.checks) {
      if (ch.kind === "max") {
        if (max === null || ch.value < max)
          max = ch.value;
      }
    }
    return max != null ? new Date(max) : null;
  }
};
ZodDate.create = (params) => {
  return new ZodDate({
    checks: [],
    coerce: params?.coerce || false,
    typeName: ZodFirstPartyTypeKind.ZodDate,
    ...processCreateParams(params)
  });
};
var ZodSymbol = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.symbol) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.symbol,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodSymbol.create = (params) => {
  return new ZodSymbol({
    typeName: ZodFirstPartyTypeKind.ZodSymbol,
    ...processCreateParams(params)
  });
};
var ZodUndefined = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.undefined) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.undefined,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodUndefined.create = (params) => {
  return new ZodUndefined({
    typeName: ZodFirstPartyTypeKind.ZodUndefined,
    ...processCreateParams(params)
  });
};
var ZodNull = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.null) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.null,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodNull.create = (params) => {
  return new ZodNull({
    typeName: ZodFirstPartyTypeKind.ZodNull,
    ...processCreateParams(params)
  });
};
var ZodAny = class extends ZodType {
  constructor() {
    super(...arguments);
    this._any = true;
  }
  _parse(input) {
    return OK(input.data);
  }
};
ZodAny.create = (params) => {
  return new ZodAny({
    typeName: ZodFirstPartyTypeKind.ZodAny,
    ...processCreateParams(params)
  });
};
var ZodUnknown = class extends ZodType {
  constructor() {
    super(...arguments);
    this._unknown = true;
  }
  _parse(input) {
    return OK(input.data);
  }
};
ZodUnknown.create = (params) => {
  return new ZodUnknown({
    typeName: ZodFirstPartyTypeKind.ZodUnknown,
    ...processCreateParams(params)
  });
};
var ZodNever = class extends ZodType {
  _parse(input) {
    const ctx = this._getOrReturnCtx(input);
    addIssueToContext(ctx, {
      code: ZodIssueCode.invalid_type,
      expected: ZodParsedType.never,
      received: ctx.parsedType
    });
    return INVALID;
  }
};
ZodNever.create = (params) => {
  return new ZodNever({
    typeName: ZodFirstPartyTypeKind.ZodNever,
    ...processCreateParams(params)
  });
};
var ZodVoid = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.undefined) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.void,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return OK(input.data);
  }
};
ZodVoid.create = (params) => {
  return new ZodVoid({
    typeName: ZodFirstPartyTypeKind.ZodVoid,
    ...processCreateParams(params)
  });
};
var ZodArray = class _ZodArray extends ZodType {
  _parse(input) {
    const { ctx, status } = this._processInputParams(input);
    const def = this._def;
    if (ctx.parsedType !== ZodParsedType.array) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.array,
        received: ctx.parsedType
      });
      return INVALID;
    }
    if (def.exactLength !== null) {
      const tooBig = ctx.data.length > def.exactLength.value;
      const tooSmall = ctx.data.length < def.exactLength.value;
      if (tooBig || tooSmall) {
        addIssueToContext(ctx, {
          code: tooBig ? ZodIssueCode.too_big : ZodIssueCode.too_small,
          minimum: tooSmall ? def.exactLength.value : void 0,
          maximum: tooBig ? def.exactLength.value : void 0,
          type: "array",
          inclusive: true,
          exact: true,
          message: def.exactLength.message
        });
        status.dirty();
      }
    }
    if (def.minLength !== null) {
      if (ctx.data.length < def.minLength.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minLength.value,
          type: "array",
          inclusive: true,
          exact: false,
          message: def.minLength.message
        });
        status.dirty();
      }
    }
    if (def.maxLength !== null) {
      if (ctx.data.length > def.maxLength.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxLength.value,
          type: "array",
          inclusive: true,
          exact: false,
          message: def.maxLength.message
        });
        status.dirty();
      }
    }
    if (ctx.common.async) {
      return Promise.all([...ctx.data].map((item, i) => {
        return def.type._parseAsync(new ParseInputLazyPath(ctx, item, ctx.path, i));
      })).then((result2) => {
        return ParseStatus.mergeArray(status, result2);
      });
    }
    const result = [...ctx.data].map((item, i) => {
      return def.type._parseSync(new ParseInputLazyPath(ctx, item, ctx.path, i));
    });
    return ParseStatus.mergeArray(status, result);
  }
  get element() {
    return this._def.type;
  }
  min(minLength, message) {
    return new _ZodArray({
      ...this._def,
      minLength: { value: minLength, message: errorUtil.toString(message) }
    });
  }
  max(maxLength, message) {
    return new _ZodArray({
      ...this._def,
      maxLength: { value: maxLength, message: errorUtil.toString(message) }
    });
  }
  length(len, message) {
    return new _ZodArray({
      ...this._def,
      exactLength: { value: len, message: errorUtil.toString(message) }
    });
  }
  nonempty(message) {
    return this.min(1, message);
  }
};
ZodArray.create = (schema, params) => {
  return new ZodArray({
    type: schema,
    minLength: null,
    maxLength: null,
    exactLength: null,
    typeName: ZodFirstPartyTypeKind.ZodArray,
    ...processCreateParams(params)
  });
};
function deepPartialify(schema) {
  if (schema instanceof ZodObject) {
    const newShape = {};
    for (const key in schema.shape) {
      const fieldSchema = schema.shape[key];
      newShape[key] = ZodOptional.create(deepPartialify(fieldSchema));
    }
    return new ZodObject({
      ...schema._def,
      shape: () => newShape
    });
  } else if (schema instanceof ZodArray) {
    return new ZodArray({
      ...schema._def,
      type: deepPartialify(schema.element)
    });
  } else if (schema instanceof ZodOptional) {
    return ZodOptional.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodNullable) {
    return ZodNullable.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodTuple) {
    return ZodTuple.create(schema.items.map((item) => deepPartialify(item)));
  } else {
    return schema;
  }
}
var ZodObject = class _ZodObject extends ZodType {
  constructor() {
    super(...arguments);
    this._cached = null;
    this.nonstrict = this.passthrough;
    this.augment = this.extend;
  }
  _getCached() {
    if (this._cached !== null)
      return this._cached;
    const shape = this._def.shape();
    const keys = util.objectKeys(shape);
    this._cached = { shape, keys };
    return this._cached;
  }
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.object) {
      const ctx2 = this._getOrReturnCtx(input);
      addIssueToContext(ctx2, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx2.parsedType
      });
      return INVALID;
    }
    const { status, ctx } = this._processInputParams(input);
    const { shape, keys: shapeKeys } = this._getCached();
    const extraKeys = [];
    if (!(this._def.catchall instanceof ZodNever && this._def.unknownKeys === "strip")) {
      for (const key in ctx.data) {
        if (!shapeKeys.includes(key)) {
          extraKeys.push(key);
        }
      }
    }
    const pairs = [];
    for (const key of shapeKeys) {
      const keyValidator = shape[key];
      const value = ctx.data[key];
      pairs.push({
        key: { status: "valid", value: key },
        value: keyValidator._parse(new ParseInputLazyPath(ctx, value, ctx.path, key)),
        alwaysSet: key in ctx.data
      });
    }
    if (this._def.catchall instanceof ZodNever) {
      const unknownKeys = this._def.unknownKeys;
      if (unknownKeys === "passthrough") {
        for (const key of extraKeys) {
          pairs.push({
            key: { status: "valid", value: key },
            value: { status: "valid", value: ctx.data[key] }
          });
        }
      } else if (unknownKeys === "strict") {
        if (extraKeys.length > 0) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.unrecognized_keys,
            keys: extraKeys
          });
          status.dirty();
        }
      } else if (unknownKeys === "strip") {
      } else {
        throw new Error(`Internal ZodObject error: invalid unknownKeys value.`);
      }
    } else {
      const catchall = this._def.catchall;
      for (const key of extraKeys) {
        const value = ctx.data[key];
        pairs.push({
          key: { status: "valid", value: key },
          value: catchall._parse(
            new ParseInputLazyPath(ctx, value, ctx.path, key)
            //, ctx.child(key), value, getParsedType(value)
          ),
          alwaysSet: key in ctx.data
        });
      }
    }
    if (ctx.common.async) {
      return Promise.resolve().then(async () => {
        const syncPairs = [];
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          syncPairs.push({
            key,
            value,
            alwaysSet: pair.alwaysSet
          });
        }
        return syncPairs;
      }).then((syncPairs) => {
        return ParseStatus.mergeObjectSync(status, syncPairs);
      });
    } else {
      return ParseStatus.mergeObjectSync(status, pairs);
    }
  }
  get shape() {
    return this._def.shape();
  }
  strict(message) {
    errorUtil.errToObj;
    return new _ZodObject({
      ...this._def,
      unknownKeys: "strict",
      ...message !== void 0 ? {
        errorMap: (issue, ctx) => {
          const defaultError = this._def.errorMap?.(issue, ctx).message ?? ctx.defaultError;
          if (issue.code === "unrecognized_keys")
            return {
              message: errorUtil.errToObj(message).message ?? defaultError
            };
          return {
            message: defaultError
          };
        }
      } : {}
    });
  }
  strip() {
    return new _ZodObject({
      ...this._def,
      unknownKeys: "strip"
    });
  }
  passthrough() {
    return new _ZodObject({
      ...this._def,
      unknownKeys: "passthrough"
    });
  }
  // const AugmentFactory =
  //   <Def extends ZodObjectDef>(def: Def) =>
  //   <Augmentation extends ZodRawShape>(
  //     augmentation: Augmentation
  //   ): ZodObject<
  //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
  //     Def["unknownKeys"],
  //     Def["catchall"]
  //   > => {
  //     return new ZodObject({
  //       ...def,
  //       shape: () => ({
  //         ...def.shape(),
  //         ...augmentation,
  //       }),
  //     }) as any;
  //   };
  extend(augmentation) {
    return new _ZodObject({
      ...this._def,
      shape: () => ({
        ...this._def.shape(),
        ...augmentation
      })
    });
  }
  /**
   * Prior to zod@1.0.12 there was a bug in the
   * inferred type of merged objects. Please
   * upgrade if you are experiencing issues.
   */
  merge(merging) {
    const merged = new _ZodObject({
      unknownKeys: merging._def.unknownKeys,
      catchall: merging._def.catchall,
      shape: () => ({
        ...this._def.shape(),
        ...merging._def.shape()
      }),
      typeName: ZodFirstPartyTypeKind.ZodObject
    });
    return merged;
  }
  // merge<
  //   Incoming extends AnyZodObject,
  //   Augmentation extends Incoming["shape"],
  //   NewOutput extends {
  //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
  //       ? Augmentation[k]["_output"]
  //       : k extends keyof Output
  //       ? Output[k]
  //       : never;
  //   },
  //   NewInput extends {
  //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
  //       ? Augmentation[k]["_input"]
  //       : k extends keyof Input
  //       ? Input[k]
  //       : never;
  //   }
  // >(
  //   merging: Incoming
  // ): ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"],
  //   NewOutput,
  //   NewInput
  // > {
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  setKey(key, schema) {
    return this.augment({ [key]: schema });
  }
  // merge<Incoming extends AnyZodObject>(
  //   merging: Incoming
  // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
  // ZodObject<
  //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
  //   Incoming["_def"]["unknownKeys"],
  //   Incoming["_def"]["catchall"]
  // > {
  //   // const mergedShape = objectUtil.mergeShapes(
  //   //   this._def.shape(),
  //   //   merging._def.shape()
  //   // );
  //   const merged: any = new ZodObject({
  //     unknownKeys: merging._def.unknownKeys,
  //     catchall: merging._def.catchall,
  //     shape: () =>
  //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
  //     typeName: ZodFirstPartyTypeKind.ZodObject,
  //   }) as any;
  //   return merged;
  // }
  catchall(index) {
    return new _ZodObject({
      ...this._def,
      catchall: index
    });
  }
  pick(mask) {
    const shape = {};
    for (const key of util.objectKeys(mask)) {
      if (mask[key] && this.shape[key]) {
        shape[key] = this.shape[key];
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => shape
    });
  }
  omit(mask) {
    const shape = {};
    for (const key of util.objectKeys(this.shape)) {
      if (!mask[key]) {
        shape[key] = this.shape[key];
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => shape
    });
  }
  /**
   * @deprecated
   */
  deepPartial() {
    return deepPartialify(this);
  }
  partial(mask) {
    const newShape = {};
    for (const key of util.objectKeys(this.shape)) {
      const fieldSchema = this.shape[key];
      if (mask && !mask[key]) {
        newShape[key] = fieldSchema;
      } else {
        newShape[key] = fieldSchema.optional();
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => newShape
    });
  }
  required(mask) {
    const newShape = {};
    for (const key of util.objectKeys(this.shape)) {
      if (mask && !mask[key]) {
        newShape[key] = this.shape[key];
      } else {
        const fieldSchema = this.shape[key];
        let newField = fieldSchema;
        while (newField instanceof ZodOptional) {
          newField = newField._def.innerType;
        }
        newShape[key] = newField;
      }
    }
    return new _ZodObject({
      ...this._def,
      shape: () => newShape
    });
  }
  keyof() {
    return createZodEnum(util.objectKeys(this.shape));
  }
};
ZodObject.create = (shape, params) => {
  return new ZodObject({
    shape: () => shape,
    unknownKeys: "strip",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
ZodObject.strictCreate = (shape, params) => {
  return new ZodObject({
    shape: () => shape,
    unknownKeys: "strict",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
ZodObject.lazycreate = (shape, params) => {
  return new ZodObject({
    shape,
    unknownKeys: "strip",
    catchall: ZodNever.create(),
    typeName: ZodFirstPartyTypeKind.ZodObject,
    ...processCreateParams(params)
  });
};
var ZodUnion = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const options = this._def.options;
    function handleResults(results) {
      for (const result of results) {
        if (result.result.status === "valid") {
          return result.result;
        }
      }
      for (const result of results) {
        if (result.result.status === "dirty") {
          ctx.common.issues.push(...result.ctx.common.issues);
          return result.result;
        }
      }
      const unionErrors = results.map((result) => new ZodError(result.ctx.common.issues));
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union,
        unionErrors
      });
      return INVALID;
    }
    if (ctx.common.async) {
      return Promise.all(options.map(async (option) => {
        const childCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          },
          parent: null
        };
        return {
          result: await option._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: childCtx
          }),
          ctx: childCtx
        };
      })).then(handleResults);
    } else {
      let dirty = void 0;
      const issues = [];
      for (const option of options) {
        const childCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          },
          parent: null
        };
        const result = option._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: childCtx
        });
        if (result.status === "valid") {
          return result;
        } else if (result.status === "dirty" && !dirty) {
          dirty = { result, ctx: childCtx };
        }
        if (childCtx.common.issues.length) {
          issues.push(childCtx.common.issues);
        }
      }
      if (dirty) {
        ctx.common.issues.push(...dirty.ctx.common.issues);
        return dirty.result;
      }
      const unionErrors = issues.map((issues2) => new ZodError(issues2));
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union,
        unionErrors
      });
      return INVALID;
    }
  }
  get options() {
    return this._def.options;
  }
};
ZodUnion.create = (types, params) => {
  return new ZodUnion({
    options: types,
    typeName: ZodFirstPartyTypeKind.ZodUnion,
    ...processCreateParams(params)
  });
};
var getDiscriminator = (type) => {
  if (type instanceof ZodLazy) {
    return getDiscriminator(type.schema);
  } else if (type instanceof ZodEffects) {
    return getDiscriminator(type.innerType());
  } else if (type instanceof ZodLiteral) {
    return [type.value];
  } else if (type instanceof ZodEnum) {
    return type.options;
  } else if (type instanceof ZodNativeEnum) {
    return util.objectValues(type.enum);
  } else if (type instanceof ZodDefault) {
    return getDiscriminator(type._def.innerType);
  } else if (type instanceof ZodUndefined) {
    return [void 0];
  } else if (type instanceof ZodNull) {
    return [null];
  } else if (type instanceof ZodOptional) {
    return [void 0, ...getDiscriminator(type.unwrap())];
  } else if (type instanceof ZodNullable) {
    return [null, ...getDiscriminator(type.unwrap())];
  } else if (type instanceof ZodBranded) {
    return getDiscriminator(type.unwrap());
  } else if (type instanceof ZodReadonly) {
    return getDiscriminator(type.unwrap());
  } else if (type instanceof ZodCatch) {
    return getDiscriminator(type._def.innerType);
  } else {
    return [];
  }
};
var ZodDiscriminatedUnion = class _ZodDiscriminatedUnion extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.object) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const discriminator = this.discriminator;
    const discriminatorValue = ctx.data[discriminator];
    const option = this.optionsMap.get(discriminatorValue);
    if (!option) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_union_discriminator,
        options: Array.from(this.optionsMap.keys()),
        path: [discriminator]
      });
      return INVALID;
    }
    if (ctx.common.async) {
      return option._parseAsync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
    } else {
      return option._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
    }
  }
  get discriminator() {
    return this._def.discriminator;
  }
  get options() {
    return this._def.options;
  }
  get optionsMap() {
    return this._def.optionsMap;
  }
  /**
   * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
   * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
   * have a different value for each object in the union.
   * @param discriminator the name of the discriminator property
   * @param types an array of object schemas
   * @param params
   */
  static create(discriminator, options, params) {
    const optionsMap = /* @__PURE__ */ new Map();
    for (const type of options) {
      const discriminatorValues = getDiscriminator(type.shape[discriminator]);
      if (!discriminatorValues.length) {
        throw new Error(`A discriminator value for key \`${discriminator}\` could not be extracted from all schema options`);
      }
      for (const value of discriminatorValues) {
        if (optionsMap.has(value)) {
          throw new Error(`Discriminator property ${String(discriminator)} has duplicate value ${String(value)}`);
        }
        optionsMap.set(value, type);
      }
    }
    return new _ZodDiscriminatedUnion({
      typeName: ZodFirstPartyTypeKind.ZodDiscriminatedUnion,
      discriminator,
      options,
      optionsMap,
      ...processCreateParams(params)
    });
  }
};
function mergeValues(a, b) {
  const aType = getParsedType(a);
  const bType = getParsedType(b);
  if (a === b) {
    return { valid: true, data: a };
  } else if (aType === ZodParsedType.object && bType === ZodParsedType.object) {
    const bKeys = util.objectKeys(b);
    const sharedKeys = util.objectKeys(a).filter((key) => bKeys.indexOf(key) !== -1);
    const newObj = { ...a, ...b };
    for (const key of sharedKeys) {
      const sharedValue = mergeValues(a[key], b[key]);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newObj[key] = sharedValue.data;
    }
    return { valid: true, data: newObj };
  } else if (aType === ZodParsedType.array && bType === ZodParsedType.array) {
    if (a.length !== b.length) {
      return { valid: false };
    }
    const newArray = [];
    for (let index = 0; index < a.length; index++) {
      const itemA = a[index];
      const itemB = b[index];
      const sharedValue = mergeValues(itemA, itemB);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newArray.push(sharedValue.data);
    }
    return { valid: true, data: newArray };
  } else if (aType === ZodParsedType.date && bType === ZodParsedType.date && +a === +b) {
    return { valid: true, data: a };
  } else {
    return { valid: false };
  }
}
var ZodIntersection = class extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    const handleParsed = (parsedLeft, parsedRight) => {
      if (isAborted(parsedLeft) || isAborted(parsedRight)) {
        return INVALID;
      }
      const merged = mergeValues(parsedLeft.value, parsedRight.value);
      if (!merged.valid) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_intersection_types
        });
        return INVALID;
      }
      if (isDirty(parsedLeft) || isDirty(parsedRight)) {
        status.dirty();
      }
      return { status: status.value, value: merged.data };
    };
    if (ctx.common.async) {
      return Promise.all([
        this._def.left._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        }),
        this._def.right._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        })
      ]).then(([left, right]) => handleParsed(left, right));
    } else {
      return handleParsed(this._def.left._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      }), this._def.right._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      }));
    }
  }
};
ZodIntersection.create = (left, right, params) => {
  return new ZodIntersection({
    left,
    right,
    typeName: ZodFirstPartyTypeKind.ZodIntersection,
    ...processCreateParams(params)
  });
};
var ZodTuple = class _ZodTuple extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.array) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.array,
        received: ctx.parsedType
      });
      return INVALID;
    }
    if (ctx.data.length < this._def.items.length) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.too_small,
        minimum: this._def.items.length,
        inclusive: true,
        exact: false,
        type: "array"
      });
      return INVALID;
    }
    const rest = this._def.rest;
    if (!rest && ctx.data.length > this._def.items.length) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.too_big,
        maximum: this._def.items.length,
        inclusive: true,
        exact: false,
        type: "array"
      });
      status.dirty();
    }
    const items = [...ctx.data].map((item, itemIndex) => {
      const schema = this._def.items[itemIndex] || this._def.rest;
      if (!schema)
        return null;
      return schema._parse(new ParseInputLazyPath(ctx, item, ctx.path, itemIndex));
    }).filter((x) => !!x);
    if (ctx.common.async) {
      return Promise.all(items).then((results) => {
        return ParseStatus.mergeArray(status, results);
      });
    } else {
      return ParseStatus.mergeArray(status, items);
    }
  }
  get items() {
    return this._def.items;
  }
  rest(rest) {
    return new _ZodTuple({
      ...this._def,
      rest
    });
  }
};
ZodTuple.create = (schemas, params) => {
  if (!Array.isArray(schemas)) {
    throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
  }
  return new ZodTuple({
    items: schemas,
    typeName: ZodFirstPartyTypeKind.ZodTuple,
    rest: null,
    ...processCreateParams(params)
  });
};
var ZodRecord = class _ZodRecord extends ZodType {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.object) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.object,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const pairs = [];
    const keyType = this._def.keyType;
    const valueType = this._def.valueType;
    for (const key in ctx.data) {
      pairs.push({
        key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, key)),
        value: valueType._parse(new ParseInputLazyPath(ctx, ctx.data[key], ctx.path, key)),
        alwaysSet: key in ctx.data
      });
    }
    if (ctx.common.async) {
      return ParseStatus.mergeObjectAsync(status, pairs);
    } else {
      return ParseStatus.mergeObjectSync(status, pairs);
    }
  }
  get element() {
    return this._def.valueType;
  }
  static create(first, second, third) {
    if (second instanceof ZodType) {
      return new _ZodRecord({
        keyType: first,
        valueType: second,
        typeName: ZodFirstPartyTypeKind.ZodRecord,
        ...processCreateParams(third)
      });
    }
    return new _ZodRecord({
      keyType: ZodString.create(),
      valueType: first,
      typeName: ZodFirstPartyTypeKind.ZodRecord,
      ...processCreateParams(second)
    });
  }
};
var ZodMap = class extends ZodType {
  get keySchema() {
    return this._def.keyType;
  }
  get valueSchema() {
    return this._def.valueType;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.map) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.map,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const keyType = this._def.keyType;
    const valueType = this._def.valueType;
    const pairs = [...ctx.data.entries()].map(([key, value], index) => {
      return {
        key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, [index, "key"])),
        value: valueType._parse(new ParseInputLazyPath(ctx, value, ctx.path, [index, "value"]))
      };
    });
    if (ctx.common.async) {
      const finalMap = /* @__PURE__ */ new Map();
      return Promise.resolve().then(async () => {
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          if (key.status === "aborted" || value.status === "aborted") {
            return INVALID;
          }
          if (key.status === "dirty" || value.status === "dirty") {
            status.dirty();
          }
          finalMap.set(key.value, value.value);
        }
        return { status: status.value, value: finalMap };
      });
    } else {
      const finalMap = /* @__PURE__ */ new Map();
      for (const pair of pairs) {
        const key = pair.key;
        const value = pair.value;
        if (key.status === "aborted" || value.status === "aborted") {
          return INVALID;
        }
        if (key.status === "dirty" || value.status === "dirty") {
          status.dirty();
        }
        finalMap.set(key.value, value.value);
      }
      return { status: status.value, value: finalMap };
    }
  }
};
ZodMap.create = (keyType, valueType, params) => {
  return new ZodMap({
    valueType,
    keyType,
    typeName: ZodFirstPartyTypeKind.ZodMap,
    ...processCreateParams(params)
  });
};
var ZodSet = class _ZodSet extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.set) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.set,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const def = this._def;
    if (def.minSize !== null) {
      if (ctx.data.size < def.minSize.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_small,
          minimum: def.minSize.value,
          type: "set",
          inclusive: true,
          exact: false,
          message: def.minSize.message
        });
        status.dirty();
      }
    }
    if (def.maxSize !== null) {
      if (ctx.data.size > def.maxSize.value) {
        addIssueToContext(ctx, {
          code: ZodIssueCode.too_big,
          maximum: def.maxSize.value,
          type: "set",
          inclusive: true,
          exact: false,
          message: def.maxSize.message
        });
        status.dirty();
      }
    }
    const valueType = this._def.valueType;
    function finalizeSet(elements2) {
      const parsedSet = /* @__PURE__ */ new Set();
      for (const element of elements2) {
        if (element.status === "aborted")
          return INVALID;
        if (element.status === "dirty")
          status.dirty();
        parsedSet.add(element.value);
      }
      return { status: status.value, value: parsedSet };
    }
    const elements = [...ctx.data.values()].map((item, i) => valueType._parse(new ParseInputLazyPath(ctx, item, ctx.path, i)));
    if (ctx.common.async) {
      return Promise.all(elements).then((elements2) => finalizeSet(elements2));
    } else {
      return finalizeSet(elements);
    }
  }
  min(minSize, message) {
    return new _ZodSet({
      ...this._def,
      minSize: { value: minSize, message: errorUtil.toString(message) }
    });
  }
  max(maxSize, message) {
    return new _ZodSet({
      ...this._def,
      maxSize: { value: maxSize, message: errorUtil.toString(message) }
    });
  }
  size(size, message) {
    return this.min(size, message).max(size, message);
  }
  nonempty(message) {
    return this.min(1, message);
  }
};
ZodSet.create = (valueType, params) => {
  return new ZodSet({
    valueType,
    minSize: null,
    maxSize: null,
    typeName: ZodFirstPartyTypeKind.ZodSet,
    ...processCreateParams(params)
  });
};
var ZodFunction = class _ZodFunction extends ZodType {
  constructor() {
    super(...arguments);
    this.validate = this.implement;
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.function) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.function,
        received: ctx.parsedType
      });
      return INVALID;
    }
    function makeArgsIssue(args, error) {
      return makeIssue({
        data: args,
        path: ctx.path,
        errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
        issueData: {
          code: ZodIssueCode.invalid_arguments,
          argumentsError: error
        }
      });
    }
    function makeReturnsIssue(returns, error) {
      return makeIssue({
        data: returns,
        path: ctx.path,
        errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
        issueData: {
          code: ZodIssueCode.invalid_return_type,
          returnTypeError: error
        }
      });
    }
    const params = { errorMap: ctx.common.contextualErrorMap };
    const fn = ctx.data;
    if (this._def.returns instanceof ZodPromise) {
      const me = this;
      return OK(async function(...args) {
        const error = new ZodError([]);
        const parsedArgs = await me._def.args.parseAsync(args, params).catch((e) => {
          error.addIssue(makeArgsIssue(args, e));
          throw error;
        });
        const result = await Reflect.apply(fn, this, parsedArgs);
        const parsedReturns = await me._def.returns._def.type.parseAsync(result, params).catch((e) => {
          error.addIssue(makeReturnsIssue(result, e));
          throw error;
        });
        return parsedReturns;
      });
    } else {
      const me = this;
      return OK(function(...args) {
        const parsedArgs = me._def.args.safeParse(args, params);
        if (!parsedArgs.success) {
          throw new ZodError([makeArgsIssue(args, parsedArgs.error)]);
        }
        const result = Reflect.apply(fn, this, parsedArgs.data);
        const parsedReturns = me._def.returns.safeParse(result, params);
        if (!parsedReturns.success) {
          throw new ZodError([makeReturnsIssue(result, parsedReturns.error)]);
        }
        return parsedReturns.data;
      });
    }
  }
  parameters() {
    return this._def.args;
  }
  returnType() {
    return this._def.returns;
  }
  args(...items) {
    return new _ZodFunction({
      ...this._def,
      args: ZodTuple.create(items).rest(ZodUnknown.create())
    });
  }
  returns(returnType) {
    return new _ZodFunction({
      ...this._def,
      returns: returnType
    });
  }
  implement(func) {
    const validatedFunc = this.parse(func);
    return validatedFunc;
  }
  strictImplement(func) {
    const validatedFunc = this.parse(func);
    return validatedFunc;
  }
  static create(args, returns, params) {
    return new _ZodFunction({
      args: args ? args : ZodTuple.create([]).rest(ZodUnknown.create()),
      returns: returns || ZodUnknown.create(),
      typeName: ZodFirstPartyTypeKind.ZodFunction,
      ...processCreateParams(params)
    });
  }
};
var ZodLazy = class extends ZodType {
  get schema() {
    return this._def.getter();
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const lazySchema = this._def.getter();
    return lazySchema._parse({ data: ctx.data, path: ctx.path, parent: ctx });
  }
};
ZodLazy.create = (getter, params) => {
  return new ZodLazy({
    getter,
    typeName: ZodFirstPartyTypeKind.ZodLazy,
    ...processCreateParams(params)
  });
};
var ZodLiteral = class extends ZodType {
  _parse(input) {
    if (input.data !== this._def.value) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_literal,
        expected: this._def.value
      });
      return INVALID;
    }
    return { status: "valid", value: input.data };
  }
  get value() {
    return this._def.value;
  }
};
ZodLiteral.create = (value, params) => {
  return new ZodLiteral({
    value,
    typeName: ZodFirstPartyTypeKind.ZodLiteral,
    ...processCreateParams(params)
  });
};
function createZodEnum(values, params) {
  return new ZodEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodEnum,
    ...processCreateParams(params)
  });
}
var ZodEnum = class _ZodEnum extends ZodType {
  _parse(input) {
    if (typeof input.data !== "string") {
      const ctx = this._getOrReturnCtx(input);
      const expectedValues = this._def.values;
      addIssueToContext(ctx, {
        expected: util.joinValues(expectedValues),
        received: ctx.parsedType,
        code: ZodIssueCode.invalid_type
      });
      return INVALID;
    }
    if (!this._cache) {
      this._cache = new Set(this._def.values);
    }
    if (!this._cache.has(input.data)) {
      const ctx = this._getOrReturnCtx(input);
      const expectedValues = this._def.values;
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_enum_value,
        options: expectedValues
      });
      return INVALID;
    }
    return OK(input.data);
  }
  get options() {
    return this._def.values;
  }
  get enum() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  get Values() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  get Enum() {
    const enumValues = {};
    for (const val of this._def.values) {
      enumValues[val] = val;
    }
    return enumValues;
  }
  extract(values, newDef = this._def) {
    return _ZodEnum.create(values, {
      ...this._def,
      ...newDef
    });
  }
  exclude(values, newDef = this._def) {
    return _ZodEnum.create(this.options.filter((opt) => !values.includes(opt)), {
      ...this._def,
      ...newDef
    });
  }
};
ZodEnum.create = createZodEnum;
var ZodNativeEnum = class extends ZodType {
  _parse(input) {
    const nativeEnumValues = util.getValidEnumValues(this._def.values);
    const ctx = this._getOrReturnCtx(input);
    if (ctx.parsedType !== ZodParsedType.string && ctx.parsedType !== ZodParsedType.number) {
      const expectedValues = util.objectValues(nativeEnumValues);
      addIssueToContext(ctx, {
        expected: util.joinValues(expectedValues),
        received: ctx.parsedType,
        code: ZodIssueCode.invalid_type
      });
      return INVALID;
    }
    if (!this._cache) {
      this._cache = new Set(util.getValidEnumValues(this._def.values));
    }
    if (!this._cache.has(input.data)) {
      const expectedValues = util.objectValues(nativeEnumValues);
      addIssueToContext(ctx, {
        received: ctx.data,
        code: ZodIssueCode.invalid_enum_value,
        options: expectedValues
      });
      return INVALID;
    }
    return OK(input.data);
  }
  get enum() {
    return this._def.values;
  }
};
ZodNativeEnum.create = (values, params) => {
  return new ZodNativeEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodNativeEnum,
    ...processCreateParams(params)
  });
};
var ZodPromise = class extends ZodType {
  unwrap() {
    return this._def.type;
  }
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    if (ctx.parsedType !== ZodParsedType.promise && ctx.common.async === false) {
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.promise,
        received: ctx.parsedType
      });
      return INVALID;
    }
    const promisified = ctx.parsedType === ZodParsedType.promise ? ctx.data : Promise.resolve(ctx.data);
    return OK(promisified.then((data) => {
      return this._def.type.parseAsync(data, {
        path: ctx.path,
        errorMap: ctx.common.contextualErrorMap
      });
    }));
  }
};
ZodPromise.create = (schema, params) => {
  return new ZodPromise({
    type: schema,
    typeName: ZodFirstPartyTypeKind.ZodPromise,
    ...processCreateParams(params)
  });
};
var ZodEffects = class extends ZodType {
  innerType() {
    return this._def.schema;
  }
  sourceType() {
    return this._def.schema._def.typeName === ZodFirstPartyTypeKind.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
  }
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    const effect = this._def.effect || null;
    const checkCtx = {
      addIssue: (arg) => {
        addIssueToContext(ctx, arg);
        if (arg.fatal) {
          status.abort();
        } else {
          status.dirty();
        }
      },
      get path() {
        return ctx.path;
      }
    };
    checkCtx.addIssue = checkCtx.addIssue.bind(checkCtx);
    if (effect.type === "preprocess") {
      const processed = effect.transform(ctx.data, checkCtx);
      if (ctx.common.async) {
        return Promise.resolve(processed).then(async (processed2) => {
          if (status.value === "aborted")
            return INVALID;
          const result = await this._def.schema._parseAsync({
            data: processed2,
            path: ctx.path,
            parent: ctx
          });
          if (result.status === "aborted")
            return INVALID;
          if (result.status === "dirty")
            return DIRTY(result.value);
          if (status.value === "dirty")
            return DIRTY(result.value);
          return result;
        });
      } else {
        if (status.value === "aborted")
          return INVALID;
        const result = this._def.schema._parseSync({
          data: processed,
          path: ctx.path,
          parent: ctx
        });
        if (result.status === "aborted")
          return INVALID;
        if (result.status === "dirty")
          return DIRTY(result.value);
        if (status.value === "dirty")
          return DIRTY(result.value);
        return result;
      }
    }
    if (effect.type === "refinement") {
      const executeRefinement = (acc) => {
        const result = effect.refinement(acc, checkCtx);
        if (ctx.common.async) {
          return Promise.resolve(result);
        }
        if (result instanceof Promise) {
          throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
        }
        return acc;
      };
      if (ctx.common.async === false) {
        const inner = this._def.schema._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (inner.status === "aborted")
          return INVALID;
        if (inner.status === "dirty")
          status.dirty();
        executeRefinement(inner.value);
        return { status: status.value, value: inner.value };
      } else {
        return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((inner) => {
          if (inner.status === "aborted")
            return INVALID;
          if (inner.status === "dirty")
            status.dirty();
          return executeRefinement(inner.value).then(() => {
            return { status: status.value, value: inner.value };
          });
        });
      }
    }
    if (effect.type === "transform") {
      if (ctx.common.async === false) {
        const base = this._def.schema._parseSync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (!isValid(base))
          return INVALID;
        const result = effect.transform(base.value, checkCtx);
        if (result instanceof Promise) {
          throw new Error(`Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`);
        }
        return { status: status.value, value: result };
      } else {
        return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((base) => {
          if (!isValid(base))
            return INVALID;
          return Promise.resolve(effect.transform(base.value, checkCtx)).then((result) => ({
            status: status.value,
            value: result
          }));
        });
      }
    }
    util.assertNever(effect);
  }
};
ZodEffects.create = (schema, effect, params) => {
  return new ZodEffects({
    schema,
    typeName: ZodFirstPartyTypeKind.ZodEffects,
    effect,
    ...processCreateParams(params)
  });
};
ZodEffects.createWithPreprocess = (preprocess, schema, params) => {
  return new ZodEffects({
    schema,
    effect: { type: "preprocess", transform: preprocess },
    typeName: ZodFirstPartyTypeKind.ZodEffects,
    ...processCreateParams(params)
  });
};
var ZodOptional = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType === ZodParsedType.undefined) {
      return OK(void 0);
    }
    return this._def.innerType._parse(input);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodOptional.create = (type, params) => {
  return new ZodOptional({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodOptional,
    ...processCreateParams(params)
  });
};
var ZodNullable = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType === ZodParsedType.null) {
      return OK(null);
    }
    return this._def.innerType._parse(input);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodNullable.create = (type, params) => {
  return new ZodNullable({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodNullable,
    ...processCreateParams(params)
  });
};
var ZodDefault = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    let data = ctx.data;
    if (ctx.parsedType === ZodParsedType.undefined) {
      data = this._def.defaultValue();
    }
    return this._def.innerType._parse({
      data,
      path: ctx.path,
      parent: ctx
    });
  }
  removeDefault() {
    return this._def.innerType;
  }
};
ZodDefault.create = (type, params) => {
  return new ZodDefault({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodDefault,
    defaultValue: typeof params.default === "function" ? params.default : () => params.default,
    ...processCreateParams(params)
  });
};
var ZodCatch = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const newCtx = {
      ...ctx,
      common: {
        ...ctx.common,
        issues: []
      }
    };
    const result = this._def.innerType._parse({
      data: newCtx.data,
      path: newCtx.path,
      parent: {
        ...newCtx
      }
    });
    if (isAsync(result)) {
      return result.then((result2) => {
        return {
          status: "valid",
          value: result2.status === "valid" ? result2.value : this._def.catchValue({
            get error() {
              return new ZodError(newCtx.common.issues);
            },
            input: newCtx.data
          })
        };
      });
    } else {
      return {
        status: "valid",
        value: result.status === "valid" ? result.value : this._def.catchValue({
          get error() {
            return new ZodError(newCtx.common.issues);
          },
          input: newCtx.data
        })
      };
    }
  }
  removeCatch() {
    return this._def.innerType;
  }
};
ZodCatch.create = (type, params) => {
  return new ZodCatch({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodCatch,
    catchValue: typeof params.catch === "function" ? params.catch : () => params.catch,
    ...processCreateParams(params)
  });
};
var ZodNaN = class extends ZodType {
  _parse(input) {
    const parsedType = this._getType(input);
    if (parsedType !== ZodParsedType.nan) {
      const ctx = this._getOrReturnCtx(input);
      addIssueToContext(ctx, {
        code: ZodIssueCode.invalid_type,
        expected: ZodParsedType.nan,
        received: ctx.parsedType
      });
      return INVALID;
    }
    return { status: "valid", value: input.data };
  }
};
ZodNaN.create = (params) => {
  return new ZodNaN({
    typeName: ZodFirstPartyTypeKind.ZodNaN,
    ...processCreateParams(params)
  });
};
var BRAND = Symbol("zod_brand");
var ZodBranded = class extends ZodType {
  _parse(input) {
    const { ctx } = this._processInputParams(input);
    const data = ctx.data;
    return this._def.type._parse({
      data,
      path: ctx.path,
      parent: ctx
    });
  }
  unwrap() {
    return this._def.type;
  }
};
var ZodPipeline = class _ZodPipeline extends ZodType {
  _parse(input) {
    const { status, ctx } = this._processInputParams(input);
    if (ctx.common.async) {
      const handleAsync = async () => {
        const inResult = await this._def.in._parseAsync({
          data: ctx.data,
          path: ctx.path,
          parent: ctx
        });
        if (inResult.status === "aborted")
          return INVALID;
        if (inResult.status === "dirty") {
          status.dirty();
          return DIRTY(inResult.value);
        } else {
          return this._def.out._parseAsync({
            data: inResult.value,
            path: ctx.path,
            parent: ctx
          });
        }
      };
      return handleAsync();
    } else {
      const inResult = this._def.in._parseSync({
        data: ctx.data,
        path: ctx.path,
        parent: ctx
      });
      if (inResult.status === "aborted")
        return INVALID;
      if (inResult.status === "dirty") {
        status.dirty();
        return {
          status: "dirty",
          value: inResult.value
        };
      } else {
        return this._def.out._parseSync({
          data: inResult.value,
          path: ctx.path,
          parent: ctx
        });
      }
    }
  }
  static create(a, b) {
    return new _ZodPipeline({
      in: a,
      out: b,
      typeName: ZodFirstPartyTypeKind.ZodPipeline
    });
  }
};
var ZodReadonly = class extends ZodType {
  _parse(input) {
    const result = this._def.innerType._parse(input);
    const freeze = (data) => {
      if (isValid(data)) {
        data.value = Object.freeze(data.value);
      }
      return data;
    };
    return isAsync(result) ? result.then((data) => freeze(data)) : freeze(result);
  }
  unwrap() {
    return this._def.innerType;
  }
};
ZodReadonly.create = (type, params) => {
  return new ZodReadonly({
    innerType: type,
    typeName: ZodFirstPartyTypeKind.ZodReadonly,
    ...processCreateParams(params)
  });
};
function cleanParams(params, data) {
  const p = typeof params === "function" ? params(data) : typeof params === "string" ? { message: params } : params;
  const p2 = typeof p === "string" ? { message: p } : p;
  return p2;
}
function custom(check, _params = {}, fatal) {
  if (check)
    return ZodAny.create().superRefine((data, ctx) => {
      const r = check(data);
      if (r instanceof Promise) {
        return r.then((r2) => {
          if (!r2) {
            const params = cleanParams(_params, data);
            const _fatal = params.fatal ?? fatal ?? true;
            ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
          }
        });
      }
      if (!r) {
        const params = cleanParams(_params, data);
        const _fatal = params.fatal ?? fatal ?? true;
        ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
      }
      return;
    });
  return ZodAny.create();
}
var late = {
  object: ZodObject.lazycreate
};
var ZodFirstPartyTypeKind;
(function(ZodFirstPartyTypeKind2) {
  ZodFirstPartyTypeKind2["ZodString"] = "ZodString";
  ZodFirstPartyTypeKind2["ZodNumber"] = "ZodNumber";
  ZodFirstPartyTypeKind2["ZodNaN"] = "ZodNaN";
  ZodFirstPartyTypeKind2["ZodBigInt"] = "ZodBigInt";
  ZodFirstPartyTypeKind2["ZodBoolean"] = "ZodBoolean";
  ZodFirstPartyTypeKind2["ZodDate"] = "ZodDate";
  ZodFirstPartyTypeKind2["ZodSymbol"] = "ZodSymbol";
  ZodFirstPartyTypeKind2["ZodUndefined"] = "ZodUndefined";
  ZodFirstPartyTypeKind2["ZodNull"] = "ZodNull";
  ZodFirstPartyTypeKind2["ZodAny"] = "ZodAny";
  ZodFirstPartyTypeKind2["ZodUnknown"] = "ZodUnknown";
  ZodFirstPartyTypeKind2["ZodNever"] = "ZodNever";
  ZodFirstPartyTypeKind2["ZodVoid"] = "ZodVoid";
  ZodFirstPartyTypeKind2["ZodArray"] = "ZodArray";
  ZodFirstPartyTypeKind2["ZodObject"] = "ZodObject";
  ZodFirstPartyTypeKind2["ZodUnion"] = "ZodUnion";
  ZodFirstPartyTypeKind2["ZodDiscriminatedUnion"] = "ZodDiscriminatedUnion";
  ZodFirstPartyTypeKind2["ZodIntersection"] = "ZodIntersection";
  ZodFirstPartyTypeKind2["ZodTuple"] = "ZodTuple";
  ZodFirstPartyTypeKind2["ZodRecord"] = "ZodRecord";
  ZodFirstPartyTypeKind2["ZodMap"] = "ZodMap";
  ZodFirstPartyTypeKind2["ZodSet"] = "ZodSet";
  ZodFirstPartyTypeKind2["ZodFunction"] = "ZodFunction";
  ZodFirstPartyTypeKind2["ZodLazy"] = "ZodLazy";
  ZodFirstPartyTypeKind2["ZodLiteral"] = "ZodLiteral";
  ZodFirstPartyTypeKind2["ZodEnum"] = "ZodEnum";
  ZodFirstPartyTypeKind2["ZodEffects"] = "ZodEffects";
  ZodFirstPartyTypeKind2["ZodNativeEnum"] = "ZodNativeEnum";
  ZodFirstPartyTypeKind2["ZodOptional"] = "ZodOptional";
  ZodFirstPartyTypeKind2["ZodNullable"] = "ZodNullable";
  ZodFirstPartyTypeKind2["ZodDefault"] = "ZodDefault";
  ZodFirstPartyTypeKind2["ZodCatch"] = "ZodCatch";
  ZodFirstPartyTypeKind2["ZodPromise"] = "ZodPromise";
  ZodFirstPartyTypeKind2["ZodBranded"] = "ZodBranded";
  ZodFirstPartyTypeKind2["ZodPipeline"] = "ZodPipeline";
  ZodFirstPartyTypeKind2["ZodReadonly"] = "ZodReadonly";
})(ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {}));
var instanceOfType = (cls, params = {
  message: `Input not instance of ${cls.name}`
}) => custom((data) => data instanceof cls, params);
var stringType = ZodString.create;
var numberType = ZodNumber.create;
var nanType = ZodNaN.create;
var bigIntType = ZodBigInt.create;
var booleanType = ZodBoolean.create;
var dateType = ZodDate.create;
var symbolType = ZodSymbol.create;
var undefinedType = ZodUndefined.create;
var nullType = ZodNull.create;
var anyType = ZodAny.create;
var unknownType = ZodUnknown.create;
var neverType = ZodNever.create;
var voidType = ZodVoid.create;
var arrayType = ZodArray.create;
var objectType = ZodObject.create;
var strictObjectType = ZodObject.strictCreate;
var unionType = ZodUnion.create;
var discriminatedUnionType = ZodDiscriminatedUnion.create;
var intersectionType = ZodIntersection.create;
var tupleType = ZodTuple.create;
var recordType = ZodRecord.create;
var mapType = ZodMap.create;
var setType = ZodSet.create;
var functionType = ZodFunction.create;
var lazyType = ZodLazy.create;
var literalType = ZodLiteral.create;
var enumType = ZodEnum.create;
var nativeEnumType = ZodNativeEnum.create;
var promiseType = ZodPromise.create;
var effectsType = ZodEffects.create;
var optionalType = ZodOptional.create;
var nullableType = ZodNullable.create;
var preprocessType = ZodEffects.createWithPreprocess;
var pipelineType = ZodPipeline.create;
var ostring = () => stringType().optional();
var onumber = () => numberType().optional();
var oboolean = () => booleanType().optional();
var coerce = {
  string: (arg) => ZodString.create({ ...arg, coerce: true }),
  number: (arg) => ZodNumber.create({ ...arg, coerce: true }),
  boolean: (arg) => ZodBoolean.create({
    ...arg,
    coerce: true
  }),
  bigint: (arg) => ZodBigInt.create({ ...arg, coerce: true }),
  date: (arg) => ZodDate.create({ ...arg, coerce: true })
};
var NEVER = INVALID;

// packages/formal-ir/dist/schema.js
var FormalExprNodeSchema = external_exports.lazy(() => external_exports.discriminatedUnion("kind", [
  external_exports.object({ kind: external_exports.literal("bvar"), index: external_exports.number().int() }),
  external_exports.object({ kind: external_exports.literal("fvar"), name: external_exports.string(), fvarId: external_exports.string() }),
  external_exports.object({ kind: external_exports.literal("mvar"), mvarId: external_exports.string() }),
  external_exports.object({ kind: external_exports.literal("sort"), level: external_exports.string() }),
  external_exports.object({ kind: external_exports.literal("const"), name: external_exports.string(), levels: external_exports.array(external_exports.string()) }),
  external_exports.object({
    kind: external_exports.literal("app"),
    fn: FormalExprNodeSchema,
    args: external_exports.array(FormalExprNodeSchema)
  }),
  external_exports.object({
    kind: external_exports.literal("lam"),
    binderName: external_exports.string(),
    binderInfo: external_exports.string(),
    binderType: FormalExprNodeSchema,
    body: FormalExprNodeSchema
  }),
  external_exports.object({
    kind: external_exports.literal("forall"),
    binderName: external_exports.string(),
    binderInfo: external_exports.string(),
    binderType: FormalExprNodeSchema,
    body: FormalExprNodeSchema
  }),
  external_exports.object({
    kind: external_exports.literal("let"),
    binderName: external_exports.string(),
    binderType: FormalExprNodeSchema,
    value: FormalExprNodeSchema,
    body: FormalExprNodeSchema
  }),
  external_exports.object({
    kind: external_exports.literal("lit"),
    litKind: external_exports.enum(["nat", "str"]),
    value: external_exports.union([external_exports.number(), external_exports.string()])
  }),
  external_exports.object({
    kind: external_exports.literal("proj"),
    structName: external_exports.string(),
    index: external_exports.number().int(),
    struct: FormalExprNodeSchema
  })
]));
var FormalExprSchema = external_exports.object({
  /** Lean's own pretty printer output. */
  pretty: external_exports.string(),
  tree: FormalExprNodeSchema,
  /** Every constant mentioned anywhere in the expression. */
  constants: external_exports.array(external_exports.string())
});
var BinderUsageSchema = external_exports.object({
  occursInProofTerm: external_exports.boolean(),
  occursInLaterBinderTypes: external_exports.boolean(),
  occursInConclusion: external_exports.boolean(),
  proofTermAvailable: external_exports.boolean(),
  unusedInProof: external_exports.boolean()
});
var FormalBinderSchema = external_exports.object({
  index: external_exports.number().int(),
  /** Display name. Inaccessible binders are shown Lean-style, as `a✝`. */
  name: external_exports.string(),
  /** The underlying Lean name, macro scopes included. */
  rawName: external_exports.string().optional(),
  fvarId: external_exports.string(),
  binderInfo: external_exports.enum(["default", "implicit", "strictImplicit", "instImplicit"]),
  /**
   * `instance` for typeclass instance binders, `hypothesis` for other
   * `Prop`-valued binders, `parameter` otherwise.
   *
   * Instances are separated because they are plumbing rather than mathematics:
   * counting `[IsStrictOrderedRing α]` as a stated assumption would distort
   * assumption sensitivity and fill figures with noise.
   */
  role: external_exports.enum(["hypothesis", "parameter", "instance"]),
  type: FormalExprSchema,
  usage: BinderUsageSchema
});
var SourceLocationSchema = external_exports.object({
  module: external_exports.string().nullable(),
  startLine: external_exports.number().int(),
  startColumn: external_exports.number().int(),
  endLine: external_exports.number().int(),
  endColumn: external_exports.number().int()
}).nullable();
var FormalDeclarationSchema = external_exports.object({
  name: external_exports.string(),
  namespace: external_exports.string(),
  kind: external_exports.enum([
    "axiom",
    "definition",
    "theorem",
    "opaque",
    "inductive",
    "constructor",
    "recursor",
    "quot"
  ]),
  docstring: external_exports.string().nullable(),
  source: SourceLocationSchema,
  binders: external_exports.array(FormalBinderSchema),
  /** The statement with all binders stripped. */
  conclusion: FormalExprSchema,
  /**
   * For a definition, the body with its binders instantiated: what the
   * definition unfolds to. `null` for theorems (proof terms are excluded on
   * purpose) and for definitions whose body is too large to be worth showing.
   */
  definitionBody: FormalExprSchema.nullable().default(null),
  /** The full type, binders included. */
  statement: FormalExprSchema,
  dependencies: external_exports.array(external_exports.string()),
  /** The trust base: axioms this declaration ultimately rests on. */
  axioms: external_exports.array(external_exports.string()),
  proofTermAvailable: external_exports.boolean(),
  /**
   * Set when extraction of this declaration failed. The row is kept rather than
   * dropped, so a sweep reports what it could not read instead of quietly
   * returning fewer declarations than the module contains.
   */
  extractionError: external_exports.string().nullable().default(null),
  /** True if the "proof" bottoms out in `sorryAx`. Such a declaration is NOT proved. */
  usesSorry: external_exports.boolean()
});
var FormalIRDocumentSchema = external_exports.object({
  formalIRVersion: external_exports.string(),
  system: external_exports.string(),
  toolchain: external_exports.string(),
  /**
   * Whether the extractor's pretty printer had notation delaborators available.
   * `raw` means expressions render as `LE.le x y` rather than `x ≤ y`; the
   * mathematics is identical but the display is degraded, and ProofLens says so
   * rather than pretending otherwise.
   */
  notationFidelity: external_exports.enum(["notation", "raw"]).default("notation"),
  modules: external_exports.array(external_exports.string()),
  declarations: external_exports.array(FormalDeclarationSchema)
});

// packages/epistemics/dist/index.js
var EPISTEMIC_ORDER = [
  "verified",
  "derived",
  "interpreted",
  "heuristic",
  "illustrative",
  "speculative"
];
function rank(status) {
  return EPISTEMIC_ORDER.indexOf(status);
}
function weakest(...statuses) {
  if (statuses.length === 0)
    return "speculative";
  return statuses.reduce((a, b) => rank(a) >= rank(b) ? a : b);
}
function isAtLeast(status, floor) {
  return rank(status) <= rank(floor);
}
var EPISTEMIC_GLOSS = {
  verified: "Checked by the Lean kernel.",
  derived: "Computed from the verified statement by a deterministic rule.",
  interpreted: "A reading of the formal statement, not part of what was proved.",
  heuristic: "A rule-of-thumb guess that may be wrong.",
  illustrative: "A display choice. It makes no mathematical claim.",
  speculative: "Generated by a model. Not verified by anything."
};
var WITNESS_BRAND = Symbol("prooflens.kernel-witness");
function mintKernelWitness(args) {
  if (!args.provedWithoutSorry)
    return null;
  return {
    [WITNESS_BRAND]: true,
    system: args.system,
    declaration: args.declaration,
    module: args.module,
    axioms: args.axioms
  };
}
function transcribe(witness, value, provenance) {
  if (witness === null || typeof witness !== "object" || !Object.hasOwn(witness, WITNESS_BRAND) || witness[WITNESS_BRAND] !== true) {
    throw new TypeError("transcribe() requires a KernelWitness minted by @prooflens/formal-ir. Verified status cannot be constructed from arbitrary data.");
  }
  return Object.freeze({
    value,
    status: "verified",
    provenance: { ...provenance, rule: void 0 }
  });
}
function derive(value, rule, inputs, provenance) {
  const status = weakest(rule.produces, "derived", ...inputs.map((c) => c.status));
  return Object.freeze({ value, status, provenance: { ...provenance, rule } });
}
var STANDARD_AXIOMS = ["propext", "Classical.choice", "Quot.sound"];
function unusualAxioms(axioms) {
  return axioms.filter((a) => !STANDARD_AXIOMS.includes(a));
}

// packages/formal-ir/dist/load.js
var FormalIRParseError = class extends Error {
  issues;
  constructor(message, issues) {
    super(message);
    this.issues = issues;
    this.name = "FormalIRParseError";
  }
};
function parseFormalIR(value) {
  const result = FormalIRDocumentSchema.safeParse(value);
  if (!result.success) {
    throw new FormalIRParseError("Input is not valid ProofLens Formal IR. It was probably produced by a different extractor version.", result.error.issues);
  }
  return result.data;
}
function kernelWitness(doc, decl) {
  return mintKernelWitness({
    system: doc.system,
    declaration: decl.name,
    module: decl.source?.module ?? null,
    axioms: decl.axioms,
    // A stub row means extraction failed, so ProofLens never saw the real
    // declaration. `usesSorry: false` records that no `sorry` was *observed* —
    // which is not evidence the kernel accepted anything.
    provedWithoutSorry: !decl.usesSorry && decl.extractionError === null
  });
}
function sourceRefFor(doc, decl, path2) {
  const ref = {
    system: doc.system,
    declaration: decl.name,
    module: decl.source?.module ?? null,
    span: decl.source ? {
      startLine: decl.source.startLine,
      startColumn: decl.source.startColumn,
      endLine: decl.source.endLine,
      endColumn: decl.source.endColumn
    } : null
  };
  if (path2 !== void 0)
    ref.path = path2;
  return ref;
}
function localDependencyEdges(doc) {
  const local = new Set(doc.declarations.map((d) => d.name));
  const edges = [];
  let externalCount = 0;
  for (const decl of doc.declarations) {
    for (const dep of decl.dependencies) {
      if (dep === decl.name)
        continue;
      if (local.has(dep))
        edges.push({ from: decl.name, to: dep });
      else
        externalCount += 1;
    }
  }
  return { edges, externalCount };
}

// packages/formal-ir/dist/paths.js
function argPath(base, index) {
  return `${base}.args[${index}]`;
}
function headConstant(node) {
  if (node.kind === "const")
    return node.name;
  if (node.kind === "app" && node.fn.kind === "const")
    return node.fn.name;
  return void 0;
}

// packages/math-ir/dist/types.js
var MATH_IR_VERSION = "0.1.0";

// packages/math-ir/dist/tables.js
var RELATIONS = {
  Eq: { relation: "equal", valueArity: 2 },
  Ne: { relation: "not-equal", valueArity: 2 },
  "LE.le": { relation: "less-than-or-equal", valueArity: 2 },
  "LT.lt": { relation: "less-than", valueArity: 2 },
  "GE.ge": { relation: "greater-than-or-equal", valueArity: 2 },
  "GT.gt": { relation: "greater-than", valueArity: 2 },
  Iff: { relation: "equivalent", valueArity: 2 }
};
var BINARY_OPERATORS = {
  "HAdd.hAdd": { op: "add", symbol: "+", valueArity: 2 },
  "HSub.hSub": { op: "sub", symbol: "\u2212", valueArity: 2 },
  "HMul.hMul": { op: "mul", symbol: "\xB7", valueArity: 2 },
  "HDiv.hDiv": { op: "div", symbol: "/", valueArity: 2 },
  "HPow.hPow": { op: "pow", symbol: "^", valueArity: 2 },
  "HMod.hMod": { op: "mod", symbol: "%", valueArity: 2 }
};
var UNARY_OPERATORS = {
  "Neg.neg": { op: "neg", symbol: "\u2212", valueArity: 1 },
  "Inv.inv": { op: "inv", symbol: "\u207B\xB9", valueArity: 1 },
  abs: { op: "abs", symbol: "abs", valueArity: 1 }
};
var NAMED_FUNCTIONS = {
  "Real.sqrt": { display: "\u221A", valueArity: 1 },
  "Real.log": { display: "log", valueArity: 1 },
  "Real.exp": { display: "exp", valueArity: 1 },
  "Real.sin": { display: "sin", valueArity: 1 },
  "Real.cos": { display: "cos", valueArity: 1 },
  "Real.tan": { display: "tan", valueArity: 1 },
  "Real.sinh": { display: "sinh", valueArity: 1 },
  "Real.cosh": { display: "cosh", valueArity: 1 },
  "Real.tanh": { display: "tanh", valueArity: 1 },
  "Real.arctan": { display: "arctan", valueArity: 1 },
  "Real.rpow": { display: "rpow", valueArity: 2 },
  "Real.toNNReal": { display: "toNNReal", valueArity: 1 },
  "Real.nnabs": { display: "nnabs", valueArity: 1 },
  "NNReal.sqrt": { display: "\u221A", valueArity: 1 },
  "Nat.succ": { display: "succ", valueArity: 1 },
  "Nat.sqrt": { display: "\u230A\u221A\u230B", valueArity: 1 },
  "Nat.factorial": { display: "factorial", valueArity: 1 },
  // Aggregations. Rendered as named applications rather than big-operator
  // notation: `∑(s, i ↦ f i)` is honest about the two arguments, where a bare
  // `∑` would hide which set is being summed over.
  "Finset.sum": { display: "\u2211", valueArity: 2 },
  "Finset.prod": { display: "\u220F", valueArity: 2 },
  "Finset.card": { display: "card", valueArity: 1 },
  tsum: { display: "\u2211'", valueArity: 1 },
  "Max.max": { display: "max", valueArity: 2 },
  "Min.min": { display: "min", valueArity: 2 },
  "Dist.dist": { display: "dist", valueArity: 2 },
  "EDist.edist": { display: "edist", valueArity: 2 },
  "Set.indicator": { display: "indicator", valueArity: 2 },
  "Nat.floor": { display: "\u230A\xB7\u230B", valueArity: 1 },
  "Nat.ceil": { display: "\u2308\xB7\u2309", valueArity: 1 },
  "Int.floor": { display: "\u230A\xB7\u230B", valueArity: 1 },
  "Int.ceil": { display: "\u2308\xB7\u2309", valueArity: 1 },
  cmp: { display: "cmp", valueArity: 2 },
  ite: { display: "if", valueArity: 3 },
  // Order duality is a genuine change of viewpoint, not a no-op, so it is named
  // rather than made transparent. Hiding it would silently turn a statement
  // about the dual order into a statement about the original.
  "OrderDual.toDual": { display: "toDual", valueArity: 1 },
  "OrderDual.ofDual": { display: "ofDual", valueArity: 1 },
  // Intervals. They appear constantly as the set argument of `MonotoneOn` and
  // friends, so leaving them opaque made otherwise-readable statements unreadable.
  "Set.Icc": { display: "[\xB7, \xB7]", valueArity: 2 },
  "Set.Ico": { display: "[\xB7, \xB7)", valueArity: 2 },
  "Set.Ioc": { display: "(\xB7, \xB7]", valueArity: 2 },
  "Set.Ioo": { display: "(\xB7, \xB7)", valueArity: 2 },
  "Set.Iic": { display: "(\u2212\u221E,\xB7]", valueArity: 1 },
  "Set.Iio": { display: "(\u2212\u221E,\xB7)", valueArity: 1 },
  "Set.Ici": { display: "[\xB7,\u221E)", valueArity: 1 },
  "Set.Ioi": { display: "(\xB7,\u221E)", valueArity: 1 },
  "Set.univ": { display: "univ", valueArity: 0 }
};
var POSITIONAL = {
  // `DFunLike.coe {F α β} [inst] (f : F) : ∀ a, β a`
  "DFunLike.coe": { kind: "coercion", index: 4 },
  "FunLike.coe": { kind: "coercion", index: 4 },
  // `Function.comp {α β γ} (f : β → γ) (g : α → β) : α → γ`
  "Function.comp": { kind: "composition", index: 3 }
};
var FILTERS = {
  "Filter.atTop": { kind: "at-top", label: "grows without bound", pointIndex: null },
  "Filter.atBot": { kind: "at-bot", label: "decreases without bound", pointIndex: null },
  nhds: { kind: "neighbourhood", label: "approaches", pointIndex: -1 },
  nhdsWithin: { kind: "punctured", label: "approaches within a set", pointIndex: -2 },
  "Filter.cofinite": { kind: "other", label: "outside any finite set", pointIndex: null },
  "Filter.cocompact": { kind: "other", label: "outside any compact set", pointIndex: null }
};
var TRANSPARENT = {
  "Nat.cast": { argIndex: -1 },
  "Int.cast": { argIndex: -1 },
  "Rat.cast": { argIndex: -1 },
  "NNReal.toReal": { argIndex: -1 },
  "OfNat.ofNat": { argIndex: 1 },
  "OfScientific.ofScientific": { argIndex: 1 },
  // `decide p` is `p` wearing a `Decidable` hat.
  "Decidable.decide": { argIndex: 0 }
};
var PREDICATES = {
  Monotone: { predicate: "monotone", label: "monotone", valueArity: 1 },
  StrictMono: { predicate: "strictly-monotone", label: "strictly increasing", valueArity: 1 },
  Antitone: { predicate: "antitone", label: "antitone", valueArity: 1 },
  StrictAnti: { predicate: "strictly-antitone", label: "strictly decreasing", valueArity: 1 },
  MonotoneOn: { predicate: "monotone", label: "monotone on a set", valueArity: 2 },
  AntitoneOn: { predicate: "antitone", label: "antitone on a set", valueArity: 2 },
  StrictMonoOn: {
    predicate: "strictly-monotone",
    label: "strictly increasing on a set",
    valueArity: 2
  },
  StrictAntiOn: {
    predicate: "strictly-antitone",
    label: "strictly decreasing on a set",
    valueArity: 2
  },
  // Named properties ProofLens can *read* without claiming to interpret. Being
  // in this table is an explicit statement that ProofLens recognises the
  // property; anything absent stays `unsupported`, which is the honest answer
  // and keeps the unsupported-mathematics backlog meaningful.
  Summable: { predicate: "other", label: "summable", valueArity: 1 },
  HasSum: { predicate: "other", label: "has a sum", valueArity: 2 },
  Continuous: { predicate: "other", label: "continuous", valueArity: 1 },
  ContinuousAt: { predicate: "other", label: "continuous at a point", valueArity: 2 },
  ContinuousOn: { predicate: "other", label: "continuous on a set", valueArity: 2 },
  ContinuousWithinAt: { predicate: "other", label: "continuous within a set", valueArity: 3 },
  CauchySeq: { predicate: "other", label: "a Cauchy sequence", valueArity: 1 },
  Differentiable: { predicate: "other", label: "differentiable", valueArity: 1 },
  DifferentiableAt: { predicate: "other", label: "differentiable at a point", valueArity: 2 },
  HasDerivAt: { predicate: "other", label: "has a derivative at a point", valueArity: 3 },
  "Real.HolderConjugate": { predicate: "other", label: "H\xF6lder conjugate", valueArity: 2 },
  "Set.InjOn": { predicate: "other", label: "injective on a set", valueArity: 2 },
  "Set.SurjOn": { predicate: "other", label: "surjective onto a set", valueArity: 3 },
  IsLUB: { predicate: "other", label: "a least upper bound", valueArity: 2 },
  IsGLB: { predicate: "other", label: "a greatest lower bound", valueArity: 2 },
  IsMax: { predicate: "other", label: "a maximum", valueArity: 1 },
  IsMin: { predicate: "other", label: "a minimum", valueArity: 1 }
};
var RELATION_PHRASE = {
  equal: "is equal to",
  "not-equal": "is not equal to",
  "less-than": "is strictly less than",
  "less-than-or-equal": "is at most",
  "greater-than": "is strictly greater than",
  "greater-than-or-equal": "is at least",
  equivalent: "holds exactly when"
};
var RELATION_SYMBOL = {
  equal: "=",
  "not-equal": "\u2260",
  "less-than": "<",
  "less-than-or-equal": "\u2264",
  "greater-than": ">",
  "greater-than-or-equal": "\u2265",
  equivalent: "\u2194"
};

// packages/math-ir/dist/annotations.js
var DIRECTIVE = /^\s*@prooflens\.([a-zA-Z][\w-]*)\s*(.*)$/;
var KEY_VALUE = /([a-zA-Z][\w-]*)\s*=\s*"([^"]*)"/g;
var ALLOWED_KEYS = /* @__PURE__ */ new Set(["meaning", "units", "domain", "axis", "role"]);
function parseDocstring(docstring) {
  if (docstring === null) {
    return { prose: null, annotations: [], suggestedVisual: null, concept: null, malformed: [] };
  }
  const proseLines = [];
  const annotations = [];
  const malformed = [];
  let suggestedVisual = null;
  let concept = null;
  for (const line2 of docstring.split("\n")) {
    const match = DIRECTIVE.exec(line2);
    if (!match) {
      proseLines.push(line2);
      continue;
    }
    const [, directive, rest] = match;
    switch (directive) {
      case "var": {
        const spaceIdx = rest.search(/\s/);
        const target = (spaceIdx === -1 ? rest : rest.slice(0, spaceIdx)).trim();
        if (target === "") {
          malformed.push(line2);
          break;
        }
        const annotation = { target };
        let sawKey = false;
        for (const kv of rest.matchAll(KEY_VALUE)) {
          const key = kv[1];
          const value = kv[2];
          if (!ALLOWED_KEYS.has(key))
            continue;
          sawKey = true;
          annotation[key] = value;
        }
        if (!sawKey)
          malformed.push(line2);
        annotations.push(annotation);
        break;
      }
      case "visual": {
        const value = rest.trim().replace(/^"|"$/g, "");
        if (value === "")
          malformed.push(line2);
        else
          suggestedVisual = value;
        break;
      }
      case "concept": {
        const value = rest.trim().replace(/^"|"$/g, "");
        if (value === "")
          malformed.push(line2);
        else
          concept = value;
        break;
      }
      default:
        malformed.push(line2);
        break;
    }
  }
  const prose = proseLines.join("\n").trim();
  return {
    prose: prose === "" ? null : prose,
    annotations,
    suggestedVisual,
    concept,
    malformed
  };
}
function annotationFor(annotations, symbol) {
  return annotations.find((a) => a.target === symbol) ?? null;
}

// packages/math-ir/dist/render.js
var PRECEDENCE = {
  add: 65,
  sub: 65,
  mul: 70,
  div: 70,
  mod: 70,
  neg: 75,
  pow: 80,
  inv: 85,
  comp: 88,
  abs: 90
};
function precedenceOf(expr) {
  if (expr.kind === "operator")
    return PRECEDENCE[expr.op] ?? 50;
  if (expr.kind === "application" || expr.kind === "opaque")
    return 90;
  return 100;
}
function wrap(child, parentPrecedence, isRight) {
  const childPrecedence = precedenceOf(child);
  const needsParens = childPrecedence < parentPrecedence || childPrecedence === parentPrecedence && isRight && child.kind === "operator";
  const rendered = renderExpression(child);
  return needsParens ? `(${rendered})` : rendered;
}
function renderExpression(expr) {
  switch (expr.kind) {
    case "variable":
      return expr.symbol;
    case "number":
      return expr.display;
    case "constant":
      return expr.display;
    case "lambda":
      return `${expr.parameter} \u21A6 ${renderExpression(expr.body)}`;
    case "opaque":
      return expr.display;
    case "application": {
      if (expr.args.length === 0)
        return expr.display;
      if (expr.display.includes("\xB7")) {
        let filled = expr.display;
        for (const arg of expr.args)
          filled = filled.replace("\xB7", renderExpression(arg));
        return filled;
      }
      return `${expr.display}(${expr.args.map(renderExpression).join(", ")})`;
    }
    case "operator": {
      const p = PRECEDENCE[expr.op] ?? 50;
      if (expr.op === "neg")
        return `\u2212${wrap(expr.args[0], p, false)}`;
      if (expr.op === "inv")
        return `${wrap(expr.args[0], p, false)}\u207B\xB9`;
      if (expr.op === "abs")
        return `|${renderExpression(expr.args[0])}|`;
      const left = wrap(expr.args[0], p, false);
      const right = wrap(expr.args[1], p, true);
      return `${left} ${expr.symbol} ${right}`;
    }
  }
}
function renderProposition(prop) {
  switch (prop.kind) {
    case "relation":
      return `${renderExpression(prop.lhs)} ${RELATION_SYMBOL[prop.relation]} ${renderExpression(prop.rhs)}`;
    case "predicate": {
      const parts = [prop.subject, ...prop.args].filter((a) => a !== null);
      return parts.length === 0 ? prop.name : `${prop.name} ${parts.map((a) => renderExpression(a)).join(" ")}`;
    }
    case "implication":
      return `${renderProposition(prop.antecedent)} \u2192 ${renderProposition(prop.consequent)}`;
    case "limit":
      return `${renderExpression(prop.subject)} \u27F6 ${prop.target.display} (along ${prop.source.display})`;
    case "existential":
      return `\u2203 ${prop.binder}, ${renderProposition(prop.body)}`;
    case "conjunction":
      return prop.conjuncts.map(renderProposition).join(" \u2227 ");
    case "membership":
      return `${renderExpression(prop.element)} \u2208 ${renderExpression(prop.collection)}`;
    case "opaque":
      return prop.display;
  }
}
function variablesIn(expr, into = /* @__PURE__ */ new Set()) {
  switch (expr.kind) {
    case "variable":
      into.add(expr.id);
      break;
    case "operator":
    case "application":
      for (const a of expr.args)
        variablesIn(a, into);
      break;
    case "lambda":
      variablesIn(expr.body, into);
      break;
    default:
      break;
  }
  return into;
}

// packages/math-ir/dist/lower.js
var MATH_IR_RULES = {
  lowerExpression: {
    id: "MATHIR_LOWER_EXPR_001",
    description: "Lean constants were mapped to mathematical operators using ProofLens's constant table.",
    produces: "derived"
  },
  lowerProposition: {
    id: "MATHIR_LOWER_PROP_001",
    description: "The conclusion's head constant was recognised as a relation, predicate, or implication.",
    produces: "derived"
  },
  unrecognised: {
    id: "MATHIR_UNRECOGNISED_001",
    description: "The head constant is not in ProofLens's table, so the structure is preserved but not named.",
    produces: "derived"
  }
};
function mentionsBVar(node, index) {
  switch (node.kind) {
    case "bvar":
      return node.index === index;
    case "app":
      return mentionsBVar(node.fn, index) || node.args.some((a) => mentionsBVar(a, index));
    case "lam":
    case "forall":
      return mentionsBVar(node.binderType, index) || mentionsBVar(node.body, index + 1);
    case "let":
      return mentionsBVar(node.binderType, index) || mentionsBVar(node.value, index) || mentionsBVar(node.body, index + 1);
    case "proj":
      return mentionsBVar(node.struct, index);
    default:
      return false;
  }
}
function isPlumbing(node) {
  if (node.kind === "const")
    return true;
  if (node.kind === "sort")
    return true;
  if (node.kind === "app") {
    const head = headConstant(node);
    if (head === void 0)
      return false;
    const last = head.split(".").pop() ?? head;
    return /^inst/i.test(last) || /^inst/i.test(head) || /to[A-Z]/.test(last);
  }
  return false;
}
function mathematicalArgs(args) {
  let i = 0;
  while (i < args.length && isPlumbing(args[i]))
    i += 1;
  return { args: args.slice(i), offset: i };
}
function shortName(name) {
  return name.split(".").pop() ?? name;
}
function opaqueDisplay(node, path2, scope, locals = NO_LOCALS) {
  switch (node.kind) {
    case "const":
      return shortName(node.name);
    case "fvar":
      return node.name;
    case "bvar":
      return scope[scope.length - 1 - node.index] ?? `#${node.index}`;
    case "lit":
      return String(node.value);
    case "sort":
      return "Type";
    case "mvar":
      return "?m";
    case "lam":
      return `${node.binderName} \u21A6 ${renderExpression(lowerExpression(node.body, `${path2}.body`, [...scope, node.binderName], locals))}`;
    case "forall":
      return `\u2200 ${node.binderName}, ${opaqueDisplay(node.body, `${path2}.body`, [...scope, node.binderName], locals)}`;
    case "let":
      return `let ${node.binderName} := \u2026`;
    case "proj":
      return `${opaqueDisplay(node.struct, `${path2}.struct`, scope, locals)}.${node.index}`;
    case "app": {
      const head = headConstant(node);
      const { args, offset } = mathematicalArgs(node.args);
      const rendered = args.map((a, i) => renderExpression(lowerExpression(a, argPath(path2, offset + i), scope, locals)));
      const name = head ? shortName(head) : opaqueDisplay(node.fn, `${path2}.fn`, scope, locals);
      return rendered.length === 0 ? name : `${name}(${rendered.join(", ")})`;
    }
  }
}
var NO_LOCALS = /* @__PURE__ */ new Map();
function lowerExpression(node, path2, scope = [], locals = NO_LOCALS) {
  switch (node.kind) {
    case "fvar":
      return { kind: "variable", id: node.fvarId, symbol: node.name, path: path2 };
    case "bvar": {
      const symbol = scope[scope.length - 1 - node.index] ?? `#${node.index}`;
      return { kind: "variable", id: `bound:${symbol}`, symbol, path: path2 };
    }
    case "lit":
      return typeof node.value === "number" ? { kind: "number", value: node.value, display: String(node.value), path: path2 } : { kind: "constant", name: "string", display: JSON.stringify(node.value), path: path2 };
    case "const":
      return { kind: "constant", name: node.name, display: shortName(node.name), path: path2 };
    case "lam":
      return {
        kind: "lambda",
        parameter: node.binderName,
        body: lowerExpression(node.body, `${path2}.body`, [...scope, node.binderName], locals),
        path: path2
      };
    case "app": {
      const head = headConstant(node);
      if (head !== void 0) {
        const transparent = TRANSPARENT[head];
        if (transparent) {
          const idx = transparent.argIndex === -1 ? node.args.length - 1 : transparent.argIndex;
          const inner = node.args[idx];
          if (inner)
            return lowerExpression(inner, argPath(path2, idx), scope, locals);
        }
        const binary = BINARY_OPERATORS[head];
        if (binary && node.args.length >= binary.valueArity) {
          const start = node.args.length - binary.valueArity;
          return {
            kind: "operator",
            op: binary.op,
            symbol: binary.symbol,
            args: node.args.slice(start).map((a, i) => lowerExpression(a, argPath(path2, start + i), scope, locals)),
            path: path2
          };
        }
        const unary = UNARY_OPERATORS[head];
        if (unary && node.args.length >= unary.valueArity) {
          const start = node.args.length - unary.valueArity;
          return {
            kind: "operator",
            op: unary.op,
            symbol: unary.symbol,
            args: node.args.slice(start).map((a, i) => lowerExpression(a, argPath(path2, start + i), scope, locals)),
            path: path2
          };
        }
        const named = NAMED_FUNCTIONS[head];
        if (named && node.args.length >= named.valueArity) {
          const start = node.args.length - named.valueArity;
          return {
            kind: "application",
            head,
            display: named.display,
            args: node.args.slice(start).map((a, i) => lowerExpression(a, argPath(path2, start + i), scope, locals)),
            path: path2
          };
        }
        const positional = POSITIONAL[head];
        if (positional && node.args.length > positional.index) {
          const primary = node.args[positional.index];
          if (positional.kind === "coercion") {
            const applied = node.args.slice(positional.index + 1);
            const fn = lowerExpression(primary, argPath(path2, positional.index), scope, locals);
            if (applied.length === 0)
              return fn;
            return {
              kind: "application",
              head,
              display: renderExpression(fn),
              args: applied.map((a, i) => lowerExpression(a, argPath(path2, positional.index + 1 + i), scope, locals)),
              path: path2
            };
          }
          const second = node.args[positional.index + 1];
          if (second) {
            const composed = {
              kind: "operator",
              op: "comp",
              symbol: "\u2218",
              args: [
                lowerExpression(primary, argPath(path2, positional.index), scope, locals),
                lowerExpression(second, argPath(path2, positional.index + 1), scope, locals)
              ],
              path: path2
            };
            const applied = node.args.slice(positional.index + 2);
            if (applied.length === 0)
              return composed;
            return {
              kind: "application",
              head,
              display: renderExpression(composed),
              args: applied.map((a, i) => lowerExpression(a, argPath(path2, positional.index + 2 + i), scope, locals)),
              path: path2
            };
          }
        }
        const local = locals.get(head);
        if (local) {
          const { args: values, offset } = mathematicalArgs(node.args);
          return {
            kind: "application",
            head,
            display: local.display,
            args: values.map((a, i) => lowerExpression(a, argPath(path2, offset + i), scope, locals)),
            path: path2
          };
        }
      }
      const { args } = mathematicalArgs(node.args);
      return {
        kind: "opaque",
        head: head ?? null,
        display: opaqueDisplay(node, path2, scope, locals),
        arity: args.length,
        path: path2
      };
    }
    default:
      return {
        kind: "opaque",
        head: null,
        display: opaqueDisplay(node, path2, scope, locals),
        arity: 0,
        path: path2
      };
  }
}
function lowerFilter(node, path2, scope, locals) {
  const head = headConstant(node);
  const entry = head === void 0 ? void 0 : FILTERS[head];
  if (!entry) {
    return {
      kind: "unknown",
      display: opaqueDisplay(node, path2, scope, locals),
      label: "an unnamed filter",
      point: null
    };
  }
  let point = null;
  if (entry.pointIndex !== null && node.kind === "app") {
    const { args, offset } = mathematicalArgs(node.args);
    const index = entry.pointIndex < 0 ? args.length + entry.pointIndex : entry.pointIndex;
    const chosen = args[index];
    if (chosen)
      point = lowerExpression(chosen, argPath(path2, offset + index), scope, locals);
  }
  const display = entry.kind === "at-top" ? "+\u221E" : entry.kind === "at-bot" ? "\u2212\u221E" : point ? renderExpression(point) : shortName(head);
  return { kind: entry.kind, display, label: entry.label, point };
}
function lowerProposition(node, path2, scope = [], locals = NO_LOCALS) {
  if (node.kind === "forall") {
    if (!mentionsBVar(node.body, 0)) {
      return {
        kind: "implication",
        antecedent: lowerProposition(node.binderType, `${path2}.binderType`, scope, locals),
        consequent: lowerProposition(node.body, `${path2}.body`, [...scope, node.binderName], locals),
        path: path2
      };
    }
    return { kind: "opaque", head: null, display: opaqueDisplay(node, path2, scope, locals), path: path2 };
  }
  if (node.kind === "app") {
    const head = headConstant(node);
    if (head !== void 0) {
      const relation = RELATIONS[head];
      if (relation && node.args.length >= relation.valueArity) {
        const start = node.args.length - relation.valueArity;
        if (head === "Iff") {
          const left = lowerProposition(node.args[start], argPath(path2, start), scope, locals);
          const right = lowerProposition(node.args[start + 1], argPath(path2, start + 1), scope, locals);
          return {
            kind: "relation",
            relation: "equivalent",
            lhs: {
              kind: "opaque",
              head: null,
              display: renderProposition(left),
              arity: 0,
              path: argPath(path2, start)
            },
            rhs: {
              kind: "opaque",
              head: null,
              display: renderProposition(right),
              arity: 0,
              path: argPath(path2, start + 1)
            },
            path: path2
          };
        }
        return {
          kind: "relation",
          relation: relation.relation,
          lhs: lowerExpression(node.args[start], argPath(path2, start), scope, locals),
          rhs: lowerExpression(node.args[start + 1], argPath(path2, start + 1), scope, locals),
          path: path2
        };
      }
      if (head === "Filter.Tendsto" && node.args.length >= 5) {
        return {
          kind: "limit",
          subject: lowerExpression(node.args[2], argPath(path2, 2), scope, locals),
          source: lowerFilter(node.args[3], argPath(path2, 3), scope, locals),
          target: lowerFilter(node.args[4], argPath(path2, 4), scope, locals),
          path: path2
        };
      }
      if (head === "And" && node.args.length >= 2) {
        const conjuncts = [];
        const collect = (n, p) => {
          if (headConstant(n) === "And" && n.kind === "app" && n.args.length >= 2) {
            collect(n.args[n.args.length - 2], argPath(p, n.args.length - 2));
            collect(n.args[n.args.length - 1], argPath(p, n.args.length - 1));
            return;
          }
          conjuncts.push(lowerProposition(n, p, scope, locals));
        };
        collect(node, path2);
        return { kind: "conjunction", conjuncts, path: path2 };
      }
      if (head === "Membership.mem" && node.args.length >= 2) {
        const collectionIndex = node.args.length - 2;
        const elementIndex = node.args.length - 1;
        return {
          kind: "membership",
          element: lowerExpression(node.args[elementIndex], argPath(path2, elementIndex), scope, locals),
          collection: lowerExpression(node.args[collectionIndex], argPath(path2, collectionIndex), scope, locals),
          path: path2
        };
      }
      if (head === "Exists" && node.args.length >= 2) {
        const predicateArg = node.args[node.args.length - 1];
        if (predicateArg.kind === "lam") {
          return {
            kind: "existential",
            binder: predicateArg.binderName,
            body: lowerProposition(predicateArg.body, `${argPath(path2, node.args.length - 1)}.body`, [...scope, predicateArg.binderName], locals),
            path: path2
          };
        }
      }
      const predicate = PREDICATES[head];
      if (predicate && node.args.length >= predicate.valueArity) {
        const start = node.args.length - predicate.valueArity;
        const values = node.args.slice(start).map((a, i) => lowerExpression(a, argPath(path2, start + i), scope, locals));
        return {
          kind: "predicate",
          predicate: predicate.predicate,
          name: shortName(head),
          subject: values[0] ?? null,
          args: values.slice(1),
          path: path2
        };
      }
    }
  }
  return {
    kind: "opaque",
    head: headConstant(node) ?? null,
    display: opaqueDisplay(node, path2, scope, locals),
    path: path2
  };
}
function lowerDeclaration(doc, decl, locals = NO_LOCALS) {
  const witness = kernelWitness(doc, decl);
  const parsed = parseDocstring(decl.docstring);
  const ref = sourceRefFor(doc, decl);
  const statementClaim = witness ? transcribe(witness, decl.statement.pretty, { sources: [ref] }) : derive(decl.statement.pretty, MATH_IR_RULES.lowerProposition, [], { sources: [ref] });
  const variables = decl.binders.filter((b) => b.role === "parameter").map((b) => ({
    id: b.fvarId,
    symbol: b.name,
    typeDisplay: b.type.pretty,
    binderInfo: b.binderInfo,
    annotation: annotationFor(parsed.annotations, b.name)
  }));
  const instances = decl.binders.filter((b) => b.role === "instance").map((b) => ({ id: b.fvarId, symbol: b.name, typeDisplay: b.type.pretty }));
  const hypotheses = decl.binders.filter((b) => b.role === "hypothesis").map((b) => {
    const proposition = lowerProposition(b.type.tree, `binders[${b.index}].type`, [], locals);
    return {
      id: b.fvarId,
      symbol: b.name,
      proposition,
      display: renderProposition(proposition),
      usage: b.usage
    };
  });
  const definitionBody = decl.definitionBody ? (() => {
    const expression = lowerExpression(decl.definitionBody.tree, "definitionBody", [], locals);
    return { expression, display: renderExpression(expression) };
  })() : null;
  const conclusionProp = lowerProposition(decl.conclusion.tree, "conclusion", [], locals);
  const conclusion = derive(conclusionProp, conclusionProp.kind === "opaque" ? MATH_IR_RULES.unrecognised : MATH_IR_RULES.lowerProposition, [statementClaim], { sources: [sourceRefFor(doc, decl, "conclusion")] });
  const ceiling = witness ? "verified" : "derived";
  return {
    id: decl.name,
    name: decl.name,
    namespace: decl.namespace,
    kind: decl.kind,
    documentation: parsed.prose,
    variables,
    hypotheses,
    instances,
    conclusion,
    conclusionDisplay: renderProposition(conclusionProp),
    definitionBody,
    statementDisplay: decl.statement.pretty,
    dependencies: decl.dependencies,
    trust: {
      axioms: decl.axioms,
      unusualAxioms: unusualAxioms(decl.axioms),
      usesSorry: decl.usesSorry,
      proofTermAvailable: decl.proofTermAvailable
    },
    annotations: parsed.annotations,
    suggestedVisual: parsed.suggestedVisual,
    concept: parsed.concept,
    ceiling,
    provenance: { sources: [ref] }
  };
}
function localConstantsOf(doc) {
  const locals = /* @__PURE__ */ new Map();
  for (const decl of doc.declarations) {
    if (decl.kind === "definition" || decl.kind === "opaque" || decl.kind === "axiom") {
      locals.set(decl.name, { display: decl.name.split(".").pop() ?? decl.name });
    }
  }
  return locals;
}
function lowerDocument(doc) {
  const locals = localConstantsOf(doc);
  return {
    mathIRVersion: MATH_IR_VERSION,
    system: doc.system,
    notationFidelity: doc.notationFidelity,
    theorems: doc.declarations.map((d) => lowerDeclaration(doc, d, locals))
  };
}

// packages/classifier/dist/rules.js
var RULES = {
  UPPER_BOUND: {
    id: "RELATION_UPPER_BOUND_001",
    description: "The conclusion bounds a quantity from above.",
    produces: "derived"
  },
  LOWER_BOUND: {
    id: "RELATION_LOWER_BOUND_001",
    description: "The conclusion bounds a quantity from below.",
    produces: "derived"
  },
  POSITIVITY: {
    id: "RELATION_POSITIVITY_001",
    description: "The conclusion asserts that a quantity has a definite sign.",
    produces: "derived"
  },
  DISTINCTNESS: {
    id: "RELATION_DISTINCTNESS_001",
    description: "The conclusion asserts that two quantities differ.",
    produces: "derived"
  },
  EQUALITY: {
    id: "RELATION_EQUALITY_001",
    description: "The conclusion is an equation.",
    produces: "derived"
  },
  FUNCTIONAL: {
    id: "RELATION_FUNCTIONAL_001",
    description: "The conclusion defines one quantity in terms of others.",
    produces: "derived"
  },
  MONOTONICITY: {
    id: "PREDICATE_MONOTONICITY_001",
    description: "The conclusion asserts a monotonicity property.",
    produces: "derived"
  },
  LIMIT: {
    id: "PREDICATE_LIMIT_001",
    description: "The conclusion asserts a limit along a filter.",
    produces: "derived"
  },
  PROPERTY: {
    id: "PREDICATE_PROPERTY_001",
    description: "The conclusion asserts a named property of a subject.",
    produces: "derived"
  },
  CONJUNCTION: {
    id: "PROPOSITION_CONJUNCTION_001",
    description: "The conclusion asserts several facts at once.",
    produces: "derived"
  },
  MEMBERSHIP: {
    id: "RELATION_MEMBERSHIP_001",
    description: "The conclusion asserts that a value lies in a collection.",
    produces: "derived"
  },
  EXISTENCE: {
    id: "PROPOSITION_EXISTENCE_001",
    description: "The conclusion asserts that something exists.",
    produces: "derived"
  },
  IMPLICATION: {
    id: "PROPOSITION_IMPLICATION_001",
    description: "The conclusion is an implication between two propositions.",
    produces: "derived"
  },
  EQUIVALENCE: {
    id: "PROPOSITION_EQUIVALENCE_001",
    description: "The conclusion asserts that two propositions are equivalent.",
    produces: "derived"
  },
  ASSUMPTION_SENSITIVITY: {
    id: "PROOF_ASSUMPTION_SENSITIVITY_001",
    description: "Occurrence analysis of the elaborated proof term identified which stated hypotheses it actually uses.",
    produces: "derived"
  },
  TRUST: {
    id: "PROOF_TRUST_BASE_001",
    description: "The declaration's axiom dependencies were inspected.",
    produces: "derived"
  },
  DEFINITION: {
    id: "DECLARATION_DEFINITION_001",
    description: "The declaration introduces a definition rather than asserting a proposition.",
    produces: "derived"
  },
  SENSITIVITY: {
    id: "PARAMETER_SENSITIVITY_001",
    description: "The direction in which the bound responds to each parameter follows from the sign hypotheses.",
    produces: "derived"
  },
  DEPENDENCY_GRAPH: {
    id: "GRAPH_DEPENDENCY_001",
    description: "Edges were read from the constants each declaration's proof term references.",
    produces: "derived"
  },
  UNSUPPORTED: {
    id: "STRUCTURE_UNSUPPORTED_001",
    description: "No deterministic classifier recognises this conclusion's structure.",
    produces: "derived"
  }
};

// packages/classifier/dist/signs.js
function signFactsOf(theorem) {
  const facts = /* @__PURE__ */ new Map();
  const record = (id, sign) => {
    const existing = facts.get(id);
    if (existing === "positive")
      return;
    facts.set(id, sign);
  };
  for (const hypothesis of theorem.hypotheses) {
    const prop = hypothesis.proposition;
    if (prop.kind !== "relation")
      continue;
    const { lhs, rhs, relation } = prop;
    const zeroLeft = lhs.kind === "number" && lhs.value === 0;
    const zeroRight = rhs.kind === "number" && rhs.value === 0;
    if (zeroLeft && rhs.kind === "variable") {
      if (relation === "less-than")
        record(rhs.id, "positive");
      else if (relation === "less-than-or-equal")
        record(rhs.id, "nonnegative");
    }
    if (zeroRight && lhs.kind === "variable") {
      if (relation === "greater-than")
        record(lhs.id, "positive");
      else if (relation === "greater-than-or-equal")
        record(lhs.id, "nonnegative");
      else if (relation === "less-than")
        record(lhs.id, "negative");
      else if (relation === "less-than-or-equal")
        record(lhs.id, "nonpositive");
    }
  }
  return facts;
}
function isStrictlyPositive(sign) {
  return sign === "positive";
}
function multiplySigns(a, b) {
  if (a === "zero" || b === "zero")
    return "zero";
  if (a === "unknown" || b === "unknown")
    return "unknown";
  const negA = a === "negative" || a === "nonpositive";
  const negB = b === "negative" || b === "nonpositive";
  const strict = (a === "positive" || a === "negative") && (b === "positive" || b === "negative");
  const negative = negA !== negB;
  if (negative)
    return strict ? "negative" : "nonpositive";
  return strict ? "positive" : "nonnegative";
}
function signOf(expr, facts) {
  switch (expr.kind) {
    case "number":
      if (expr.value > 0)
        return "positive";
      if (expr.value < 0)
        return "negative";
      return "zero";
    case "variable":
      return facts.get(expr.id) ?? "unknown";
    case "operator": {
      const [a, b] = expr.args;
      switch (expr.op) {
        case "mul":
          return a && b ? multiplySigns(signOf(a, facts), signOf(b, facts)) : "unknown";
        case "div":
          return a && b ? multiplySigns(signOf(a, facts), signOf(b, facts)) : "unknown";
        case "add": {
          if (!a || !b)
            return "unknown";
          const sa = signOf(a, facts);
          const sb = signOf(b, facts);
          if (isStrictlyPositive(sa) && (sb === "positive" || sb === "nonnegative" || sb === "zero"))
            return "positive";
          if (isStrictlyPositive(sb) && (sa === "positive" || sa === "nonnegative" || sa === "zero"))
            return "positive";
          if ((sa === "nonnegative" || sa === "zero" || sa === "positive") && (sb === "nonnegative" || sb === "zero" || sb === "positive"))
            return "nonnegative";
          return "unknown";
        }
        case "neg": {
          if (!a)
            return "unknown";
          return multiplySigns(signOf(a, facts), "negative");
        }
        case "abs":
          return "nonnegative";
        case "pow": {
          if (!a)
            return "unknown";
          const sa = signOf(a, facts);
          return sa === "positive" ? "positive" : "unknown";
        }
        default:
          return "unknown";
      }
    }
    case "application": {
      if (expr.head === "Real.sqrt")
        return "nonnegative";
      if (expr.head === "Real.exp")
        return "positive";
      if (expr.head === "Real.log") {
        const arg = expr.args[0];
        if (arg && arg.kind === "number") {
          if (arg.value > 1)
            return "positive";
          if (arg.value === 1)
            return "zero";
          if (arg.value > 0)
            return "negative";
        }
        return "unknown";
      }
      return "unknown";
    }
    default:
      return "unknown";
  }
}
function mentions(expr, variableId) {
  switch (expr.kind) {
    case "variable":
      return expr.id === variableId;
    case "operator":
    case "application":
      return expr.args.some((a) => mentions(a, variableId));
    case "lambda":
      return mentions(expr.body, variableId);
    default:
      return false;
  }
}
function flip(direction) {
  if (direction === "increasing")
    return "decreasing";
  if (direction === "decreasing")
    return "increasing";
  return direction;
}
function directionOf(expr, variableId, facts) {
  if (!mentions(expr, variableId))
    return "constant";
  switch (expr.kind) {
    case "variable":
      return expr.id === variableId ? "increasing" : "constant";
    case "operator": {
      const [a, b] = expr.args;
      switch (expr.op) {
        case "add": {
          if (!a || !b)
            return "unknown";
          const da = directionOf(a, variableId, facts);
          const db = directionOf(b, variableId, facts);
          if (da === "unknown" || db === "unknown")
            return "unknown";
          if (da === "increasing" && db !== "decreasing")
            return "increasing";
          if (db === "increasing" && da !== "decreasing")
            return "increasing";
          if (da === "decreasing" && db !== "increasing")
            return "decreasing";
          if (db === "decreasing" && da !== "increasing")
            return "decreasing";
          return "constant";
        }
        case "sub": {
          if (!a || !b)
            return "unknown";
          const da = directionOf(a, variableId, facts);
          const db = flip(directionOf(b, variableId, facts));
          if (da === "unknown" || db === "unknown")
            return "unknown";
          if (da === "increasing" && db !== "decreasing")
            return "increasing";
          if (db === "increasing" && da !== "decreasing")
            return "increasing";
          if (da === "decreasing" && db !== "increasing")
            return "decreasing";
          if (db === "decreasing" && da !== "increasing")
            return "decreasing";
          return "constant";
        }
        case "mul": {
          if (!a || !b)
            return "unknown";
          const aHas = mentions(a, variableId);
          const bHas = mentions(b, variableId);
          if (aHas && bHas)
            return "unknown";
          const [varying, fixed] = aHas ? [a, b] : [b, a];
          const coefficient = signOf(fixed, facts);
          const inner = directionOf(varying, variableId, facts);
          if (inner === "unknown")
            return "unknown";
          if (coefficient === "positive")
            return inner;
          if (coefficient === "negative")
            return flip(inner);
          return "unknown";
        }
        case "div": {
          if (!a || !b)
            return "unknown";
          const numeratorHas = mentions(a, variableId);
          const denominatorHas = mentions(b, variableId);
          if (numeratorHas && denominatorHas)
            return "unknown";
          if (numeratorHas) {
            const denominatorSign2 = signOf(b, facts);
            const inner2 = directionOf(a, variableId, facts);
            if (inner2 === "unknown")
              return "unknown";
            if (denominatorSign2 === "positive")
              return inner2;
            if (denominatorSign2 === "negative")
              return flip(inner2);
            return "unknown";
          }
          const numeratorSign = signOf(a, facts);
          const denominatorSign = signOf(b, facts);
          const inner = directionOf(b, variableId, facts);
          if (inner === "unknown")
            return "unknown";
          if (numeratorSign === "positive" && denominatorSign === "positive")
            return flip(inner);
          if (numeratorSign === "negative" && denominatorSign === "positive")
            return inner;
          return "unknown";
        }
        case "neg": {
          if (!a)
            return "unknown";
          return flip(directionOf(a, variableId, facts));
        }
        default:
          return "unknown";
      }
    }
    case "application":
      return "unknown";
    default:
      return "unknown";
  }
}
var DIRECTION_PHRASE = {
  increasing: "increases",
  decreasing: "decreases",
  constant: "does not change",
  unknown: "changes in a way ProofLens cannot determine from the stated hypotheses"
};

// packages/classifier/dist/classify.js
function makeClassification(theorem, rule, payload, rationale, path2) {
  const claim = derive(payload, rule, [theorem.conclusion], {
    sources: [
      {
        ...theorem.provenance.sources[0] ?? {
          system: "lean4",
          declaration: theorem.name
        },
        path: path2
      }
    ],
    inputs: [theorem.id]
  });
  return { rule, payload, claim, rationale };
}
function sensitivityOf(theorem, bound) {
  const facts = signFactsOf(theorem);
  return theorem.variables.map((v2) => ({
    variableId: v2.id,
    symbol: v2.symbol,
    direction: directionOf(bound, v2.id, facts)
  })).filter((s) => s.direction !== "constant" && s.direction !== "unknown");
}
function readingScore(bounded, bound) {
  const boundedScore = bounded.kind === "number" ? 0 : bounded.kind === "variable" ? 3 : bounded.kind === "opaque" ? 1 : 2;
  const boundBonus = bound.kind === "number" ? 1 : 0;
  return boundedScore + boundBonus;
}
function classifyPositivity(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "relation")
    return [];
  const { relation, lhs, rhs } = prop;
  const zeroLeft = lhs.kind === "number" && lhs.value === 0;
  const zeroRight = rhs.kind === "number" && rhs.value === 0;
  let quantity = null;
  let strict = false;
  let sense = "positive";
  if (zeroLeft && (relation === "less-than" || relation === "less-than-or-equal")) {
    quantity = rhs;
    strict = relation === "less-than";
  } else if (zeroRight && (relation === "greater-than" || relation === "greater-than-or-equal")) {
    quantity = lhs;
    strict = relation === "greater-than";
  } else if (zeroRight && (relation === "less-than" || relation === "less-than-or-equal")) {
    quantity = lhs;
    strict = relation === "less-than";
    sense = "negative";
  }
  if (!quantity)
    return [];
  return [
    makeClassification(theorem, RULES.POSITIVITY, { kind: "positivity", data: { quantity, strict, sense } }, `The conclusion compares \`${renderExpression(quantity)}\` against zero, so it asserts that the quantity is ${strict ? "strictly " : "non-strictly "}${sense}.`, quantity.path)
  ];
}
function classifyDistinctness(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "relation" || prop.relation !== "not-equal")
    return [];
  return [
    makeClassification(theorem, RULES.DISTINCTNESS, { kind: "distinctness", data: { left: prop.lhs, right: prop.rhs } }, `The conclusion asserts that \`${renderExpression(prop.lhs)}\` and \`${renderExpression(prop.rhs)}\` are different.`, prop.path)
  ];
}
function classifyBounds(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "relation")
    return [];
  const { relation, lhs, rhs } = prop;
  const out = [];
  const strict = relation === "less-than" || relation === "greater-than";
  const upper = relation === "less-than" || relation === "less-than-or-equal" ? { bounded: lhs, bound: rhs, boundedPath: lhs.path, boundPath: rhs.path } : relation === "greater-than" || relation === "greater-than-or-equal" ? { bounded: rhs, bound: lhs, boundedPath: rhs.path, boundPath: lhs.path } : null;
  if (upper) {
    const symbol = relation === "less-than" || relation === "greater-than" ? "<" : "\u2264";
    const upperScore = readingScore(upper.bounded, upper.bound);
    const lowerScore = readingScore(upper.bound, upper.bounded);
    const upperIsNatural = upperScore >= lowerScore;
    out.push(makeClassification(theorem, RULES.UPPER_BOUND, {
      kind: "upper-bound",
      data: {
        boundedQuantity: upper.bounded,
        bound: upper.bound,
        strict,
        natural: upperIsNatural,
        sensitivity: sensitivityOf(theorem, upper.bound)
      }
    }, `The conclusion \`${theorem.conclusionDisplay}\` puts \`${renderExpression(upper.bounded)}\` on the smaller side of \`${symbol}\`, so \`${renderExpression(upper.bound)}\` is an upper bound for it.`, upper.boundedPath));
    out.push(makeClassification(theorem, RULES.LOWER_BOUND, {
      kind: "lower-bound",
      data: {
        boundedQuantity: upper.bound,
        bound: upper.bounded,
        strict,
        natural: !upperIsNatural,
        sensitivity: sensitivityOf(theorem, upper.bounded)
      }
    }, `The same conclusion bounds \`${renderExpression(upper.bound)}\` from below by \`${renderExpression(upper.bounded)}\`.`, upper.boundPath));
  }
  return out;
}
function classifyEquality(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "relation" || prop.relation !== "equal")
    return [];
  const functional = prop.lhs.kind === "variable" && prop.rhs.kind !== "variable";
  const rule = functional ? RULES.FUNCTIONAL : RULES.EQUALITY;
  return [
    makeClassification(theorem, rule, {
      kind: functional ? "functional-relationship" : "equality",
      data: { left: prop.lhs, right: prop.rhs, functional }
    }, functional ? `The left side is the single quantity \`${renderExpression(prop.lhs)}\`, so the equation expresses it in terms of the others.` : `The conclusion equates \`${renderExpression(prop.lhs)}\` and \`${renderExpression(prop.rhs)}\`.`, prop.path)
  ];
}
function classifyMonotonicity(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "predicate")
    return [];
  const map = {
    monotone: { direction: "increasing", strict: false },
    "strictly-monotone": { direction: "increasing", strict: true },
    antitone: { direction: "decreasing", strict: false },
    "strictly-antitone": { direction: "decreasing", strict: true }
  };
  const entry = map[prop.predicate];
  if (!entry)
    return [];
  return [
    makeClassification(theorem, RULES.MONOTONICITY, {
      kind: "monotonicity",
      data: {
        direction: entry.direction,
        strict: entry.strict,
        subject: prop.subject,
        predicateName: prop.name
      }
    }, `The conclusion applies \`${prop.name}\`, which asserts a ${entry.strict ? "strictly " : ""}${entry.direction === "increasing" ? "increasing" : "decreasing"} relationship.`, prop.path)
  ];
}
function classifyLimit(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "limit")
    return [];
  const convergent = prop.target.kind === "neighbourhood" || prop.target.kind === "punctured";
  return [
    makeClassification(theorem, RULES.LIMIT, {
      kind: "limit",
      data: {
        subject: prop.subject,
        source: prop.source,
        target: prop.target,
        convergent
      }
    }, `The conclusion is a \`Filter.Tendsto\`: \`${renderExpression(prop.subject)}\` ${convergent ? `approaches \`${prop.target.display}\`` : `${prop.target.label}`} as its input ${prop.source.label}.`, prop.path)
  ];
}
function classifyExistence(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "existential")
    return [];
  return [
    makeClassification(theorem, RULES.EXISTENCE, { kind: "existence", data: { binder: prop.binder, body: prop.body } }, `The conclusion is an existential: it asserts that some \`${prop.binder}\` satisfying \`${renderProposition(prop.body)}\` exists, without ProofLens knowing which one the proof produces.`, prop.path)
  ];
}
function classifyProperty(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "predicate" || prop.predicate !== "other")
    return [];
  const label = PREDICATES[prop.name]?.label ?? prop.name;
  return [
    makeClassification(theorem, RULES.PROPERTY, {
      kind: "property",
      data: { name: prop.name, label, subject: prop.subject, args: prop.args }
    }, `The conclusion applies \`${prop.name}\`, which ProofLens reads as "${prop.subject ? renderExpression(prop.subject) : "the subject"} is ${label}". ProofLens recognises the property but does not interpret what it means.`, prop.path)
  ];
}
function classifyConjunction(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "conjunction")
    return [];
  return [
    makeClassification(theorem, RULES.CONJUNCTION, { kind: "conjunction", data: { conjuncts: prop.conjuncts } }, `The conclusion asserts ${prop.conjuncts.length} facts at once: ${prop.conjuncts.map((c) => `\`${renderProposition(c)}\``).join(" and ")}.`, prop.path)
  ];
}
function classifyMembership(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind !== "membership")
    return [];
  return [
    makeClassification(theorem, RULES.MEMBERSHIP, { kind: "membership", data: { element: prop.element, collection: prop.collection } }, `The conclusion places \`${renderExpression(prop.element)}\` inside \`${renderExpression(prop.collection)}\`.`, prop.path)
  ];
}
function classifyImplication(theorem) {
  const prop = theorem.conclusion.value;
  if (prop.kind === "implication") {
    return [
      makeClassification(theorem, RULES.IMPLICATION, { kind: "implication", data: { antecedent: prop.antecedent, consequent: prop.consequent } }, `The conclusion has the shape \`A \u2192 B\`, with A = \`${renderProposition(prop.antecedent)}\` and B = \`${renderProposition(prop.consequent)}\`.`, prop.path)
    ];
  }
  if (prop.kind === "relation" && prop.relation === "equivalent") {
    return [
      makeClassification(theorem, RULES.EQUIVALENCE, {
        kind: "equivalence",
        data: {
          antecedent: {
            kind: "opaque",
            head: null,
            display: renderExpression(prop.lhs),
            path: prop.lhs.path
          },
          consequent: {
            kind: "opaque",
            head: null,
            display: renderExpression(prop.rhs),
            path: prop.rhs.path
          }
        }
      }, `The conclusion is an \`\u2194\`, so each side holds exactly when the other does.`, prop.path)
    ];
  }
  return [];
}
function classifyAssumptionSensitivity(theorem) {
  if (theorem.hypotheses.length === 0)
    return [];
  const unused = theorem.hypotheses.filter((h) => h.usage.unusedInProof);
  const used = theorem.hypotheses.filter((h) => !h.usage.unusedInProof);
  const proofTermAvailable = theorem.hypotheses.some((h) => h.usage.proofTermAvailable);
  if (!proofTermAvailable)
    return [];
  const rationale = unused.length === 0 ? `Every one of the ${theorem.hypotheses.length} stated hypotheses occurs in the elaborated proof term.` : `${unused.map((h) => `\`${h.symbol}\``).join(", ")} ${unused.length === 1 ? "does" : "do"} not occur in the elaborated proof term, in any later hypothesis type, or in the conclusion.`;
  return [
    makeClassification(theorem, RULES.ASSUMPTION_SENSITIVITY, { kind: "assumption-sensitivity", data: { used, unusedInProof: unused, proofTermAvailable } }, rationale, "proofTerm")
  ];
}
function classifyTrust(theorem) {
  const { usesSorry, unusualAxioms: unusualAxioms2 } = theorem.trust;
  if (!usesSorry && unusualAxioms2.length === 0)
    return [];
  return [
    makeClassification(theorem, RULES.TRUST, { kind: "trust", data: { usesSorry, unusualAxioms: unusualAxioms2 } }, usesSorry ? "The proof term reaches `sorryAx`. This statement has NOT been proved." : `The proof depends on axioms beyond Lean's standard three: ${unusualAxioms2.join(", ")}.`, "axioms")
  ];
}
function classifyDefinition(theorem) {
  if (theorem.kind !== "definition" && theorem.kind !== "opaque")
    return [];
  const body = theorem.definitionBody;
  const out = [
    makeClassification(theorem, RULES.DEFINITION, {
      kind: "definition",
      data: {
        name: theorem.name,
        body: body?.expression ?? null,
        bodyDisplay: body?.display ?? null
      }
    }, body ? `\`${theorem.name}\` is a ${theorem.kind} that unfolds to \`${body.display}\`.` : `\`${theorem.name}\` is a ${theorem.kind}, so it introduces a concept rather than asserting a proposition.`, "statement")
  ];
  if (body) {
    const short = theorem.name.split(".").pop() ?? theorem.name;
    const args = theorem.variables.map((v2) => ({
      kind: "variable",
      id: v2.id,
      symbol: v2.symbol,
      path: `binders.${v2.symbol}`
    }));
    const left = args.length === 0 ? { kind: "constant", name: theorem.name, display: short, path: "statement" } : { kind: "application", head: theorem.name, display: short, args, path: "statement" };
    out.push(makeClassification(theorem, RULES.FUNCTIONAL, {
      kind: "functional-relationship",
      data: { left, right: body.expression, functional: true }
    }, `The definition expresses \`${short}\` in terms of ${args.length === 0 ? "no parameters" : args.map((a) => `\`${renderExpression(a)}\``).join(", ")}.`, "definitionBody"));
  }
  return out;
}
function classifyTheorem(theorem) {
  const structural = [
    ...classifyDefinition(theorem),
    ...classifyLimit(theorem),
    ...classifyProperty(theorem),
    ...classifyConjunction(theorem),
    ...classifyMembership(theorem),
    ...classifyExistence(theorem),
    ...classifyPositivity(theorem),
    ...classifyDistinctness(theorem),
    ...classifyBounds(theorem),
    ...classifyEquality(theorem),
    ...classifyMonotonicity(theorem),
    ...classifyImplication(theorem)
  ];
  const analytical = [...classifyAssumptionSensitivity(theorem), ...classifyTrust(theorem)];
  if (structural.length === 0) {
    const prop = theorem.conclusion.value;
    const head = prop.kind === "opaque" ? prop.head : null;
    structural.push(makeClassification(theorem, RULES.UNSUPPORTED, {
      kind: "unsupported",
      data: {
        reason: head === null ? "The conclusion's structure is not one ProofLens has a rule for yet." : prop.kind === "opaque" ? `\`${head}\` is not in ProofLens's constant table yet.` : `ProofLens can read this conclusion as a ${prop.kind} on \`${head}\`, but has no classifier that acts on it yet.`,
        head
      }
    }, `No deterministic classifier matched \`${theorem.conclusionDisplay}\`. The formal statement, its structure, and its dependencies are still available below.`, theorem.conclusion.value.path));
  }
  return [...structural, ...analytical];
}
function primaryClassification(classifications) {
  const naturalBound2 = classifications.find((c) => (c.payload.kind === "upper-bound" || c.payload.kind === "lower-bound") && c.payload.data.natural);
  const priority = [
    "limit",
    "positivity",
    "monotonicity",
    "distinctness",
    "upper-bound",
    "lower-bound",
    "functional-relationship",
    "equality",
    "implication",
    "equivalence",
    "existence",
    "property",
    "membership",
    "conjunction",
    "definition",
    "unsupported"
  ];
  for (const kind of priority) {
    if ((kind === "upper-bound" || kind === "lower-bound") && naturalBound2)
      return naturalBound2;
    const found = classifications.find((c) => c.payload.kind === kind);
    if (found)
      return found;
  }
  return classifications[0];
}

// packages/classifier/dist/explain.js
function layer(id, title, text2, status, rule, theorem) {
  return {
    id,
    title,
    claim: derive(text2, { ...rule, produces: status }, [theorem.conclusion], {
      sources: theorem.provenance.sources,
      inputs: [theorem.id]
    })
  };
}
function explain(theorem, classifications, options = {}) {
  const layers = [];
  const witness = options.formalDocument && options.formalDeclaration ? kernelWitness(options.formalDocument, options.formalDeclaration) : null;
  const isDefinition = theorem.kind === "definition" || theorem.kind === "opaque";
  const shortName2 = theorem.name.split(".").pop() ?? theorem.name;
  if (witness) {
    layers.push({
      id: "formal",
      // A definition is accepted by the kernel, not proved by it. Titling its
      // type "What was proved" would be a small lie in the layer a reader
      // trusts most.
      title: isDefinition ? "What was defined" : "What was proved",
      claim: transcribe(witness, theorem.statementDisplay, {
        sources: theorem.provenance.sources
      })
    });
  } else {
    layers.push(layer("formal", "What was stated", theorem.statementDisplay, "derived", RULES.TRUST, theorem));
  }
  const prop = theorem.conclusion.value;
  let mathematical;
  if (prop.kind === "relation") {
    mathematical = `${renderExpression(prop.lhs)} ${RELATION_PHRASE[prop.relation]} ${renderExpression(prop.rhs)}.`;
  } else if (prop.kind === "predicate") {
    mathematical = `${prop.subject ? renderExpression(prop.subject) : "the subject"} satisfies ${prop.name}.`;
  } else if (prop.kind === "limit") {
    mathematical = `${renderExpression(prop.subject)} ${prop.target.kind === "neighbourhood" || prop.target.kind === "punctured" ? `approaches ${prop.target.display}` : prop.target.label} as its input ${prop.source.label}.`;
  } else if (prop.kind === "conjunction") {
    mathematical = `The conclusion asserts ${prop.conjuncts.length} things at once.`;
  } else if (prop.kind === "membership") {
    mathematical = `${renderExpression(prop.element)} lies in ${renderExpression(prop.collection)}.`;
  } else if (prop.kind === "existential") {
    mathematical = `Some ${prop.binder} exists for which ${renderProposition(prop.body)}.`;
  } else if (prop.kind === "implication") {
    mathematical = "The conclusion asserts that one proposition follows from another.";
  } else if (isDefinition && theorem.definitionBody) {
    mathematical = `${shortName2} is defined to be ${theorem.definitionBody.display}.`;
  } else if (isDefinition) {
    mathematical = `${shortName2} introduces a ${theorem.kind} of type ${theorem.conclusionDisplay}. Its body was not extracted.`;
  } else {
    mathematical = `The conclusion is \`${theorem.conclusionDisplay}\`. ProofLens does not have a reading for its head symbol.`;
  }
  layers.push(layer("mathematical", "In words", mathematical, "derived", RULES.EQUALITY, theorem));
  const bound = classifications.find((c) => c.payload.kind === "upper-bound" && c.payload.data.natural);
  const lower = classifications.find((c) => c.payload.kind === "lower-bound");
  const mono = classifications.find((c) => c.payload.kind === "monotonicity");
  const limit = classifications.find((c) => c.payload.kind === "limit");
  const functional = classifications.find((c) => c.payload.kind === "functional-relationship");
  const unsupported = classifications.find((c) => c.payload.kind === "unsupported");
  let structural;
  if (bound && bound.payload.kind === "upper-bound") {
    const { boundedQuantity, bound: boundExpr, strict } = bound.payload.data;
    structural = `The theorem establishes ${strict ? "a strict " : "an "}upper bound: \`${renderExpression(boundedQuantity)}\` cannot exceed \`${renderExpression(boundExpr)}\` under the stated assumptions.`;
  } else if (lower && lower.payload.kind === "lower-bound") {
    const { boundedQuantity, bound: boundExpr } = lower.payload.data;
    structural = `The theorem establishes a lower bound: \`${renderExpression(boundedQuantity)}\` is at least \`${renderExpression(boundExpr)}\`.`;
  } else if (limit && limit.payload.kind === "limit") {
    const { subject, source, target, convergent } = limit.payload.data;
    structural = convergent ? `The theorem establishes a limit: \`${renderExpression(subject)}\` converges to \`${target.display}\` as its input ${source.label}.` : `The theorem establishes a divergence: \`${renderExpression(subject)}\` ${target.label} as its input ${source.label}.`;
  } else if (mono && mono.payload.kind === "monotonicity") {
    const { direction, strict, subject } = mono.payload.data;
    structural = `The theorem establishes that ${subject ? `\`${renderExpression(subject)}\`` : "the function"} is ${strict ? "strictly " : ""}${direction}.`;
  } else if (functional && functional.payload.kind === "functional-relationship") {
    structural = `${isDefinition ? "The definition expresses" : "The theorem defines"} \`${renderExpression(functional.payload.data.left)}\` in terms of the other quantities.`;
  } else if (unsupported && unsupported.payload.kind === "unsupported") {
    structural = `No deterministic visualization classifier currently supports this theorem structure. ${unsupported.payload.data.reason}`;
  } else {
    structural = "The theorem relates the quantities in its statement.";
  }
  layers.push(layer("structural", "What kind of statement this is", structural, "derived", RULES.UPPER_BOUND, theorem));
  const sensitivity = classifications.find((c) => c.payload.kind === "assumption-sensitivity");
  if (sensitivity && sensitivity.payload.kind === "assumption-sensitivity") {
    const { unusedInProof, used } = sensitivity.payload.data;
    const text2 = unusedInProof.length === 0 ? `All ${used.length} stated hypotheses are used by this proof.` : `${unusedInProof.map((h) => `\`${h.symbol} : ${h.display}\``).join(" and ")} ${unusedInProof.length === 1 ? "is" : "are"} stated but never used by this proof term. That does not mean the hypothesis is mathematically unnecessary \u2014 only that this particular proof does not touch it.`;
    layers.push(layer("assumptions", "Which assumptions did the work", text2, "derived", RULES.ASSUMPTION_SENSITIVITY, theorem));
  }
  const naturalBound2 = classifications.find((c) => (c.payload.kind === "upper-bound" || c.payload.kind === "lower-bound") && c.payload.data.natural);
  if (naturalBound2 && (naturalBound2.payload.kind === "upper-bound" || naturalBound2.payload.kind === "lower-bound")) {
    const { sensitivity: directions, bound: boundExpr } = naturalBound2.payload.data;
    const which = naturalBound2.payload.kind === "upper-bound" ? "upper bound" : "lower bound";
    if (directions.length > 0) {
      const clauses = directions.map((d) => `increasing \`${d.symbol}\` ${DIRECTION_PHRASE[d.direction]} it`);
      layers.push(layer("parameters", `How the ${which} responds`, `The ${which} is \`${renderExpression(boundExpr)}\`. Holding the other quantities fixed, and using only the sign hypotheses this theorem states: ${clauses.join("; ")}.`, "derived", RULES.SENSITIVITY, theorem));
    }
  }
  if (theorem.trust.usesSorry) {
    layers.push(layer("trust", "Not proved", "This declaration's proof reaches `sorryAx`. Nothing about it has been verified, and every reading below is about the statement, not about a theorem.", "derived", RULES.TRUST, theorem));
  } else if (theorem.trust.unusualAxioms.length > 0) {
    layers.push(layer("trust", "Trust base", `Beyond Lean's standard axioms, this proof depends on: ${theorem.trust.unusualAxioms.join(", ")}.`, "derived", RULES.TRUST, theorem));
  }
  const annotated = theorem.variables.filter((v2) => v2.annotation?.meaning);
  if (annotated.length > 0) {
    const text2 = annotated.map((v2) => `\`${v2.symbol}\` is ${v2.annotation.meaning}${v2.annotation.units ? ` (${v2.annotation.units})` : ""}`).join("; ");
    layers.push({
      id: "domain",
      title: "What the symbols mean",
      // Author-declared meaning. Lean verified none of it.
      claim: derive(`${text2}. These readings come from the declaration's ProofLens annotations, not from anything Lean checked.`, { ...RULES.SENSITIVITY, id: "SEMANTIC_ANNOTATION_001", produces: "interpreted" }, [], { sources: theorem.provenance.sources })
    });
  }
  return layers;
}

// packages/classifier/dist/dependencies.js
function dependencyGraph(doc) {
  const { edges, externalCount } = localDependencyEdges(doc);
  const outgoing = /* @__PURE__ */ new Map();
  for (const e of edges) {
    const list = outgoing.get(e.from) ?? [];
    list.push(e.to);
    outgoing.set(e.from, list);
  }
  const depthCache = /* @__PURE__ */ new Map();
  const visiting = /* @__PURE__ */ new Set();
  const depthOf = (id) => {
    const cached = depthCache.get(id);
    if (cached !== void 0)
      return cached;
    if (visiting.has(id))
      return 0;
    visiting.add(id);
    const deps = outgoing.get(id) ?? [];
    const depth = deps.length === 0 ? 0 : 1 + Math.max(...deps.map(depthOf));
    visiting.delete(id);
    depthCache.set(id, depth);
    return depth;
  };
  const nodes = doc.declarations.map((d) => ({
    id: d.name,
    label: d.name.split(".").pop() ?? d.name,
    kind: d.kind,
    depth: depthOf(d.name),
    concept: null
  }));
  const graph = { nodes, edges, externalDependencyCount: externalCount };
  return derive(graph, RULES.DEPENDENCY_GRAPH, [], {
    sources: doc.declarations.map((d) => ({
      system: doc.system,
      declaration: d.name,
      module: d.source?.module ?? null
    })),
    note: "Edges are the constants each proof term actually references. Dependencies on declarations outside the extracted modules are counted, not drawn."
  });
}
function subgraphFor(graph, root) {
  const keep = /* @__PURE__ */ new Set();
  const stack = [root];
  const adjacency = /* @__PURE__ */ new Map();
  for (const e of graph.edges) {
    const list = adjacency.get(e.from) ?? [];
    list.push(e.to);
    adjacency.set(e.from, list);
  }
  while (stack.length > 0) {
    const id = stack.pop();
    if (keep.has(id))
      continue;
    keep.add(id);
    for (const next of adjacency.get(id) ?? [])
      stack.push(next);
  }
  return {
    nodes: graph.nodes.filter((n) => keep.has(n.id)),
    edges: graph.edges.filter((e) => keep.has(e.from) && keep.has(e.to)),
    externalDependencyCount: graph.externalDependencyCount
  };
}

// packages/visual-ir/dist/plan.js
function refFor(theorem, path2) {
  const base = theorem.provenance.sources[0];
  const ref = base ? { ...base } : { system: "lean4", declaration: theorem.name };
  if (path2 !== void 0)
    ref.path = path2;
  return ref;
}
function epistemicNotice(status, what) {
  return {
    id: `notice:${what}`,
    kind: "legend",
    text: status === "illustrative" ? "Positions in this figure are schematic. They show which side of the bound a quantity lies on, not how large anything is." : `This figure is ${status}.`,
    epistemic: "illustrative"
  };
}
function planBound(theorem, classification, direction) {
  const payload = classification.payload;
  if (payload.kind !== "upper-bound" && payload.kind !== "lower-bound")
    return null;
  const { boundedQuantity, bound, strict, sensitivity } = payload.data;
  const boundedLabel = renderExpression(boundedQuantity);
  const boundLabel = renderExpression(bound);
  const status = classification.claim.status;
  const boundedAnnotation = theorem.variables.find((v2) => boundedQuantity.kind === "variable" && v2.id === boundedQuantity.id)?.annotation;
  const permittedSide = direction === "upper" ? "left" : "right";
  const annotations = [
    {
      id: "rationale",
      kind: "rationale",
      text: classification.rationale,
      epistemic: status
    },
    epistemicNotice("illustrative", "axis")
  ];
  for (const s of sensitivity) {
    annotations.push({
      id: `sensitivity:${s.variableId}`,
      kind: "callout",
      text: `Increasing ${s.symbol} ${DIRECTION_PHRASE[s.direction]} the bound.`,
      target: "bound",
      epistemic: "derived"
    });
  }
  if (theorem.trust.usesSorry) {
    annotations.push({
      id: "warning:sorry",
      kind: "warning",
      text: "This statement is not proved: its proof reaches `sorryAx`.",
      epistemic: "derived"
    });
  }
  return {
    id: `${theorem.id}:${direction}-bound`,
    type: direction === "upper" ? "upper-bound-plot" : "lower-bound-plot",
    title: `${boundedLabel} ${direction === "upper" ? strict ? "<" : "\u2264" : strict ? ">" : "\u2265"} ${boundLabel}`,
    subtitle: theorem.concept ?? theorem.name.split(".").pop(),
    entities: [
      {
        id: "bounded",
        kind: "quantity",
        label: boundedLabel,
        detail: boundedAnnotation?.meaning ? `${boundedAnnotation.meaning}${boundedAnnotation.units ? ` (${boundedAnnotation.units})` : ""}` : void 0,
        position: { x: direction === "upper" ? 0.32 : 0.68 },
        emphasis: "primary",
        state: "permitted",
        epistemic: status,
        sourceRef: refFor(theorem, boundedQuantity.path)
      },
      {
        id: "bound",
        kind: "bound",
        label: boundLabel,
        position: { x: 0.5 },
        emphasis: "primary",
        state: strict ? "excluded" : "permitted",
        epistemic: status,
        sourceRef: refFor(theorem, bound.path)
      },
      {
        id: "permitted-region",
        kind: "region",
        label: `${boundedLabel} may lie here`,
        position: { x: permittedSide === "left" ? 0.25 : 0.75 },
        state: "permitted",
        emphasis: "secondary",
        // The region is a real consequence of the theorem; only its drawn
        // extent is a display choice.
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      },
      {
        id: "excluded-region",
        kind: "region",
        label: `ruled out by the theorem`,
        position: { x: permittedSide === "left" ? 0.75 : 0.25 },
        state: "excluded",
        emphasis: "muted",
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      }
    ],
    relationships: [
      {
        id: "bounded-by",
        kind: "bounded-by",
        from: "bounded",
        to: "bound",
        label: strict ? "strictly" : "at most",
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      }
    ],
    axes: [
      {
        id: "value",
        orientation: "horizontal",
        label: boundedAnnotation?.meaning ?? boundedLabel,
        units: boundedAnnotation?.units,
        scale: "schematic",
        ticks: [{ at: 0.5, label: boundLabel, emphasis: "primary" }],
        // Nothing here is measured. Saying otherwise would be a fabrication.
        epistemic: "illustrative"
      }
    ],
    annotations,
    epistemic: weakest(status, "illustrative"),
    provenance: {
      sources: [refFor(theorem, "conclusion")],
      rule: classification.rule,
      inputs: [theorem.id]
    },
    rationale: classification.rationale
  };
}
function planLimit(theorem, classification) {
  if (classification.payload.kind !== "limit")
    return null;
  const { subject, source, target, convergent } = classification.payload.data;
  const status = classification.claim.status;
  const label = renderExpression(subject);
  const entities = [
    {
      id: "function",
      kind: "function",
      label,
      detail: convergent ? `approaches ${target.display}` : target.label,
      position: { x: 0.5, y: convergent ? 0.35 : 0.5 },
      emphasis: "primary",
      epistemic: status,
      sourceRef: refFor(theorem, subject.path)
    },
    {
      id: "direction",
      kind: "label",
      label: source.display,
      detail: `input ${source.label}`,
      position: { x: 0.95, y: 0 },
      emphasis: "secondary",
      epistemic: status,
      sourceRef: refFor(theorem, "conclusion")
    }
  ];
  if (!convergent) {
    entities.push({
      id: "divergence",
      kind: "label",
      label: target.display,
      detail: target.label,
      position: { x: 0.5, y: target.kind === "at-bot" ? 0 : 1 },
      emphasis: "primary",
      epistemic: status,
      sourceRef: refFor(theorem, "conclusion")
    });
  }
  if (convergent) {
    entities.push({
      id: "limit-value",
      kind: "bound",
      label: target.display,
      detail: "the limit",
      position: { x: 0.5, y: 0.3 },
      emphasis: "primary",
      state: "permitted",
      epistemic: status,
      sourceRef: refFor(theorem, target.point?.path ?? "conclusion")
    });
  }
  return {
    id: `${theorem.id}:limit`,
    type: "limit-plot",
    title: convergent ? `${label} \u27F6 ${target.display}` : `${label} ${target.label}`,
    subtitle: theorem.concept ?? theorem.name.split(".").pop(),
    entities,
    relationships: convergent ? [
      {
        id: "approaches",
        kind: "maps-to",
        from: "function",
        to: "limit-value",
        label: `as the input ${source.label}`,
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      }
    ] : [],
    axes: [
      {
        id: "input",
        orientation: "horizontal",
        label: `input (${source.label})`,
        scale: "schematic",
        ticks: [],
        epistemic: "illustrative"
      },
      {
        id: "output",
        orientation: "vertical",
        label: "value",
        scale: "schematic",
        ticks: convergent ? [{ at: 0.3, label: target.display, emphasis: "primary" }] : [],
        epistemic: "illustrative"
      }
    ],
    annotations: [
      { id: "rationale", kind: "rationale", text: classification.rationale, epistemic: status },
      {
        id: "shape-notice",
        kind: "legend",
        text: convergent ? "The drawn curve is one arbitrary function with the proved limit. The theorem constrains where the values end up, not the path they take to get there." : "The theorem says the values leave every bound. The drawn curve is illustrative; no rate of growth is claimed.",
        epistemic: "illustrative"
      }
    ],
    epistemic: weakest(status, "illustrative"),
    provenance: {
      sources: [refFor(theorem, "conclusion")],
      rule: classification.rule,
      inputs: [theorem.id]
    },
    rationale: classification.rationale
  };
}
function planPositivity(theorem, classification) {
  if (classification.payload.kind !== "positivity")
    return null;
  const { quantity, strict, sense } = classification.payload.data;
  const status = classification.claim.status;
  const label = renderExpression(quantity);
  const positive = sense === "positive";
  const zeroAt = positive ? 0.32 : 0.68;
  const quantityAt = positive ? 0.68 : 0.32;
  const comparator = positive ? strict ? ">" : "\u2265" : strict ? "<" : "\u2264";
  return {
    id: `${theorem.id}:positivity`,
    type: "number-line",
    title: `${label} ${comparator} 0`,
    subtitle: theorem.concept ?? theorem.name.split(".").pop(),
    entities: [
      {
        id: "zero",
        kind: "bound",
        label: "0",
        position: { x: zeroAt },
        emphasis: "secondary",
        state: strict ? "excluded" : "permitted",
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      },
      {
        id: "quantity",
        kind: "quantity",
        label,
        position: { x: quantityAt },
        emphasis: "primary",
        state: "permitted",
        epistemic: status,
        sourceRef: refFor(theorem, quantity.path)
      },
      {
        id: "permitted-region",
        kind: "region",
        label: `${label} lies here`,
        position: { x: positive ? 0.72 : 0.28 },
        state: "permitted",
        emphasis: "secondary",
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      },
      {
        id: "excluded-region",
        kind: "region",
        label: "ruled out by the theorem",
        position: { x: positive ? 0.14 : 0.86 },
        state: "excluded",
        emphasis: "muted",
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      }
    ],
    relationships: [
      {
        id: "sign",
        kind: "bounded-by",
        from: positive ? "zero" : "quantity",
        to: positive ? "quantity" : "zero",
        label: strict ? "strictly less than" : "at most",
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      }
    ],
    axes: [
      {
        id: "value",
        orientation: "horizontal",
        label,
        scale: "schematic",
        ticks: [{ at: zeroAt, label: "0", emphasis: "primary" }],
        epistemic: "illustrative"
      }
    ],
    annotations: [
      { id: "rationale", kind: "rationale", text: classification.rationale, epistemic: status },
      epistemicNotice("illustrative", "axis")
    ],
    epistemic: weakest(status, "illustrative"),
    provenance: {
      sources: [refFor(theorem, "conclusion")],
      rule: classification.rule,
      inputs: [theorem.id]
    },
    rationale: classification.rationale
  };
}
function planMonotonicity(theorem, classification) {
  if (classification.payload.kind !== "monotonicity")
    return null;
  const { direction, strict, subject, predicateName } = classification.payload.data;
  const status = classification.claim.status;
  const label = subject ? renderExpression(subject) : "f";
  return {
    id: `${theorem.id}:monotonicity`,
    type: "monotonicity-plot",
    title: `${predicateName} ${label}`,
    subtitle: theorem.concept ?? theorem.name.split(".").pop(),
    entities: [
      {
        id: "function",
        kind: "function",
        label,
        detail: `${strict ? "strictly " : ""}${direction}`,
        position: { x: 0.5, y: 0.5 },
        emphasis: "primary",
        epistemic: status,
        sourceRef: refFor(theorem, subject?.path ?? "conclusion")
      },
      {
        id: "input-lower",
        kind: "label",
        label: "u",
        position: { x: 0.2, y: 0 },
        epistemic: "illustrative"
      },
      {
        id: "input-upper",
        kind: "label",
        label: "v",
        position: { x: 0.8, y: 0 },
        epistemic: "illustrative"
      }
    ],
    relationships: [
      {
        id: "order-preserved",
        kind: "maps-to",
        from: "input-lower",
        to: "input-upper",
        label: direction === "increasing" ? "u \u2264 v \u27F9 f u \u2264 f v" : "u \u2264 v \u27F9 f v \u2264 f u",
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      }
    ],
    axes: [
      {
        id: "input",
        orientation: "horizontal",
        label: "input",
        scale: "schematic",
        ticks: [],
        epistemic: "illustrative"
      },
      {
        id: "output",
        orientation: "vertical",
        label: "output",
        scale: "schematic",
        ticks: [],
        epistemic: "illustrative"
      }
    ],
    annotations: [
      { id: "rationale", kind: "rationale", text: classification.rationale, epistemic: status },
      {
        id: "shape-notice",
        kind: "legend",
        text: "The drawn curve is one arbitrary function with the proved order property. The theorem constrains the ordering, not the shape.",
        epistemic: "illustrative"
      }
    ],
    epistemic: weakest(status, "illustrative"),
    provenance: {
      sources: [refFor(theorem, "conclusion")],
      rule: classification.rule,
      inputs: [theorem.id]
    },
    rationale: classification.rationale
  };
}
function planAssumptionSensitivity(theorem, classification) {
  if (classification.payload.kind !== "assumption-sensitivity")
    return null;
  const { used, unusedInProof } = classification.payload.data;
  const status = classification.claim.status;
  const entities = [];
  const relationships = [];
  used.forEach((h, i) => {
    entities.push({
      id: `hyp:${h.id}`,
      kind: "hypothesis",
      label: h.symbol,
      detail: h.display,
      position: { layer: 0, order: i },
      emphasis: "primary",
      state: "used",
      epistemic: status,
      sourceRef: refFor(theorem, `binders.${h.symbol}`)
    });
    relationships.push({
      id: `uses:${h.id}`,
      kind: "supports",
      from: `hyp:${h.id}`,
      to: "conclusion",
      epistemic: status,
      state: "used",
      sourceRef: refFor(theorem, "proofTerm")
    });
  });
  unusedInProof.forEach((h, i) => {
    entities.push({
      id: `hyp:${h.id}`,
      kind: "hypothesis",
      label: h.symbol,
      detail: h.display,
      position: { layer: 0, order: used.length + i },
      emphasis: "muted",
      state: "unused",
      epistemic: status,
      sourceRef: refFor(theorem, `binders.${h.symbol}`)
    });
  });
  entities.push({
    id: "conclusion",
    kind: "conclusion",
    label: theorem.conclusionDisplay,
    position: { layer: 1, order: 0 },
    emphasis: "primary",
    epistemic: theorem.conclusion.status,
    sourceRef: refFor(theorem, "conclusion")
  });
  const annotations = [
    { id: "rationale", kind: "rationale", text: classification.rationale, epistemic: status },
    {
      id: "caveat",
      kind: "legend",
      text: "Detached hypotheses do not occur in this proof term. A different proof of the same statement might need them, so this is a fact about the proof, not about mathematical necessity.",
      epistemic: "derived"
    }
  ];
  if (unusedInProof.length > 0) {
    annotations.push({
      id: "callout:unused",
      kind: "callout",
      text: `${unusedInProof.length} of ${used.length + unusedInProof.length} hypotheses are never used.`,
      epistemic: status
    });
  }
  return {
    id: `${theorem.id}:assumptions`,
    type: "assumption-sensitivity",
    title: "Which assumptions the proof actually uses",
    subtitle: theorem.name.split(".").pop(),
    entities,
    relationships,
    axes: [],
    annotations,
    epistemic: status,
    provenance: {
      sources: [refFor(theorem, "proofTerm")],
      rule: classification.rule,
      inputs: [theorem.id]
    },
    rationale: classification.rationale
  };
}
function planImplication(theorem, classification) {
  if (classification.payload.kind !== "implication" && classification.payload.kind !== "equivalence")
    return null;
  const { antecedent, consequent } = classification.payload.data;
  const status = classification.claim.status;
  const bidirectional = classification.payload.kind === "equivalence";
  return {
    id: `${theorem.id}:implication`,
    type: "implication-graph",
    title: bidirectional ? "Two equivalent statements" : "What follows from what",
    subtitle: theorem.name.split(".").pop(),
    entities: [
      {
        id: "antecedent",
        kind: "node",
        label: renderProposition(antecedent),
        position: { layer: 0, order: 0 },
        emphasis: "secondary",
        epistemic: status,
        sourceRef: refFor(theorem, antecedent.path)
      },
      {
        id: "consequent",
        kind: "node",
        label: renderProposition(consequent),
        position: { layer: 1, order: 0 },
        emphasis: "primary",
        epistemic: status,
        sourceRef: refFor(theorem, consequent.path)
      }
    ],
    relationships: [
      {
        id: "implies",
        kind: "implies",
        from: "antecedent",
        to: "consequent",
        label: bidirectional ? "\u2194" : "\u2192",
        epistemic: status,
        sourceRef: refFor(theorem, "conclusion")
      }
    ],
    axes: [],
    annotations: [
      { id: "rationale", kind: "rationale", text: classification.rationale, epistemic: status }
    ],
    epistemic: status,
    provenance: {
      sources: [refFor(theorem, "conclusion")],
      rule: classification.rule,
      inputs: [theorem.id]
    },
    rationale: classification.rationale
  };
}
function planFunctionalRelationship(theorem, classification) {
  if (classification.payload.kind !== "functional-relationship")
    return null;
  const { left, right } = classification.payload.data;
  const status = classification.claim.status;
  const inputs = Array.from(variablesIn(right)).map((id) => theorem.variables.find((v2) => v2.id === id)).filter((v2) => v2 !== void 0);
  if (inputs.length === 0)
    return null;
  const entities = inputs.map((variable, index) => ({
    id: `in:${variable.id}`,
    kind: "quantity",
    label: variable.symbol,
    detail: variable.annotation?.meaning ? `${variable.annotation.meaning}${variable.annotation.units ? ` (${variable.annotation.units})` : ""}` : variable.typeDisplay,
    position: { layer: 0, order: index },
    emphasis: "secondary",
    epistemic: status,
    sourceRef: refFor(theorem, `binders.${variable.symbol}`)
  }));
  entities.push({
    id: "defined",
    kind: "quantity",
    label: renderExpression(left),
    detail: renderExpression(right),
    position: { layer: 1, order: 0 },
    emphasis: "primary",
    epistemic: status,
    sourceRef: refFor(theorem, left.path)
  });
  return {
    id: `${theorem.id}:relationship`,
    type: "relationship-diagram",
    title: `${renderExpression(left)} = ${renderExpression(right)}`,
    subtitle: theorem.concept ?? theorem.name.split(".").pop(),
    entities,
    relationships: inputs.map((variable) => ({
      id: `feeds:${variable.id}`,
      kind: "maps-to",
      from: `in:${variable.id}`,
      to: "defined",
      epistemic: status,
      sourceRef: refFor(theorem, left.path)
    })),
    axes: [],
    annotations: [
      { id: "rationale", kind: "rationale", text: classification.rationale, epistemic: status },
      {
        id: "reading",
        kind: "legend",
        text: "Arrows show which quantities the value is computed from. They are not causal claims.",
        epistemic: "derived"
      }
    ],
    epistemic: status,
    provenance: {
      sources: [refFor(theorem, left.path)],
      rule: classification.rule,
      inputs: [theorem.id]
    },
    rationale: classification.rationale
  };
}
function planDependencies(theorem, graph) {
  const sub = subgraphFor(graph, theorem.id);
  if (sub.nodes.length <= 1)
    return null;
  const byLayer = /* @__PURE__ */ new Map();
  const entities = sub.nodes.slice().sort((a, b) => a.depth - b.depth || a.label.localeCompare(b.label)).map((n) => {
    const order = byLayer.get(n.depth) ?? 0;
    byLayer.set(n.depth, order + 1);
    return {
      id: n.id,
      kind: "node",
      label: n.label,
      detail: n.kind,
      position: { layer: n.depth, order },
      emphasis: n.id === theorem.id ? "primary" : "secondary",
      epistemic: "derived",
      sourceRef: { system: "lean4", declaration: n.id }
    };
  });
  return {
    id: `${theorem.id}:dependencies`,
    type: "dependency-graph",
    title: "What this proof rests on",
    subtitle: theorem.name.split(".").pop(),
    entities,
    relationships: sub.edges.map((e, i) => ({
      id: `dep:${i}`,
      kind: "depends-on",
      from: e.from,
      to: e.to,
      epistemic: "derived",
      sourceRef: { system: "lean4", declaration: e.from }
    })),
    axes: [],
    annotations: [
      {
        id: "rationale",
        kind: "rationale",
        text: "Edges are the declarations this proof term actually references.",
        epistemic: "derived"
      },
      {
        id: "external",
        kind: "legend",
        text: `${sub.externalDependencyCount} further dependencies lie outside the extracted modules and are not drawn.`,
        epistemic: "derived"
      }
    ],
    epistemic: "derived",
    provenance: {
      sources: [refFor(theorem)],
      inputs: [theorem.id]
    },
    rationale: "A dependency graph is always available, because it is read directly from the proof term rather than from any recognised statement shape."
  };
}
function planExpressionTree(theorem, classification) {
  const reason = classification && classification.payload.kind === "unsupported" ? classification.payload.data.reason : "Shown so the statement's structure stays inspectable.";
  const entities = [
    {
      id: "conclusion",
      kind: "conclusion",
      label: theorem.conclusionDisplay,
      position: { layer: 0, order: 0 },
      emphasis: "primary",
      epistemic: theorem.conclusion.status,
      sourceRef: refFor(theorem, "conclusion")
    },
    ...theorem.hypotheses.map((h, i) => ({
      id: `hyp:${h.id}`,
      kind: "hypothesis",
      label: `${h.symbol} : ${h.display}`,
      position: { layer: 1, order: i },
      emphasis: "secondary",
      state: h.usage.unusedInProof ? "unused" : "used",
      epistemic: theorem.conclusion.status,
      sourceRef: refFor(theorem, `binders.${h.symbol}`)
    }))
  ];
  return {
    id: `${theorem.id}:structure`,
    type: "expression-tree",
    title: "Formal structure",
    subtitle: theorem.name.split(".").pop(),
    entities,
    relationships: theorem.hypotheses.map((h) => ({
      id: `assume:${h.id}`,
      kind: "supports",
      from: `hyp:${h.id}`,
      to: "conclusion",
      epistemic: theorem.conclusion.status
    })),
    axes: [],
    annotations: [
      { id: "rationale", kind: "rationale", text: reason, epistemic: "derived" },
      {
        id: "graceful",
        kind: "legend",
        text: "ProofLens shows a theorem's formal structure even when it cannot interpret it. Nothing here is guessed.",
        epistemic: "derived"
      }
    ],
    epistemic: theorem.conclusion.status,
    provenance: { sources: [refFor(theorem)], inputs: [theorem.id] },
    rationale: reason
  };
}
function planVisuals(theorem, classifications, context = {}) {
  const specs = [];
  const sensitivity = classifications.find((c) => c.payload.kind === "assumption-sensitivity");
  const hasUnused = sensitivity?.payload.kind === "assumption-sensitivity" && sensitivity.payload.data.unusedInProof.length > 0;
  if (sensitivity && hasUnused) {
    const spec = planAssumptionSensitivity(theorem, sensitivity);
    if (spec)
      specs.push(spec);
  }
  for (const classification of classifications) {
    switch (classification.payload.kind) {
      case "limit": {
        const spec = planLimit(theorem, classification);
        if (spec)
          specs.push(spec);
        break;
      }
      case "positivity": {
        const spec = planPositivity(theorem, classification);
        if (spec)
          specs.push(spec);
        break;
      }
      // Every inequality yields both an upper- and a lower-bound reading. Only
      // the natural one is worth a figure; drawing both would show the reader
      // the same fact twice, once uselessly.
      case "upper-bound": {
        if (!classification.payload.data.natural)
          break;
        const spec = planBound(theorem, classification, "upper");
        if (spec)
          specs.push(spec);
        break;
      }
      case "lower-bound": {
        if (!classification.payload.data.natural)
          break;
        const spec = planBound(theorem, classification, "lower");
        if (spec)
          specs.push(spec);
        break;
      }
      case "monotonicity": {
        const spec = planMonotonicity(theorem, classification);
        if (spec)
          specs.push(spec);
        break;
      }
      case "functional-relationship": {
        const spec = planFunctionalRelationship(theorem, classification);
        if (spec)
          specs.push(spec);
        break;
      }
      case "implication":
      case "equivalence": {
        const spec = planImplication(theorem, classification);
        if (spec)
          specs.push(spec);
        break;
      }
      default:
        break;
    }
  }
  if (sensitivity && !hasUnused) {
    const spec = planAssumptionSensitivity(theorem, sensitivity);
    if (spec)
      specs.push(spec);
  }
  if (context.dependencies) {
    const spec = planDependencies(theorem, context.dependencies);
    if (spec)
      specs.push(spec);
  }
  const unsupported = classifications.find((c) => c.payload.kind === "unsupported");
  if (specs.length === 0 || unsupported) {
    specs.push(planExpressionTree(theorem, unsupported));
  }
  return applyAuthorHint(theorem, specs);
}
var VISUAL_HINT_ALIASES = {
  "functional-relationship": "relationship-diagram",
  "equation-map": "relationship-diagram",
  "monotone-curve": "monotonicity-plot",
  "antitone-curve": "monotonicity-plot",
  "monotonicity-curve": "monotonicity-plot",
  limit: "limit-plot",
  convergence: "limit-plot",
  asymptote: "limit-plot",
  "positivity-fact": "number-line",
  "sign-fact": "number-line",
  "iff-equivalence": "implication-graph",
  "implication-arrow": "implication-graph",
  "implication-chain": "implication-graph",
  "bound-plot": "upper-bound-plot",
  dependency: "dependency-graph",
  "proof-dependencies": "dependency-graph",
  structure: "expression-tree"
};
function resolveVisualHint(hint) {
  const aliased = VISUAL_HINT_ALIASES[hint];
  if (aliased)
    return aliased;
  const known = [
    "upper-bound-plot",
    "lower-bound-plot",
    "number-line",
    "monotonicity-plot",
    "limit-plot",
    "relationship-diagram",
    "dependency-graph",
    "implication-graph",
    "assumption-sensitivity",
    "expression-tree",
    "text-diagram"
  ];
  return known.includes(hint) ? hint : null;
}
function applyAuthorHint(theorem, specs) {
  const raw = theorem.suggestedVisual;
  if (!raw || specs.length === 0)
    return specs;
  const hint = resolveVisualHint(raw);
  const leadIsFinding = specs[0]?.type === "assumption-sensitivity" || theorem.trust.usesSorry;
  const target = leadIsFinding ? 1 : 0;
  const index = hint === null ? -1 : specs.findIndex((s) => s.type === hint);
  if (index > target) {
    const [preferred] = specs.splice(index, 1);
    if (preferred)
      specs.splice(target, 0, preferred);
    return specs;
  }
  if (index >= 0)
    return specs;
  const first = specs[0];
  if (first) {
    first.annotations = [
      ...first.annotations,
      {
        id: "hint-unmatched",
        kind: "legend",
        text: hint === null ? `The declaration requests a \`${raw}\` figure, which is not a visualization type ProofLens knows.` : `The declaration requests a \`${raw}\` figure, but no ProofLens classifier produced one for this statement.`,
        epistemic: "derived"
      }
    ];
  }
  return specs;
}

// packages/visual-ir/dist/semantic.js
var SEMANTIC_SCENE_RULE = {
  id: "SEMANTIC_NUMERIC_BOUND_001",
  description: "Compiled a supported bound into a numeric, parameterised scene using author-declared meanings and domains.",
  produces: "interpreted"
};
var SEMANTIC_SCENE_VERSION = "0.1.0";
var DOMAIN_RANGES = [
  {
    test: /^(strictly )?positive reals?$/i,
    range: { min: 0.1, max: 10, step: 0.1, initial: 1, integer: false }
  },
  {
    test: /^nonnegative reals?$/i,
    range: { min: 0, max: 10, step: 0.1, initial: 1, integer: false }
  },
  {
    test: /^reals?$/i,
    range: { min: -10, max: 10, step: 0.1, initial: 1, integer: false }
  },
  {
    test: /^(natural numbers?|naturals?|nonnegative integers?)$/i,
    range: { min: 0, max: 20, step: 1, initial: 1, integer: true }
  }
];
function sourceFor(theorem, path2 = "conclusion") {
  const base = theorem.provenance.sources[0];
  return base ? { ...base, path: path2 } : { system: "lean4", declaration: theorem.name, path: path2 };
}
function naturalBound(classifications) {
  return classifications.find((classification) => (classification.payload.kind === "upper-bound" || classification.payload.kind === "lower-bound") && classification.payload.data.natural);
}
function unsupportedHead(expr) {
  switch (expr.kind) {
    case "number":
    case "variable":
      return null;
    case "constant":
    case "opaque":
    case "lambda":
      return expr.kind;
    case "operator": {
      if (!["add", "sub", "mul", "div", "pow", "neg", "inv", "abs"].includes(expr.op)) {
        return `operator:${expr.op}`;
      }
      for (const arg of expr.args) {
        const unsupported = unsupportedHead(arg);
        if (unsupported)
          return unsupported;
      }
      return null;
    }
    case "application": {
      if (!["Real.log", "Real.exp", "Real.sqrt"].includes(expr.head))
        return expr.head;
      for (const arg of expr.args) {
        const unsupported = unsupportedHead(arg);
        if (unsupported)
          return unsupported;
      }
      return null;
    }
  }
}
function rangeFor(variable) {
  const domain = variable.annotation?.domain;
  if (!domain)
    return null;
  const match = DOMAIN_RANGES.find((candidate) => candidate.test.test(domain));
  return match ? { ...match.range } : null;
}
function boundedUnits(theorem, bounded) {
  if (bounded.kind !== "variable")
    return void 0;
  return theorem.variables.find((variable) => variable.id === bounded.id)?.annotation?.units;
}
function compileSemanticScene(theorem, classifications) {
  const classification = naturalBound(classifications);
  if (!classification) {
    return {
      status: "blocked",
      code: "NO_NATURAL_BOUND",
      reason: "This declaration has no natural upper or lower bound to turn into a numeric scene."
    };
  }
  const payload = classification.payload;
  if (payload.kind !== "upper-bound" && payload.kind !== "lower-bound") {
    throw new Error("naturalBound returned a non-bound classification");
  }
  const unsupported = unsupportedHead(payload.data.bound);
  if (unsupported) {
    return {
      status: "blocked",
      code: "UNSUPPORTED_EXPRESSION",
      reason: `The bound contains ${unsupported}, which the numeric evaluator does not support.`
    };
  }
  const variableIds = variablesIn(payload.data.bound);
  const variables = theorem.variables.filter((variable) => variableIds.has(variable.id));
  const missing = variables.filter((variable) => !variable.annotation?.meaning || !variable.annotation.domain);
  if (missing.length > 0) {
    return {
      status: "blocked",
      code: "MISSING_SEMANTICS",
      reason: `Numeric rendering needs meaning and domain annotations for: ${missing.map((variable) => variable.symbol).join(", ")}.`
    };
  }
  const unsupportedDomains = variables.filter((variable) => rangeFor(variable) === null);
  if (unsupportedDomains.length > 0) {
    return {
      status: "blocked",
      code: "UNSUPPORTED_DOMAIN",
      reason: `No safe slider range is defined for: ${unsupportedDomains.map((variable) => `${variable.symbol} (${variable.annotation.domain})`).join(", ")}.`
    };
  }
  const parameters = variables.map((variable) => ({
    id: variable.id,
    symbol: variable.symbol,
    label: variable.annotation.meaning,
    units: variable.annotation.units,
    domain: variable.annotation.domain,
    axis: variable.annotation.axis === "x" ? "x" : "parameter",
    role: variable.annotation.role,
    range: rangeFor(variable),
    epistemic: "interpreted"
  }));
  const xParameter = parameters.find((parameter) => parameter.axis === "x") ?? parameters[0];
  if (!xParameter) {
    return {
      status: "blocked",
      code: "MISSING_SEMANTICS",
      reason: "The bound has no free numeric parameter to place on an axis."
    };
  }
  const boundedLabel = renderExpression(payload.data.boundedQuantity);
  const boundLabel = renderExpression(payload.data.bound);
  const direction = payload.kind === "upper-bound" ? "upper" : "lower";
  return {
    status: "ready",
    scene: {
      version: SEMANTIC_SCENE_VERSION,
      id: `${theorem.id}:semantic-bound`,
      type: "numeric-bound",
      theoremName: theorem.name,
      title: theorem.concept ?? `${boundedLabel} ${direction === "upper" ? "ceiling" : "floor"}`,
      direction,
      strict: payload.data.strict,
      bounded: payload.data.boundedQuantity,
      bound: payload.data.bound,
      boundedLabel,
      boundLabel,
      targetLabel: `target ${boundedLabel}`,
      targetUnits: boundedUnits(theorem, payload.data.boundedQuantity),
      parameters,
      xParameterId: xParameter.id,
      sensitivity: payload.data.sensitivity,
      constraintStatus: classification.claim.status,
      epistemic: "illustrative",
      provenance: {
        sources: [sourceFor(theorem, payload.data.bound.path)],
        rule: SEMANTIC_SCENE_RULE,
        inputs: [theorem.id, classification.rule.id],
        note: "The inequality comes from Lean. Meanings and domains come from author annotations. Slider defaults and plot ranges are illustrative."
      },
      caveat: "Lean verifies the inequality, not the author-supplied physical meanings or the illustrative parameter values."
    }
  };
}

// packages/pipeline/src/index.ts
var PROOFLENS_VERSION = "0.1.0";
function runPipeline(formal) {
  const math = lowerDocument(formal);
  const graph = dependencyGraph(formal);
  const byName = new Map(formal.declarations.map((d) => [d.name, d]));
  const analyses = math.theorems.map((theorem) => {
    const declaration = byName.get(theorem.name);
    const classifications = classifyTheorem(theorem);
    const explanations = explain(theorem, classifications, {
      formalDocument: formal,
      formalDeclaration: declaration
    });
    const visuals = planVisuals(theorem, classifications, { dependencies: graph.value });
    const semanticScene = compileSemanticScene(theorem, classifications);
    return {
      formal: declaration,
      math: theorem,
      classifications,
      primary: primaryClassification(classifications),
      explanations,
      visuals,
      semanticScene,
      unsupported: classifications.some((c) => c.payload.kind === "unsupported")
    };
  });
  return {
    prooflensVersion: PROOFLENS_VERSION,
    generatedFrom: {
      system: formal.system,
      toolchain: formal.toolchain,
      modules: formal.modules,
      notationFidelity: formal.notationFidelity
    },
    formal,
    math,
    dependencies: graph.value,
    analyses,
    summary: summarise(analyses)
  };
}
function runPipelineOnValue(value) {
  return runPipeline(parseFormalIR(value));
}
function summarise(analyses) {
  const histogram = {};
  let visuals = 0;
  for (const a of analyses) {
    for (const v2 of a.visuals) {
      visuals += 1;
      histogram[v2.epistemic] = (histogram[v2.epistemic] ?? 0) + 1;
    }
  }
  return {
    declarations: analyses.length,
    classified: analyses.filter((a) => !a.unsupported).length,
    unsupported: analyses.filter((a) => a.unsupported).length,
    withUnusedHypotheses: analyses.filter(
      (a) => a.classifications.some(
        (c) => c.payload.kind === "assumption-sensitivity" && c.payload.data.unusedInProof.length > 0
      )
    ).length,
    withSorry: analyses.filter((a) => a.math.trust.usesSorry).length,
    withUnusualAxioms: analyses.filter((a) => a.math.trust.unusualAxioms.length > 0).length,
    visualsPlanned: visuals,
    epistemicHistogram: histogram
  };
}
function findAnalysis(bundle, name) {
  return bundle.analyses.find((a) => a.math.name === name) ?? bundle.analyses.find((a) => a.math.name.split(".").pop() === name);
}

// packages/renderer-svg/src/xml.ts
function escapeXml(value) {
  let out = "";
  for (const ch of value) {
    switch (ch) {
      case "&":
        out += "&amp;";
        break;
      case "<":
        out += "&lt;";
        break;
      case ">":
        out += "&gt;";
        break;
      case '"':
        out += "&quot;";
        break;
      case "'":
        out += "&apos;";
        break;
      default: {
        const code = ch.codePointAt(0) ?? 0;
        const illegal = code < 32 && code !== 9 && code !== 10 && code !== 13;
        out += illegal ? " " : ch;
        break;
      }
    }
  }
  return out;
}
function sanitizeId(raw) {
  let out = "";
  for (const ch of raw) {
    out += /[A-Za-z0-9_-]/.test(ch) ? ch : "-";
  }
  out = out.replace(/-{2,}/g, "-").replace(/^-+|-+$/g, "");
  if (out === "" || /^[0-9-]/.test(out)) out = `i${out}`;
  return out;
}
function num(n) {
  if (!Number.isFinite(n)) return "0";
  const rounded = Math.round(n * 100) / 100;
  if (Object.is(rounded, -0)) return "0";
  return String(rounded);
}
function attrs(map) {
  let out = "";
  for (const key of Object.keys(map)) {
    const value = map[key];
    if (value === void 0 || value === null || value === false) continue;
    const text2 = typeof value === "number" ? num(value) : value;
    out += ` ${key}="${escapeXml(text2)}"`;
  }
  return out;
}
function el(name, attrMap, children = "") {
  const head = `<${name}${attrs(attrMap)}`;
  return children === "" ? `${head}/>` : `${head}>${children}</${name}>`;
}
function titleEl(text2) {
  return `<title>${escapeXml(text2)}</title>`;
}
function clamp01(n, fallback = 0.5) {
  if (n === void 0 || !Number.isFinite(n)) return fallback;
  return n < 0 ? 0 : n > 1 ? 1 : n;
}

// packages/renderer-svg/src/animate.ts
var STAGE_STEP = 0.55;
var ENTER_DURATION = 0.45;
var TRACE_DURATION = 2;
var CHROME_FADE_DURATION = 0.35;
function stageDelay(stage) {
  return stage * STAGE_STEP;
}
function afterStage(stage) {
  return stageDelay(stage) + ENTER_DURATION;
}
var ANIMATION_LEGEND_ROW = {
  swatch: "none",
  text: "Order of appearance follows the proof's dependency structure. The pacing is a display choice."
};
function buildAnimationStylesheet(ctx) {
  const targets = ctx.animTargets;
  if (targets.length === 0) return "";
  let css = "";
  if (targets.some((t) => t.kind === "enter")) {
    css += `@keyframes ${ctx.id("enter")}{from{opacity:0;transform:translateY(6px)}}`;
  }
  if (targets.some((t) => t.kind === "fade")) {
    css += `@keyframes ${ctx.id("fade")}{from{opacity:0}}`;
  }
  const drawLengths = [];
  for (const target of targets) {
    if (target.kind === "draw" && !drawLengths.includes(target.length)) {
      drawLengths.push(target.length);
    }
  }
  for (const length of drawLengths) {
    css += `@keyframes ${ctx.id(`draw-${length}`)}{from{stroke-dashoffset:${num(length)}}}`;
  }
  if (targets.some((t) => t.kind === "draw" && t.revealMarker !== void 0)) {
    css += `@keyframes ${ctx.id("marker")}{from{marker-end:none}}`;
  }
  for (const target of targets) {
    const name = target.kind === "enter" ? ctx.id("enter") : target.kind === "fade" ? ctx.id("fade") : ctx.id(`draw-${target.length}`);
    const delay = `${num(target.delay)}s`;
    let declarations = "";
    let animations = `${name} ${num(target.duration)}s ease-out both`;
    let delays = delay;
    if (target.kind === "draw") {
      declarations += `stroke-dasharray:${num(target.length)};`;
      if (target.revealMarker !== void 0) {
        animations += `,${ctx.id("marker")} ${num(target.duration)}s step-end both`;
        delays += `,${delay}`;
      }
    }
    css += `.${target.className}{${declarations}animation:${animations};animation-delay:${delays}}`;
  }
  css += `@media (prefers-reduced-motion:reduce){[class*="pl-anim-"]{animation:none !important}}`;
  return css;
}

// packages/renderer-svg/src/measure.ts
var NARROW = new Set(" .,:;!|'`ijlt()[]{}/\\-\xAC\u2032".split(""));
var WIDE = new Set("mwMW@%\u27F9\u27FA\u27F6\u2192\u2194\u2264\u2265\u2200\u2203\u222B\u2211\u220F".split(""));
function charRatio(ch) {
  if (NARROW.has(ch)) return 0.34;
  if (WIDE.has(ch)) return 0.95;
  if (ch >= "A" && ch <= "Z") return 0.69;
  if (ch >= "0" && ch <= "9") return 0.57;
  if (ch >= "a" && ch <= "z") return 0.55;
  const code = ch.codePointAt(0) ?? 0;
  if (code >= 4352 && code <= 8191) return 1;
  if (code >= 11904 && code <= 42191) return 1;
  if (code >= 65280 && code <= 65376) return 1;
  if (code > 8192) return 0.7;
  return 0.58;
}
function measureText(text2, fontSize) {
  let ratio = 0;
  for (const ch of text2) ratio += charRatio(ch);
  return ratio * fontSize;
}
function truncateToWidth(text2, maxWidth, fontSize) {
  if (maxWidth <= 0) return { text: "", truncated: text2.length > 0 };
  if (measureText(text2, fontSize) <= maxWidth) return { text: text2, truncated: false };
  const chars = Array.from(text2);
  const ellipsisWidth = measureText("\u2026", fontSize);
  const budget = maxWidth - ellipsisWidth;
  if (budget <= 0) return { text: "\u2026", truncated: true };
  let used = 0;
  let kept = 0;
  for (const ch of chars) {
    const w = charRatio(ch) * fontSize;
    if (used + w > budget) break;
    used += w;
    kept += 1;
  }
  const head = chars.slice(0, kept).join("").replace(/\s+$/, "");
  return { text: `${head}\u2026`, truncated: true };
}
function wrapToWidth(text2, maxWidth, fontSize, maxLines) {
  if (maxLines <= 0 || maxWidth <= 0) return { lines: [], truncated: text2.length > 0 };
  const words = text2.split(/\s+/).filter((w) => w.length > 0);
  if (words.length === 0) return { lines: [], truncated: false };
  const lines = [];
  let current = "";
  let truncated = false;
  const pushCurrent = () => {
    if (current !== "") lines.push(current);
    current = "";
  };
  for (let i = 0; i < words.length; i += 1) {
    const word = words[i];
    const candidate = current === "" ? word : `${current} ${word}`;
    if (measureText(candidate, fontSize) <= maxWidth) {
      current = candidate;
      continue;
    }
    if (current !== "") {
      pushCurrent();
      if (lines.length >= maxLines) {
        truncated = true;
        break;
      }
    }
    let rest = word;
    while (measureText(rest, fontSize) > maxWidth) {
      const chunk = truncateHard(rest, maxWidth, fontSize);
      if (chunk === "") break;
      lines.push(chunk);
      rest = rest.slice(chunk.length);
      if (lines.length >= maxLines) {
        truncated = true;
        break;
      }
    }
    if (lines.length >= maxLines) {
      if (rest.length > 0) truncated = true;
      break;
    }
    current = rest;
  }
  if (lines.length < maxLines) pushCurrent();
  else if (current !== "") truncated = true;
  if (lines.length > maxLines) {
    truncated = true;
    lines.length = maxLines;
  }
  if (truncated && lines.length > 0) {
    const last = lines[lines.length - 1];
    lines[lines.length - 1] = truncateToWidth(`${last}\u2026`, maxWidth, fontSize).text;
  }
  return { lines, truncated };
}
function truncateHard(text2, maxWidth, fontSize) {
  let used = 0;
  let kept = 0;
  for (const ch of text2) {
    const w = charRatio(ch) * fontSize;
    if (used + w > maxWidth) break;
    used += w;
    kept += 1;
  }
  return text2.slice(0, Math.max(1, kept));
}
function clampCenter(x, textWidth, min, max) {
  const half = textWidth / 2;
  if (max - min < textWidth) return (min + max) / 2;
  if (x - half < min) return min + half;
  if (x + half > max) return max - half;
  return x;
}

// packages/renderer-svg/src/context.ts
function createContext(spec, width, pad, prefix, animate = false) {
  const base = sanitizeId(`${prefix}-${spec.id}`);
  const targets = [];
  const byShape = /* @__PURE__ */ new Map();
  return {
    spec,
    width,
    pad,
    animate,
    animTargets: targets,
    id: (suffix) => `${base}-${sanitizeId(suffix)}`,
    anim(options) {
      if (!animate) return "";
      const duration = options.duration ?? 0.45;
      const length = Math.max(1, Math.ceil(options.length ?? 0));
      const shape = [
        options.kind,
        options.delay,
        duration,
        options.kind === "draw" ? length : "",
        options.revealMarker ?? ""
      ].join("|");
      let className = byShape.get(shape);
      if (className === void 0) {
        className = `pl-anim-${base}-a${targets.length}`;
        byShape.set(shape, className);
        targets.push({
          className,
          kind: options.kind,
          delay: options.delay,
          duration,
          length,
          revealMarker: options.revealMarker
        });
      }
      return ` ${className}`;
    },
    resetAnim() {
      targets.length = 0;
      byShape.clear();
    }
  };
}
function isWeak(status) {
  return !isAtLeast(status, "derived");
}
function weakStrokeClass(status) {
  return isWeak(status) ? " pl-weak-stroke" : "";
}
function weakFillClass(status) {
  return isWeak(status) ? " pl-weak-fill" : "";
}
function specHasWeakElement(spec) {
  return isWeak(spec.epistemic) || spec.entities.some((e) => isWeak(e.epistemic)) || spec.relationships.some((r) => isWeak(r.epistemic)) || spec.axes.some((a) => isWeak(a.epistemic));
}
function statusPhrase(status) {
  return `epistemic status: ${status}`;
}
function text(content, options) {
  const { text: shown, truncated } = options.maxWidth === void 0 ? { text: content, truncated: false } : truncateToWidth(content, options.maxWidth, options.fontSize);
  const tip = options.title ?? content;
  const needsTitle = truncated || options.title !== void 0;
  const body = (needsTitle ? titleEl(tip) : "") + escapeXml(shown);
  return `<text${attrs({
    x: options.x,
    y: options.y,
    class: options.className,
    "text-anchor": options.anchor === "start" ? void 0 : options.anchor
  })}>${body}</text>`;
}
function paragraph(content, options) {
  const { lines, truncated } = wrapToWidth(
    content,
    options.maxWidth,
    options.fontSize,
    options.maxLines
  );
  if (lines.length === 0) return { svg: "", height: 0, lines: 0 };
  let svg = "";
  lines.forEach((line2, i) => {
    const isFirst = i === 0;
    const tip = truncated && isFirst ? content : void 0;
    svg += `<text${attrs({
      x: options.x,
      y: options.y + i * options.lineHeight,
      class: options.className,
      "text-anchor": options.anchor === "start" ? void 0 : options.anchor
    })}>${(tip ? titleEl(tip) : "") + escapeXml(line2)}</text>`;
  });
  return { svg, height: (lines.length - 1) * options.lineHeight, lines: lines.length };
}
function box(options) {
  return el(
    "rect",
    {
      x: options.x,
      y: options.y,
      width: Math.max(0, options.width),
      height: Math.max(0, options.height),
      rx: options.radius ?? 5,
      ry: options.radius ?? 5,
      class: options.className
    },
    options.tooltip ? titleEl(options.tooltip) : ""
  );
}
function line(x1, y1, x2, y2, className, tooltip) {
  return el("line", { x1, y1, x2, y2, class: className }, tooltip ? titleEl(tooltip) : "");
}
function circle(cx, cy, r, className, tooltip) {
  return el("circle", { cx, cy, r, class: className }, tooltip ? titleEl(tooltip) : "");
}
function path(d, className, extra = {}, tooltip) {
  return el("path", { d, class: className, ...extra }, tooltip ? titleEl(tooltip) : "");
}
function connector(x1, y1, x2, y2) {
  const dir = x2 >= x1 ? 1 : -1;
  const dx = dir * Math.max(24, Math.abs(x2 - x1) * 0.45);
  return `M ${num(x1)} ${num(y1)} C ${num(x1 + dx)} ${num(y1)} ${num(x2 - dx)} ${num(y2)} ${num(x2)} ${num(y2)}`;
}
function connectorVertical(x1, y1, x2, y2) {
  const dir = y2 >= y1 ? 1 : -1;
  const dy = dir * Math.max(18, Math.abs(y2 - y1) * 0.45);
  return `M ${num(x1)} ${num(y1)} C ${num(x1)} ${num(y1 + dy)} ${num(x2)} ${num(y2 - dy)} ${num(x2)} ${num(y2)}`;
}
function entityTooltip(entity) {
  const bits = [entity.label];
  if (entity.detail) bits.push(entity.detail);
  if (entity.state && entity.state !== "neutral") bits.push(`state: ${entity.state}`);
  bits.push(statusPhrase(entity.epistemic));
  return bits.join(" \u2014 ");
}
function entitiesOfKind(spec, kind) {
  return spec.entities.map((entity, index) => ({ entity, index })).filter((e) => e.entity.kind === kind).sort((a, b) => orderOf(a.entity, a.index) - orderOf(b.entity, b.index)).map((e) => e.entity);
}
function orderOf(entity, fallback) {
  const order = entity.position?.order;
  return order === void 0 ? fallback : order;
}

// packages/renderer-svg/src/chrome.ts
function headerHeight(spec) {
  return spec.subtitle ? 62 : 46;
}
function renderHeader(spec, ctx) {
  const inner = ctx.width - ctx.pad * 2;
  let svg = text(spec.title, {
    x: ctx.pad,
    y: 26,
    className: "pl-title",
    fontSize: 17,
    maxWidth: inner
  });
  let y = 42;
  if (spec.subtitle) {
    svg += text(spec.subtitle.toUpperCase(), {
      x: ctx.pad,
      y,
      className: "pl-subtitle",
      fontSize: 11,
      maxWidth: inner,
      title: spec.subtitle
    });
    y += 16;
  }
  svg += text(`${spec.type} \xB7 ${spec.epistemic}`.toUpperCase(), {
    x: ctx.pad,
    y,
    className: "pl-status",
    fontSize: 10.5,
    maxWidth: inner,
    title: `${spec.type}: ${EPISTEMIC_GLOSS[spec.epistemic]}`
  });
  svg += line(ctx.pad, y + 8, ctx.width - ctx.pad, y + 8, "pl-rule");
  return svg;
}
function swatch(kind, x, cy, arrowId) {
  switch (kind) {
    case "none":
      return "";
    case "solid-line":
      return line(x, cy, x + 22, cy, "pl-axis");
    case "dashed-line":
      return line(x, cy, x + 22, cy, "pl-axis pl-schematic");
    case "permit":
      return box({ x, y: cy - 6, width: 22, height: 12, className: "pl-region-permit", radius: 2 });
    case "exclude":
      return box({
        x,
        y: cy - 6,
        width: 22,
        height: 12,
        className: "pl-region-exclude",
        radius: 2
      });
    case "open-dot":
      return line(x, cy, x + 22, cy, "pl-axis") + circle(x + 11, cy, 5, "pl-dot-open");
    case "filled-dot":
      return line(x, cy, x + 22, cy, "pl-axis") + circle(x + 11, cy, 5, "pl-dot");
    case "used-box":
      return box({ x, y: cy - 6, width: 22, height: 12, className: "pl-box-primary", radius: 3 });
    case "unused-box":
      return box({ x, y: cy - 6, width: 22, height: 12, className: "pl-box-unused", radius: 3 });
    case "arrow":
      return path(`M ${num(x)} ${num(cy)} L ${num(x + 16)} ${num(cy)}`, "pl-edge", {
        "marker-end": `url(#${arrowId})`
      });
    case "curve":
      return path(
        `M ${num(x)} ${num(cy + 5)} C ${num(x + 8)} ${num(cy + 5)} ${num(x + 14)} ${num(cy - 5)} ${num(x + 22)} ${num(cy - 5)}`,
        "pl-curve"
      );
    case "asymptote":
      return line(x, cy, x + 22, cy, "pl-asymptote");
    default:
      return "";
  }
}
function renderLegend(rows, ctx, arrowId) {
  if (rows.length === 0) return { svg: "", height: 0 };
  const textX = ctx.pad + 30;
  const maxWidth = ctx.width - ctx.pad - textX;
  let y = 0;
  let svg = line(ctx.pad, y, ctx.width - ctx.pad, y, "pl-rule");
  y += 16;
  svg += text("HOW TO READ THIS FIGURE", {
    x: ctx.pad,
    y,
    className: "pl-heading",
    fontSize: 10,
    maxWidth
  });
  y += 14;
  for (const row of rows) {
    const para = paragraph(row.text, {
      x: textX,
      y: y + 9,
      className: "pl-note",
      maxWidth,
      fontSize: 10.5,
      lineHeight: 13,
      maxLines: 3
    });
    const rowHeight = Math.max(18, para.height + 16);
    svg += swatch(row.swatch, ctx.pad, y + 5, arrowId);
    svg += para.svg;
    y += rowHeight;
  }
  return { svg, height: y + 4 };
}
var ANNOTATION_RANK = {
  rationale: 0,
  warning: 1,
  callout: 2,
  caption: 3,
  legend: 4
};
function renderAnnotations(spec, ctx) {
  const inner = ctx.width - ctx.pad * 2;
  const ordered = spec.annotations.map((annotation, index) => ({ annotation, index })).sort(
    (a, b) => ANNOTATION_RANK[a.annotation.kind] - ANNOTATION_RANK[b.annotation.kind] || a.index - b.index
  ).map((a) => a.annotation);
  const rationaleTexts = /* @__PURE__ */ new Set();
  const items = [];
  items.push({ kind: "rationale", text: spec.rationale, status: spec.epistemic });
  rationaleTexts.add(spec.rationale.trim());
  for (const annotation of ordered) {
    if (annotation.kind === "rationale" && rationaleTexts.has(annotation.text.trim())) continue;
    if (annotation.kind === "rationale") rationaleTexts.add(annotation.text.trim());
    items.push({ kind: annotation.kind, text: annotation.text, status: annotation.epistemic });
  }
  let y = 0;
  let svg = line(ctx.pad, y, ctx.width - ctx.pad, y, "pl-rule");
  y += 14;
  for (const item of items) {
    if (item.kind === "rationale") {
      const para2 = paragraph(item.text, {
        x: ctx.pad + 12,
        y: y + 11,
        className: "pl-rationale",
        maxWidth: inner - 12,
        fontSize: 12,
        lineHeight: 16,
        maxLines: 5
      });
      const h = para2.height + 16;
      svg += box({
        x: ctx.pad,
        y,
        width: 3,
        height: h,
        className: "pl-rationale-bar",
        radius: 1.5,
        tooltip: `Why this figure was chosen \u2014 ${item.status}: ${EPISTEMIC_GLOSS[item.status]}`
      });
      svg += para2.svg;
      y += h + 10;
      continue;
    }
    if (item.kind === "warning") {
      const para2 = paragraph(`\u26A0  ${item.text}`, {
        x: ctx.pad + 10,
        y: y + 18,
        className: "pl-warn-text",
        maxWidth: inner - 20,
        fontSize: 11.5,
        lineHeight: 15,
        maxLines: 5
      });
      const h = para2.height + 26;
      svg += box({
        x: ctx.pad,
        y,
        width: inner,
        height: h,
        className: "pl-warn-box",
        radius: 4,
        tooltip: `Warning \u2014 ${item.text}`
      });
      svg += para2.svg;
      y += h + 10;
      continue;
    }
    const label = item.kind === "callout" ? "\u2022" : "\u2014";
    const para = paragraph(`${label}  ${item.text}`, {
      x: ctx.pad,
      y: y + 10,
      className: "pl-note",
      maxWidth: inner,
      fontSize: 10.5,
      lineHeight: 13.5,
      maxLines: 4
    });
    svg += para.svg;
    y += para.height + 18;
  }
  return { svg, height: y };
}

// packages/renderer-svg/src/layouts.ts
function arrowDefs(ctx) {
  return el(
    "defs",
    {},
    marker(ctx.id("arrow"), "var(--pl-accent)") + marker(ctx.id("arrow-muted"), "var(--pl-edge)")
  );
}
function marker(id, fill) {
  return el(
    "marker",
    {
      id,
      viewBox: "0 0 10 10",
      refX: 9.5,
      refY: 5,
      markerWidth: 6,
      markerHeight: 6,
      orient: "auto",
      markerUnits: "strokeWidth"
    },
    el("path", { d: "M 0 0 L 10 5 L 0 10 z", fill })
  );
}
var WEAK_LEGEND_ROW = {
  swatch: "dashed-line",
  text: "Dashed strokes and lightened fills mark elements weaker than \u201Cderived\u201D \u2014 illustrative choices made for legibility. They carry no mathematical claim."
};
var NL_BOUND_LABEL_Y = 16;
var NL_QTY_LABEL_Y = 34;
var NL_MARKER_TOP = 42;
var NL_BAND_TOP = 50;
var NL_AXIS_Y = 74;
var NL_BAND_BOTTOM = 98;
var NL_MARKER_BOTTOM = 106;
var NL_REGION_LABEL_Y = 92;
var NL_TICK_LABEL_Y = 118;
var NL_AXIS_TITLE_Y = 136;
var NL_HEIGHT = 148;
function layoutNumberLine(spec, ctx) {
  const legend = [];
  const fadeAt = (stage) => ctx.anim({ kind: "fade", delay: stageDelay(stage) });
  const x0 = ctx.pad + 12;
  const x1 = ctx.width - ctx.pad - 12;
  const span = x1 - x0;
  const X = (p, fallback = 0.5) => x0 + clamp01(p, fallback) * span;
  const axis = spec.axes[0];
  const schematic = axis === void 0 || axis.scale === "schematic";
  const bound = entitiesOfKind(spec, "bound")[0];
  const boundX = clamp01(bound?.position?.x, 0.5);
  let svg = "";
  const hatchId = ctx.id("hatch");
  svg += el(
    "defs",
    {},
    el(
      "pattern",
      {
        id: hatchId,
        width: 8,
        height: 8,
        patternUnits: "userSpaceOnUse",
        patternTransform: "rotate(45)"
      },
      el("line", {
        x1: 0,
        y1: 0,
        x2: 0,
        y2: 8,
        stroke: "var(--pl-exclude)",
        "stroke-width": 1.25,
        opacity: 0.42
      })
    )
  );
  for (const region of entitiesOfKind(spec, "region")) {
    const at = clamp01(region.position?.x, 0.25);
    const left = at <= boundX ? x0 : X(boundX);
    const right = at <= boundX ? X(boundX) : x1;
    const excluded = region.state === "excluded";
    const cls = excluded ? "pl-region-exclude" : "pl-region-permit";
    const regionAnim = fadeAt(excluded ? 3 : 2);
    svg += box({
      x: left,
      y: NL_BAND_TOP,
      width: right - left,
      height: NL_BAND_BOTTOM - NL_BAND_TOP,
      className: `${cls}${weakStrokeClass(region.epistemic)}${weakFillClass(region.epistemic)}${regionAnim}`,
      radius: 3,
      tooltip: entityTooltip(region)
    });
    if (excluded) {
      svg += el(
        "rect",
        {
          x: left,
          y: NL_BAND_TOP,
          width: right - left,
          height: NL_BAND_BOTTOM - NL_BAND_TOP,
          rx: 3,
          ry: 3,
          fill: `url(#${hatchId})`,
          stroke: "none",
          class: regionAnim === "" ? void 0 : regionAnim.trimStart()
        },
        ""
      );
    }
    const labelWidth = right - left - 14;
    svg += text(region.label, {
      x: (left + right) / 2,
      y: NL_REGION_LABEL_Y,
      className: `${excluded ? "pl-exclude-text" : "pl-permit-text"}${regionAnim}`,
      anchor: "middle",
      fontSize: 10.5,
      maxWidth: Math.max(20, labelWidth),
      title: entityTooltip(region)
    });
  }
  const axisStatus = axis?.epistemic ?? "illustrative";
  const axisClass = `pl-axis${schematic ? " pl-schematic" : ""}${weakStrokeClass(axisStatus)}${fadeAt(0)}`;
  svg += line(
    x0 - 8,
    NL_AXIS_Y,
    x1 + 8,
    NL_AXIS_Y,
    axisClass,
    axis ? `${axis.label} \u2014 ${axis.scale} axis, ${statusPhrase(axisStatus)}` : `schematic axis, ${statusPhrase(axisStatus)}`
  );
  for (const tick of axis?.ticks ?? []) {
    const tx = X(tick.at);
    svg += line(tx, NL_BAND_BOTTOM, tx, NL_BAND_BOTTOM + 6, `pl-axis${fadeAt(0)}`, tick.label);
    const width = measureText(tick.label, 10);
    svg += text(tick.label, {
      x: clampCenter(tx, width, ctx.pad, ctx.width - ctx.pad),
      y: NL_TICK_LABEL_Y,
      className: `pl-tick${fadeAt(0)}`,
      anchor: "middle",
      fontSize: 10,
      maxWidth: ctx.width - ctx.pad * 2
    });
  }
  if (axis) {
    const units = axis.units ? ` (${axis.units})` : "";
    const suffix = schematic ? " \xB7 schematic scale" : "";
    svg += text(`${axis.label}${units}${suffix}`, {
      x: x0 - 8,
      y: NL_AXIS_TITLE_Y,
      className: `pl-axis-title${fadeAt(0)}`,
      fontSize: 10.5,
      maxWidth: ctx.width - ctx.pad * 2,
      title: `${axis.label}${units} \u2014 ${axis.scale} axis`
    });
  }
  for (const quantity of entitiesOfKind(spec, "quantity")) {
    const qx = X(quantity.position?.x);
    svg += circle(
      qx,
      NL_AXIS_Y,
      5,
      `pl-dot${weakFillClass(quantity.epistemic)}${fadeAt(4)}`,
      entityTooltip(quantity)
    );
    const label = quantity.detail ? `${quantity.label}  \xB7  ${quantity.detail}` : quantity.label;
    const width = measureText(label, 12);
    svg += text(label, {
      x: clampCenter(qx, width, ctx.pad, ctx.width - ctx.pad),
      y: NL_QTY_LABEL_Y,
      className: `pl-label pl-mono${isWeak(quantity.epistemic) ? " pl-weak-text" : ""}${fadeAt(4)}`,
      anchor: "middle",
      fontSize: 12,
      maxWidth: ctx.width - ctx.pad * 2,
      title: entityTooltip(quantity)
    });
  }
  let strict = false;
  if (bound) {
    strict = bound.state === "excluded";
    const bx = X(bound.position?.x);
    const markerAnim = isWeak(bound.epistemic) ? fadeAt(1) : ctx.anim({
      kind: "draw",
      delay: stageDelay(1),
      length: NL_MARKER_BOTTOM - NL_MARKER_TOP
    });
    svg += line(
      bx,
      NL_MARKER_TOP,
      bx,
      NL_MARKER_BOTTOM,
      // Strictness is carried by the open circle alone. A dash here would
      // collide with the epistemic encoding, where dashed means "illustrative".
      `pl-marker${weakStrokeClass(bound.epistemic)}${markerAnim}`,
      entityTooltip(bound)
    );
    svg += circle(
      bx,
      NL_AXIS_Y,
      5.5,
      `${strict ? "pl-dot-open" : "pl-dot"}${fadeAt(1)}`,
      `${entityTooltip(bound)} \u2014 ${strict ? "open circle: the bound is excluded (strict)" : "filled circle: the bound is included (non-strict)"}`
    );
    const width = measureText(bound.label, 12.5);
    svg += text(bound.label, {
      x: clampCenter(bx, width, ctx.pad, ctx.width - ctx.pad),
      y: NL_BOUND_LABEL_Y,
      className: `pl-label-strong pl-mono${fadeAt(1)}`,
      anchor: "middle",
      fontSize: 12.5,
      maxWidth: ctx.width - ctx.pad * 2,
      title: entityTooltip(bound)
    });
  }
  legend.push({
    swatch: "permit",
    text: "Solid band: the range of values the theorem permits."
  });
  legend.push({
    swatch: "exclude",
    text: "Hatched band: values the theorem rules out."
  });
  if (bound) {
    legend.push(
      strict ? {
        swatch: "open-dot",
        text: "Open circle: the bound is a strict inequality, so the bound value itself is excluded."
      } : {
        swatch: "filled-dot",
        text: "Filled circle: the inequality is non-strict, so the bound value itself is permitted."
      }
    );
  }
  if (schematic) {
    legend.push({
      swatch: "dashed-line",
      text: "Broken axis: the scale is schematic. Which side of the marker a quantity lies on is meaningful; the distance is not."
    });
  }
  return { svg, height: NL_HEIGHT, legend };
}
function layoutMonotonicity(spec, ctx) {
  const legend = [];
  const axesAnim = ctx.anim({ kind: "fade", delay: stageDelay(0) });
  const afterTrace = () => ctx.anim({ kind: "fade", delay: stageDelay(1) + TRACE_DURATION });
  const px0 = ctx.pad + 30;
  const py0 = 14;
  const py1 = 200;
  const plotWidth = Math.min(320, Math.max(180, (ctx.width - ctx.pad * 2) * 0.48));
  const px1 = px0 + plotWidth;
  const fn = spec.entities.find((e) => e.kind === "function");
  const detail = (fn?.detail ?? "").toLowerCase();
  const decreasing = detail.includes("decreasing");
  const status = fn?.epistemic ?? spec.epistemic;
  const xAxis = spec.axes.find((a) => a.orientation === "horizontal");
  const yAxis = spec.axes.find((a) => a.orientation === "vertical");
  const schematic = (xAxis?.scale ?? "schematic") === "schematic";
  const axisClass = `pl-axis${schematic ? " pl-schematic" : ""}${axesAnim}`;
  let svg = "";
  svg += line(
    px0,
    py0 - 6,
    px0,
    py1,
    axisClass,
    yAxis ? `${yAxis.label} (${yAxis.scale})` : "output"
  );
  svg += line(
    px0,
    py1,
    px1 + 10,
    py1,
    axisClass,
    xAxis ? `${xAxis.label} (${xAxis.scale})` : "input"
  );
  const ax = px0 + 12;
  const bx = px1 - 12;
  const ay = decreasing ? py0 + 16 : py1 - 16;
  const by = decreasing ? py1 - 16 : py0 + 16;
  const c1x = ax + (bx - ax) * 0.45;
  const c2x = ax + (bx - ax) * 0.55;
  const curveAnim = isWeak(status) ? ctx.anim({ kind: "fade", delay: stageDelay(1), duration: TRACE_DURATION }) : ctx.anim({
    kind: "draw",
    delay: stageDelay(1),
    duration: TRACE_DURATION,
    length: cubicLength(ax, ay, c1x, ay, c2x, by, bx, by)
  });
  const d = `M ${num(ax)} ${num(ay)} C ${num(c1x)} ${num(ay)} ${num(c2x)} ${num(by)} ${num(bx)} ${num(by)}`;
  svg += path(
    d,
    `pl-curve${weakStrokeClass(status)}${curveAnim}`,
    {},
    `${fn?.label ?? "f"} \u2014 ${fn?.detail ?? "monotone"} \u2014 ${statusPhrase(status)}`
  );
  const samples = spec.entities.filter((e) => e.kind === "label" && e.position?.x !== void 0);
  for (const sample of samples) {
    const t = solveCubicT(ax, c1x, c2x, bx, ax + clamp01(sample.position?.x) * (bx - ax));
    const sx = cubic(ax, c1x, c2x, bx, t);
    const sy = cubic(ay, ay, by, by, t);
    svg += line(sx, py1, sx, sy, `pl-guide${afterTrace()}`);
    svg += line(px0, sy, sx, sy, `pl-guide${afterTrace()}`);
    svg += circle(sx, sy, 3.5, `pl-dot${afterTrace()}`, entityTooltip(sample));
    svg += text(sample.label, {
      x: sx,
      y: py1 + 15,
      className: `pl-label pl-mono${afterTrace()}`,
      anchor: "middle",
      fontSize: 11.5,
      maxWidth: 60,
      title: entityTooltip(sample)
    });
  }
  svg += text(xAxis?.label ?? "input", {
    x: px1 + 12,
    y: py1 + 15,
    className: `pl-axis-title${axesAnim}`,
    fontSize: 10.5,
    maxWidth: 90
  });
  svg += el(
    "text",
    {
      x: px0 - 14,
      y: (py0 + py1) / 2,
      // Fade, never enter: a CSS transform would displace the rotation this
      // label carries in its own `transform` attribute mid-animation.
      class: `pl-axis-title${axesAnim}`,
      "text-anchor": "middle",
      transform: `rotate(-90 ${num(px0 - 14)} ${num((py0 + py1) / 2)})`
    },
    escapeXml(yAxis?.label ?? "output")
  );
  const capX = px1 + 34;
  const capWidth = ctx.width - ctx.pad - capX;
  if (capWidth > 90) {
    let capY = py0 + 22;
    svg += text(fn?.label ?? "f", {
      x: capX,
      y: capY,
      className: `pl-label-strong pl-mono${afterTrace()}`,
      fontSize: 13,
      maxWidth: capWidth,
      title: fn ? entityTooltip(fn) : void 0
    });
    capY += 18;
    if (fn?.detail) {
      const para = paragraph(fn.detail, {
        x: capX,
        y: capY,
        className: `pl-detail${afterTrace()}`,
        maxWidth: capWidth,
        fontSize: 10.5,
        lineHeight: 13,
        maxLines: 2
      });
      svg += para.svg;
      capY += para.height + 20;
    }
    for (const relationship of spec.relationships) {
      if (!relationship.label) continue;
      const para = paragraph(relationship.label, {
        x: capX,
        y: capY,
        className: `pl-label pl-mono${isWeak(relationship.epistemic) ? " pl-weak-text" : ""}${afterTrace()}`,
        maxWidth: capWidth,
        fontSize: 12,
        lineHeight: 16,
        maxLines: 3
      });
      svg += para.svg;
      capY += para.height + 22;
    }
  }
  legend.push({
    swatch: "curve",
    text: "The curve is one arbitrary function with the proved order property. The theorem constrains the ordering, not the shape."
  });
  if (schematic) {
    legend.push({
      swatch: "dashed-line",
      text: "Broken axes: both scales are schematic. No magnitude shown here was measured."
    });
  }
  return { svg, height: py1 + 34, legend };
}
function cubic(p0, p1, p2, p3, t) {
  const u = 1 - t;
  return u * u * u * p0 + 3 * u * u * t * p1 + 3 * u * t * t * p2 + t * t * t * p3;
}
function solveCubicT(p0, p1, p2, p3, target) {
  let lo = 0;
  let hi = 1;
  for (let i = 0; i < 48; i += 1) {
    const mid = (lo + hi) / 2;
    if (cubic(p0, p1, p2, p3, mid) < target) lo = mid;
    else hi = mid;
  }
  return (lo + hi) / 2;
}
function cubicLength(x0, y0, x1, y1, x2, y2, x3, y3) {
  let length = 0;
  let px = x0;
  let py = y0;
  for (let i = 1; i <= 24; i += 1) {
    const t = i / 24;
    const x = cubic(x0, x1, x2, x3, t);
    const y = cubic(y0, y1, y2, y3, t);
    length += Math.hypot(x - px, y - py);
    px = x;
    py = y;
  }
  return Math.ceil(length + 2);
}
function edgeLength(x1, y1, x2, y2) {
  return Math.ceil(Math.abs(x2 - x1) + Math.abs(y2 - y1)) + 100;
}
var LM_TOP = 14;
var LM_BASE = 200;
function layoutLimit(spec, ctx) {
  const legend = [];
  const axesAnim = ctx.anim({ kind: "fade", delay: stageDelay(0) });
  const afterTrace = () => ctx.anim({ kind: "fade", delay: stageDelay(1) + TRACE_DURATION });
  const px0 = ctx.pad + 46;
  const available = ctx.width - ctx.pad - px0;
  const plotWidth = Math.min(340, Math.max(150, available * 0.62));
  const px1 = px0 + plotWidth;
  const py0 = LM_TOP;
  const py1 = LM_BASE;
  const fn = spec.entities.find((e) => e.kind === "function");
  const limit = spec.entities.find((e) => e.id === "limit-value") ?? entitiesOfKind(spec, "bound")[0];
  const direction = spec.entities.find((e) => e.id === "direction" || e.kind === "label");
  const convergent = limit !== void 0;
  const detail = (fn?.detail ?? "").toLowerCase();
  const downward = !convergent && detail.includes("decreases without bound");
  const status = fn?.epistemic ?? spec.epistemic;
  const xAxis = spec.axes.find((a) => a.orientation === "horizontal");
  const yAxis = spec.axes.find((a) => a.orientation === "vertical");
  const schematic = (xAxis?.scale ?? "schematic") === "schematic";
  const axisClass = `pl-axis${schematic ? " pl-schematic" : ""}`;
  const Y = (v2) => py1 - v2 * (py1 - py0);
  let svg = "";
  svg += line(
    px0,
    py0 - 6,
    px0,
    py1,
    `${axisClass}${weakStrokeClass(yAxis?.epistemic ?? "illustrative")}${axesAnim}`,
    yAxis ? `${yAxis.label} \u2014 ${yAxis.scale} axis, ${statusPhrase(yAxis.epistemic)}` : "value \u2014 schematic axis"
  );
  svg += path(
    `M ${num(px0)} ${num(py1)} L ${num(px1 + 10)} ${num(py1)}`,
    `${axisClass}${weakStrokeClass(xAxis?.epistemic ?? "illustrative")}${axesAnim}`,
    { "marker-end": `url(#${ctx.id("arrow-muted")})` },
    xAxis ? `${xAxis.label} \u2014 ${xAxis.scale} axis, ${statusPhrase(xAxis.epistemic)}` : "input \u2014 schematic axis"
  );
  const limitY = Math.max(py0 + 24, Math.min(py1 - 30, Y(clamp01(limit?.position?.y, 0.3))));
  for (const tick of yAxis?.ticks ?? []) {
    const ty = Math.max(py0, Math.min(py1, Y(clamp01(tick.at, 0.3))));
    svg += line(px0 - 5, ty, px0, ty, `pl-axis${axesAnim}`, tick.label);
    svg += text(tick.label, {
      x: px0 - 8,
      y: ty + 3.5,
      className: `pl-tick${axesAnim}`,
      anchor: "end",
      fontSize: 10,
      maxWidth: Math.max(0, px0 - ctx.pad - 10),
      title: `${tick.label} on the ${yAxis?.label ?? "value"} axis`
    });
  }
  if (limit) {
    svg += path(
      `M ${num(px0)} ${num(limitY)} L ${num(px1 + 6)} ${num(limitY)}`,
      `pl-asymptote${weakStrokeClass(limit.epistemic)}${axesAnim}`,
      {},
      `${entityTooltip(limit)} \u2014 the curve approaches this line and never meets it`
    );
    svg += text(limit.label, {
      x: px1 + 8,
      y: limitY + 14,
      className: `pl-label-strong pl-mono${isWeak(limit.epistemic) ? " pl-weak-text" : ""}${axesAnim}`,
      anchor: "end",
      fontSize: 12.5,
      maxWidth: Math.max(0, plotWidth * 0.6),
      title: entityTooltip(limit)
    });
  }
  const ax = px0 + 12;
  const bx = px1 - 12;
  const dx = bx - ax;
  let curveEnd = py1;
  let curveLen;
  let d;
  if (convergent) {
    const yEnd = limitY - 4;
    const yStart = Math.max(py0 + 6, Math.min(py0 + 16, yEnd - 36));
    const dy = yEnd - yStart;
    const c1x = ax + dx * 0.18;
    const c1y = yStart + dy * 0.72;
    const c2x = ax + dx * 0.48;
    d = `M ${num(ax)} ${num(yStart)} C ${num(c1x)} ${num(c1y)} ${num(c2x)} ${num(yEnd)} ${num(bx)} ${num(yEnd)}`;
    curveLen = cubicLength(ax, yStart, c1x, c1y, c2x, yEnd, bx, yEnd);
    curveEnd = yEnd;
  } else {
    const yStart = downward ? py0 + 22 : py1 - 22;
    const yEnd = downward ? py1 + 18 : py0 - 8;
    const dy = yEnd - yStart;
    const c1x = ax + dx * 0.55;
    const c2x = ax + dx * 0.86;
    const c2y = yStart + dy * 0.55;
    d = `M ${num(ax)} ${num(yStart)} C ${num(c1x)} ${num(yStart)} ${num(c2x)} ${num(c2y)} ${num(bx)} ${num(yEnd)}`;
    curveLen = cubicLength(ax, yStart, c1x, yStart, c2x, c2y, bx, yEnd);
    curveEnd = yEnd;
  }
  const curveAnim = isWeak(status) ? ctx.anim({ kind: "fade", delay: stageDelay(1), duration: TRACE_DURATION }) : ctx.anim({
    kind: "draw",
    delay: stageDelay(1),
    duration: TRACE_DURATION,
    length: curveLen,
    // The divergence arrowhead appears only as the trace reaches the edge
    // of the frame; a convergent curve never carries one.
    revealMarker: convergent ? void 0 : ctx.id("arrow")
  });
  svg += path(
    d,
    `pl-curve${weakStrokeClass(status)}${curveAnim}`,
    convergent ? {} : { "marker-end": `url(#${ctx.id("arrow")})` },
    `${fn?.label ?? "the function"} \u2014 ${fn?.detail ?? (convergent ? "converges" : "diverges")} \u2014 ${statusPhrase(status)}`
  );
  if (!convergent) {
    const noticeY = downward ? py1 - 12 : py0 + 12;
    svg += text("the values leave every bound", {
      x: px0 + 10,
      y: noticeY,
      className: `pl-label${afterTrace()}`,
      fontSize: 12,
      maxWidth: Math.max(0, bx - px0 - 24),
      title: `${fn?.label ?? "the function"} ${fn?.detail ?? "leaves every bound"}`
    });
  }
  if (direction) {
    svg += text(direction.label, {
      x: px1 + 16,
      y: py1 + 4,
      className: `pl-label-strong pl-mono${isWeak(direction.epistemic) ? " pl-weak-text" : ""}${axesAnim}`,
      fontSize: 12.5,
      maxWidth: Math.max(0, ctx.width - ctx.pad - (px1 + 16)),
      title: entityTooltip(direction)
    });
  }
  const axisTitleY = downward ? py1 + 36 : py1 + 20;
  if (xAxis) {
    const units = xAxis.units ? ` (${xAxis.units})` : "";
    const suffix = schematic ? " \xB7 schematic scale" : "";
    svg += text(`${xAxis.label}${units}${suffix}`, {
      x: px0,
      y: axisTitleY,
      className: `pl-axis-title${axesAnim}`,
      fontSize: 10.5,
      maxWidth: Math.max(0, ctx.width - ctx.pad - px0),
      title: `${xAxis.label}${units} \u2014 ${xAxis.scale} axis`
    });
  }
  svg += el(
    "text",
    {
      x: ctx.pad + 8,
      y: (py0 + py1) / 2,
      // Fade, never enter: a CSS transform would displace this label's own
      // rotation mid-animation.
      class: `pl-axis-title${axesAnim}`,
      "text-anchor": "middle",
      transform: `rotate(-90 ${num(ctx.pad + 8)} ${num((py0 + py1) / 2)})`
    },
    escapeXml(yAxis?.label ?? "value")
  );
  const capX = px1 + 34;
  const capWidth = ctx.width - ctx.pad - capX;
  if (capWidth > 90) {
    let capY = py0 + 22;
    svg += text(fn?.label ?? "the function", {
      x: capX,
      y: capY,
      className: `pl-label-strong pl-mono${afterTrace()}`,
      fontSize: 13,
      maxWidth: capWidth,
      title: fn ? entityTooltip(fn) : void 0
    });
    capY += 18;
    if (fn?.detail) {
      const para = paragraph(fn.detail, {
        x: capX,
        y: capY,
        className: `pl-detail${afterTrace()}`,
        maxWidth: capWidth,
        fontSize: 10.5,
        lineHeight: 13,
        maxLines: 2
      });
      svg += para.svg;
      capY += para.height + 20;
    }
    for (const relationship of spec.relationships) {
      if (!relationship.label) continue;
      const para = paragraph(relationship.label, {
        x: capX,
        y: capY,
        className: `pl-label pl-mono${isWeak(relationship.epistemic) ? " pl-weak-text" : ""}${afterTrace()}`,
        maxWidth: capWidth,
        fontSize: 12,
        lineHeight: 16,
        maxLines: 3
      });
      svg += para.svg;
      capY += para.height + 22;
    }
    if (direction?.detail) {
      svg += text(direction.detail, {
        x: capX,
        y: capY,
        className: `pl-detail${afterTrace()}`,
        fontSize: 10.5,
        maxWidth: capWidth,
        title: entityTooltip(direction)
      });
    }
  }
  if (convergent) {
    legend.push({
      swatch: "asymptote",
      text: "Dotted horizontal line: the limit value. The curve closes on it and never meets it \u2014 the theorem says the values get arbitrarily close, not that any of them is the limit."
    });
    legend.push({
      swatch: "curve",
      text: "The curve is one arbitrary function with the proved limit. The theorem constrains where the values end up, not the path they take to get there."
    });
    legend.push({
      swatch: "arrow",
      text: `Arrowhead on the input axis: the direction the input travels${xAxis ? ` (${xAxis.label})` : ""}. There is no arrowhead on the curve, because the values settle rather than leave.`
    });
  } else {
    legend.push({
      swatch: "curve",
      text: `The curve leaves the frame ${downward ? "through the bottom" : "through the top"} and the arrowhead says it keeps going. There is no limit line to draw: the values leave every bound.`
    });
    legend.push({
      swatch: "arrow",
      text: `Arrowheads mark direction of travel: along the input axis${xAxis ? ` (${xAxis.label})` : ""}, and past the edge of the frame for the values themselves.`
    });
  }
  if (schematic) {
    legend.push({
      swatch: "dashed-line",
      text: "Broken axes: both scales are schematic. No magnitude shown here was measured, and neither the steepness of the curve nor how quickly it flattens means anything."
    });
  }
  return { svg, height: Math.max(axisTitleY + 14, curveEnd + 16), legend };
}
function layoutAssumptionSensitivity(spec, ctx) {
  const legend = [];
  const inner = ctx.width - ctx.pad * 2;
  const colWidth = Math.max(160, Math.round(inner * 0.44));
  const leftX = ctx.pad;
  const rightX = ctx.width - ctx.pad - colWidth;
  const hypotheses = entitiesOfKind(spec, "hypothesis");
  const used = hypotheses.filter((h) => h.state !== "unused");
  const unused = hypotheses.filter((h) => h.state === "unused");
  const conclusion = entitiesOfKind(spec, "conclusion")[0];
  const usedAnim = ctx.anim({ kind: "enter", delay: stageDelay(0) });
  const unusedAnim = () => ctx.anim({ kind: "fade", delay: stageDelay(1) });
  const conclusionStage = unused.length > 0 ? 2 : 1;
  const conclusionAnim = () => ctx.anim({ kind: "enter", delay: stageDelay(conclusionStage) });
  const boxes = [];
  let svg = "";
  let y = 12;
  const layoutGroup = (group2, heading, isUsed) => {
    if (group2.length === 0) return;
    const anim = isUsed ? usedAnim : unusedAnim();
    svg += text(heading, {
      x: leftX,
      y,
      className: `pl-heading${anim}`,
      fontSize: 10,
      maxWidth: colWidth
    });
    y += 12;
    for (const entity of group2) {
      const height = hypothesisBoxHeight(entity, colWidth, isUsed);
      boxes.push({ entity, x: leftX, y, width: colWidth, height, used: isUsed });
      svg += renderHypothesisBox(entity, leftX, y, colWidth, isUsed, anim);
      y += height + 10;
    }
    y += 8;
  };
  layoutGroup(used, `USED BY THE PROOF TERM (${used.length})`, true);
  if (unused.length > 0) {
    svg += line(leftX, y - 4, leftX + colWidth, y - 4, `pl-rule${unusedAnim()}`);
    y += 8;
    layoutGroup(unused, `STATED BUT NEVER USED (${unused.length})`, false);
  }
  const leftBottom = Math.max(y, 40);
  let rightSvg = "";
  let rightBottom = 40;
  if (conclusion) {
    const label = conclusion.label;
    const wrapped = wrapToWidth(label, colWidth - 20, 12, 5);
    const height = 24 + Math.max(1, wrapped.lines.length) * 16;
    const usedBoxes = boxes.filter((b) => b.used);
    const first = usedBoxes[0];
    const last = usedBoxes[usedBoxes.length - 1];
    const anchorCenter = first && last ? (first.y + (last.y + last.height)) / 2 : leftBottom / 2 + 12;
    const top = Math.max(
      24,
      Math.min(anchorCenter - height / 2, Math.max(24, leftBottom - height))
    );
    rightSvg += text("CONCLUSION", {
      x: rightX,
      y: top - 10,
      className: `pl-heading${conclusionAnim()}`,
      fontSize: 10,
      maxWidth: colWidth
    });
    rightSvg += box({
      x: rightX,
      y: top,
      width: colWidth,
      height,
      className: `pl-box-primary${weakStrokeClass(conclusion.epistemic)}${conclusionAnim()}`,
      tooltip: entityTooltip(conclusion)
    });
    const para = paragraph(label, {
      x: rightX + 10,
      y: top + 22,
      className: `pl-label pl-mono${conclusionAnim()}`,
      maxWidth: colWidth - 20,
      fontSize: 12,
      lineHeight: 16,
      maxLines: 5
    });
    rightSvg += para.svg;
    rightBottom = top + height;
    const byId = new Map(boxes.map((b) => [b.entity.id, b]));
    const conclusionCy = top + height / 2;
    for (const relationship of spec.relationships) {
      if (relationship.to !== conclusion.id) continue;
      const source = byId.get(relationship.from);
      if (!source || !source.used) continue;
      const x1 = source.x + source.width;
      const y1 = source.y + source.height / 2;
      const d = connector(x1, y1, rightX, conclusionCy);
      const wireAnim = isWeak(relationship.epistemic) ? ctx.anim({ kind: "fade", delay: afterStage(conclusionStage) }) : ctx.anim({
        kind: "draw",
        delay: afterStage(conclusionStage),
        length: edgeLength(x1, y1, rightX, conclusionCy),
        revealMarker: ctx.id("arrow")
      });
      svg += path(
        d,
        `pl-edge-used${weakStrokeClass(relationship.epistemic)}${wireAnim}`,
        { "marker-end": `url(#${ctx.id("arrow")})` },
        `${source.entity.label} is used to prove the conclusion \u2014 ${statusPhrase(relationship.epistemic)}`
      );
    }
  }
  svg += rightSvg;
  legend.push({
    swatch: "used-box",
    text: "Connected box: the elaborated proof term references this hypothesis."
  });
  if (unused.length > 0) {
    legend.push({
      swatch: "unused-box",
      text: "Detached, dashed box: the hypothesis is stated but never referenced by this proof. That is a fact about the proof, not about mathematical necessity \u2014 another proof might need it."
    });
  }
  legend.push({
    swatch: "arrow",
    text: "An arrow means \u201Cthis assumption is used in deriving the conclusion\u201D."
  });
  return { svg, height: Math.max(leftBottom, rightBottom) + 10, legend };
}
function hypothesisBoxHeight(entity, width, used) {
  const detailLines = entity.detail ? wrapToWidth(entity.detail, width - 20, 10.5, 2).lines.length : 0;
  return 12 + 16 + detailLines * 13 + (used ? 0 : 14) + 8;
}
function renderHypothesisBox(entity, x, y, width, used, anim = "") {
  const height = hypothesisBoxHeight(entity, width, used);
  const className = used ? `pl-box${weakStrokeClass(entity.epistemic)}${anim}` : `pl-box-unused${weakFillClass(entity.epistemic)}${anim}`;
  let svg = box({ x, y, width, height, className, tooltip: entityTooltip(entity) });
  let cursor = y + 22;
  svg += text(entity.label, {
    x: x + 10,
    y: cursor,
    className: `pl-label-strong pl-mono${used ? "" : " pl-unused-text"}${anim}`,
    fontSize: 12.5,
    maxWidth: width - 20,
    title: entityTooltip(entity)
  });
  cursor += 15;
  if (entity.detail) {
    const para = paragraph(entity.detail, {
      x: x + 10,
      y: cursor,
      className: `pl-detail pl-mono${used ? "" : " pl-unused-text"}${anim}`,
      maxWidth: width - 20,
      fontSize: 10.5,
      lineHeight: 13,
      maxLines: 2
    });
    svg += para.svg;
    cursor += para.height + 14;
  }
  if (!used) {
    svg += text("NEVER USED IN THIS PROOF", {
      x: x + 10,
      y: cursor,
      className: `pl-badge${anim}`,
      fontSize: 9,
      maxWidth: width - 20
    });
  }
  return svg;
}
function layoutLayeredGraph(spec, ctx) {
  const legend = [];
  const inner = ctx.width - ctx.pad * 2;
  const layers = /* @__PURE__ */ new Map();
  for (const entity of spec.entities) {
    const layer2 = entity.position?.layer ?? 0;
    const bucket = layers.get(layer2);
    if (bucket) bucket.push(entity);
    else layers.set(layer2, [entity]);
  }
  const layerKeys = Array.from(layers.keys()).sort((a, b) => a - b);
  for (const key of layerKeys) {
    const bucket = layers.get(key);
    bucket.sort((a, b) => (a.position?.order ?? 0) - (b.position?.order ?? 0));
  }
  const stageOf = (entity) => {
    const index = layerKeys.indexOf(entity.position?.layer ?? 0);
    return index < 0 ? 0 : index;
  };
  const hasDetail = spec.entities.some((e) => e.detail);
  const nodeHeight = hasDetail ? 42 : 30;
  const columnWidth = layerKeys.length > 0 ? inner / layerKeys.length : inner;
  const horizontal = columnWidth - 22 >= 110;
  const rects = /* @__PURE__ */ new Map();
  let svg = "";
  let height;
  if (horizontal) {
    const boxWidth = columnWidth - 22;
    const heights = layerKeys.map((key) => {
      const n = layers.get(key).length;
      return n * nodeHeight + (n - 1) * 14;
    });
    const tallest = heights.reduce((a, b) => Math.max(a, b), 0);
    layerKeys.forEach((key, i) => {
      const bucket = layers.get(key);
      const columnHeight = heights[i] ?? 0;
      let y = 8 + (tallest - columnHeight) / 2;
      const x = ctx.pad + i * columnWidth + 11;
      for (const entity of bucket) {
        rects.set(entity.id, { x, y, width: boxWidth, height: nodeHeight });
        y += nodeHeight + 14;
      }
    });
    height = tallest + 20;
  } else {
    const rowHeight = nodeHeight + 34;
    layerKeys.forEach((key, i) => {
      const bucket = layers.get(key);
      const cellWidth = inner / Math.max(1, bucket.length);
      const boxWidth = Math.max(60, cellWidth - 14);
      bucket.forEach((entity, j) => {
        rects.set(entity.id, {
          x: ctx.pad + j * cellWidth + (cellWidth - boxWidth) / 2,
          y: 8 + i * rowHeight,
          width: boxWidth,
          height: nodeHeight
        });
      });
    });
    height = layerKeys.length * rowHeight + 12;
  }
  const entityById = new Map(spec.entities.map((e) => [e.id, e]));
  for (const relationship of spec.relationships) {
    const from = rects.get(relationship.from);
    const to = rects.get(relationship.to);
    if (!from || !to) continue;
    const d = routeEdge(from, to, horizontal);
    const weak = isWeak(relationship.epistemic);
    const fromEntity = entityById.get(relationship.from);
    const toEntity = entityById.get(relationship.to);
    const readyAt = afterStage(
      Math.max(fromEntity ? stageOf(fromEntity) : 0, toEntity ? stageOf(toEntity) : 0)
    );
    const edgeAnim = weak ? ctx.anim({ kind: "fade", delay: readyAt }) : ctx.anim({
      kind: "draw",
      delay: readyAt,
      length: edgeLength(
        from.x + from.width / 2,
        from.y + from.height / 2,
        to.x + to.width / 2,
        to.y + to.height / 2
      ),
      revealMarker: ctx.id("arrow-muted")
    });
    svg += path(
      d,
      `pl-edge${weak ? " pl-weak-stroke" : ""}${edgeAnim}`,
      { "marker-end": `url(#${ctx.id("arrow-muted")})` },
      `${labelFor(spec, relationship.from)} ${relationship.kind} ${labelFor(spec, relationship.to)} \u2014 ${statusPhrase(relationship.epistemic)}`
    );
    if (relationship.label) {
      const mid = midpoint(from, to);
      svg += text(relationship.label, {
        x: mid.x,
        y: mid.y - 5,
        className: `pl-detail${ctx.anim({ kind: "fade", delay: readyAt })}`,
        anchor: "middle",
        fontSize: 10.5,
        maxWidth: 120,
        title: `${relationship.kind}: ${relationship.label}`
      });
    }
  }
  for (const entity of spec.entities) {
    const rect = rects.get(entity.id);
    if (!rect) continue;
    const primary = entity.emphasis === "primary";
    const nodeAnim = ctx.anim({ kind: "enter", delay: stageDelay(stageOf(entity)) });
    svg += box({
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height,
      className: `${primary ? "pl-box-primary" : "pl-box"}${weakStrokeClass(entity.epistemic)}${nodeAnim}`,
      tooltip: entityTooltip(entity)
    });
    svg += text(entity.label, {
      x: rect.x + 9,
      y: rect.y + (entity.detail ? 19 : 19),
      className: `pl-label pl-mono${isWeak(entity.epistemic) ? " pl-weak-text" : ""}${nodeAnim}`,
      fontSize: 11.5,
      maxWidth: rect.width - 18,
      title: entityTooltip(entity)
    });
    if (entity.detail) {
      svg += text(entity.detail, {
        x: rect.x + 9,
        y: rect.y + 33,
        className: `pl-detail${nodeAnim}`,
        fontSize: 10.5,
        maxWidth: rect.width - 18
      });
    }
  }
  legend.push({
    swatch: "arrow",
    text: horizontal ? "Layers run left to right; an arrow points from a node to what it relates to." : "Layers run top to bottom; an arrow points from a node to what it relates to."
  });
  return { svg, height, legend };
}
function labelFor(spec, id) {
  return spec.entities.find((e) => e.id === id)?.label ?? id;
}
function midpoint(a, b) {
  return {
    x: (a.x + a.width / 2 + b.x + b.width / 2) / 2,
    y: (a.y + a.height / 2 + b.y + b.height / 2) / 2
  };
}
function routeEdge(a, b, horizontal) {
  const acy = a.y + a.height / 2;
  const bcy = b.y + b.height / 2;
  const acx = a.x + a.width / 2;
  const bcx = b.x + b.width / 2;
  if (horizontal) {
    if (b.x >= a.x + a.width) return connector(a.x + a.width, acy, b.x, bcy);
    if (b.x + b.width <= a.x) return connector(a.x, acy, b.x + b.width, bcy);
    return connectorVertical(acx, a.y + a.height, bcx, b.y);
  }
  if (b.y >= a.y + a.height) return connectorVertical(acx, a.y + a.height, bcx, b.y);
  if (b.y + b.height <= a.y) return connectorVertical(acx, a.y, bcx, b.y + b.height);
  return connector(a.x + a.width, acy, b.x, bcy);
}
function layoutExpressionTree(spec, ctx) {
  const legend = [];
  const inner = ctx.width - ctx.pad * 2;
  const conclusion = entitiesOfKind(spec, "conclusion")[0];
  const hypotheses = entitiesOfKind(spec, "hypothesis");
  const conclusionAnim = () => ctx.anim({ kind: "enter", delay: stageDelay(0) });
  const spineAt = afterStage(1);
  let svg = "";
  let y = 12;
  let spineTop = y;
  if (conclusion) {
    svg += text("CONCLUSION", {
      x: ctx.pad,
      y,
      className: `pl-heading${conclusionAnim()}`,
      fontSize: 10,
      maxWidth: inner
    });
    y += 12;
    const wrapped = wrapToWidth(conclusion.label, inner - 20, 12.5, 4);
    const height = 22 + Math.max(1, wrapped.lines.length) * 16;
    svg += box({
      x: ctx.pad,
      y,
      width: inner,
      height,
      className: `pl-box-primary${weakStrokeClass(conclusion.epistemic)}${conclusionAnim()}`,
      tooltip: entityTooltip(conclusion)
    });
    svg += paragraph(conclusion.label, {
      x: ctx.pad + 10,
      y: y + 21,
      className: `pl-label pl-mono${conclusionAnim()}`,
      maxWidth: inner - 20,
      fontSize: 12.5,
      lineHeight: 16,
      maxLines: 4
    }).svg;
    y += height + 6;
    spineTop = y;
  }
  if (hypotheses.length > 0) {
    y += 16;
    svg += text(`HYPOTHESES (${hypotheses.length})`, {
      x: ctx.pad + 26,
      y,
      className: `pl-heading${ctx.anim({ kind: "fade", delay: stageDelay(1) })}`,
      fontSize: 10,
      maxWidth: inner - 26
    });
    y += 10;
    const spineX = ctx.pad + 12;
    const boxX = ctx.pad + 26;
    const boxWidth = inner - 26;
    let lastCenter = y;
    for (const entity of hypotheses) {
      const used = entity.state !== "unused";
      const height = hypothesisBoxHeight(entity, boxWidth, used);
      const hypAnim = ctx.anim({
        kind: used ? "enter" : "fade",
        delay: stageDelay(1)
      });
      svg += renderHypothesisBox(entity, boxX, y, boxWidth, used, hypAnim);
      lastCenter = y + height / 2;
      svg += line(
        spineX,
        lastCenter,
        boxX,
        lastCenter,
        used ? `pl-edge-used${ctx.anim({ kind: "draw", delay: spineAt, length: boxX - spineX })}` : `pl-edge pl-weak-stroke${ctx.anim({ kind: "fade", delay: spineAt })}`
      );
      y += height + 10;
    }
    svg += line(
      spineX,
      spineTop,
      spineX,
      lastCenter,
      `pl-edge${ctx.anim({
        kind: "draw",
        delay: spineAt,
        length: Math.max(1, lastCenter - spineTop)
      })}`
    );
  }
  legend.push({
    swatch: "used-box",
    text: "ProofLens shows a theorem's formal structure even when it cannot interpret the statement. Nothing in this figure is guessed."
  });
  if (hypotheses.some((h) => h.state === "unused")) {
    legend.push({
      swatch: "unused-box",
      text: "Dashed box: the hypothesis is stated but never referenced by the proof term."
    });
  }
  return { svg, height: y + 6, legend };
}
function layoutGeneric(spec, ctx) {
  const inner = ctx.width - ctx.pad * 2;
  let svg = "";
  let y = 14;
  const section = (heading, lines) => {
    if (lines.length === 0) return;
    svg += text(heading, { x: ctx.pad, y, className: "pl-heading", fontSize: 10, maxWidth: inner });
    y += 15;
    for (const item of lines) {
      svg += text(item.text, {
        x: ctx.pad + 10,
        y,
        className: `pl-label${item.weak ? " pl-weak-text" : ""}`,
        fontSize: 11.5,
        maxWidth: inner - 10,
        title: item.tooltip
      });
      y += 16;
    }
    y += 10;
  };
  section(
    `ELEMENTS (${spec.entities.length})`,
    spec.entities.map((entity) => ({
      text: `${entity.label}  \xB7  ${entity.kind}${entity.state && entity.state !== "neutral" ? ` / ${entity.state}` : ""}  \xB7  ${entity.epistemic}`,
      tooltip: entityTooltip(entity),
      weak: isWeak(entity.epistemic)
    }))
  );
  section(
    `RELATIONSHIPS (${spec.relationships.length})`,
    spec.relationships.map((relationship) => ({
      text: `${labelFor(spec, relationship.from)}  \u2014${relationship.kind}\u2192  ${labelFor(spec, relationship.to)}${relationship.label ? `  (${relationship.label})` : ""}`,
      tooltip: `${relationship.kind} \u2014 ${statusPhrase(relationship.epistemic)}`,
      weak: isWeak(relationship.epistemic)
    }))
  );
  section(
    `AXES (${spec.axes.length})`,
    spec.axes.map((axis) => ({
      text: `${axis.label}${axis.units ? ` (${axis.units})` : ""}  \xB7  ${axis.orientation}  \xB7  ${axis.scale}`,
      tooltip: `${axis.label} \u2014 ${axis.scale} axis, ${statusPhrase(axis.epistemic)}`,
      weak: isWeak(axis.epistemic)
    }))
  );
  if (spec.entities.length === 0 && spec.relationships.length === 0 && spec.axes.length === 0) {
    svg += text("This figure has no elements to draw.", {
      x: ctx.pad,
      y,
      className: "pl-note",
      fontSize: 11,
      maxWidth: inner
    });
    y += 18;
  }
  return {
    svg,
    height: y,
    legend: [
      {
        swatch: "none",
        text: `No purpose-built layout exists for \u201C${spec.type}\u201D, so its contents are listed rather than drawn. Each line ends with that element's epistemic standing.`
      }
    ]
  };
}

// packages/renderer-svg/src/theme.ts
var LIGHT = {
  bg: "#ffffff",
  panel: "#f5f7f9",
  border: "#d6dbe1",
  fg: "#151a20",
  muted: "#586170",
  faint: "#7d8794",
  axis: "#39424e",
  accent: "#1f5fbf",
  accentSoft: "#e2ecfb",
  permit: "#1c6f47",
  permitFill: "#dcefe4",
  exclude: "#a03521",
  excludeFill: "#f8e5e0",
  warn: "#8f2711",
  warnBg: "#fdeae4",
  warnBorder: "#e7a48f",
  edge: "#6a7482",
  unused: "#8a93a0"
};
var DARK = {
  bg: "#12161b",
  panel: "#1b212a",
  border: "#303a47",
  fg: "#e9edf2",
  muted: "#a6b0bc",
  faint: "#7d8895",
  axis: "#b8c2ce",
  accent: "#7fb0ff",
  accentSoft: "#1c2b41",
  permit: "#6ed49e",
  permitFill: "#16301f",
  exclude: "#ff9c84",
  excludeFill: "#3a1c16",
  warn: "#ffb3a0",
  warnBg: "#38160f",
  warnBorder: "#7c3827",
  edge: "#8d98a6",
  unused: "#7d8895"
};
var DEFAULT_FONT_FAMILY = 'ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif';
var MONO_FONT_FAMILY = 'ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "DejaVu Sans Mono", "Liberation Mono", monospace';
function sanitizeFontFamily(family) {
  if (!family) return DEFAULT_FONT_FAMILY;
  const cleaned = family.replace(/[<>&{};@\\]/g, "").replace(/'/g, '"').trim();
  return cleaned === "" ? DEFAULT_FONT_FAMILY : cleaned;
}
function vars(p) {
  return [
    `--pl-bg:${p.bg}`,
    `--pl-panel:${p.panel}`,
    `--pl-border:${p.border}`,
    `--pl-fg:${p.fg}`,
    `--pl-muted:${p.muted}`,
    `--pl-faint:${p.faint}`,
    `--pl-axis:${p.axis}`,
    `--pl-accent:${p.accent}`,
    `--pl-accent-soft:${p.accentSoft}`,
    `--pl-permit:${p.permit}`,
    `--pl-permit-fill:${p.permitFill}`,
    `--pl-exclude:${p.exclude}`,
    `--pl-exclude-fill:${p.excludeFill}`,
    `--pl-warn:${p.warn}`,
    `--pl-warn-bg:${p.warnBg}`,
    `--pl-warn-border:${p.warnBorder}`,
    `--pl-edge:${p.edge}`,
    `--pl-unused:${p.unused}`
  ].join(";");
}
function buildStylesheet(theme, fontFamily) {
  const sans = sanitizeFontFamily(fontFamily);
  let palette;
  if (theme === "dark") {
    palette = `:root{${vars(DARK)}}`;
  } else if (theme === "light") {
    palette = `:root{${vars(LIGHT)}}`;
  } else {
    palette = `:root{${vars(LIGHT)}}@media (prefers-color-scheme:dark){:root{${vars(DARK)}}}`;
  }
  const rules = [
    `.pl-bg{fill:var(--pl-bg)}`,
    `text{font-family:${sans};fill:var(--pl-fg);white-space:pre}`,
    `.pl-mono{font-family:${MONO_FONT_FAMILY}}`,
    `.pl-title{font-size:17px;font-weight:600;fill:var(--pl-fg)}`,
    `.pl-subtitle{font-size:11px;fill:var(--pl-muted);letter-spacing:.08em}`,
    `.pl-status{font-size:10.5px;fill:var(--pl-faint);letter-spacing:.06em}`,
    `.pl-heading{font-size:10px;fill:var(--pl-faint);letter-spacing:.1em;font-weight:600}`,
    `.pl-label{font-size:12px;fill:var(--pl-fg)}`,
    `.pl-label-strong{font-size:12.5px;font-weight:600;fill:var(--pl-fg)}`,
    `.pl-detail{font-size:10.5px;fill:var(--pl-muted)}`,
    `.pl-tick{font-size:10px;fill:var(--pl-muted)}`,
    `.pl-axis-title{font-size:10.5px;fill:var(--pl-muted)}`,
    `.pl-axis{stroke:var(--pl-axis);stroke-width:1.5;fill:none;stroke-linecap:round}`,
    `.pl-guide{stroke:var(--pl-border);stroke-width:1;fill:none;stroke-dasharray:2 3}`,
    `.pl-schematic{stroke-dasharray:7 4}`,
    `.pl-marker{stroke:var(--pl-accent);stroke-width:2;fill:none;stroke-linecap:round}`,
    `.pl-dot{fill:var(--pl-accent);stroke:none}`,
    `.pl-dot-open{fill:var(--pl-bg);stroke:var(--pl-accent);stroke-width:2.25}`,
    `.pl-region-permit{fill:var(--pl-permit-fill);stroke:var(--pl-permit);stroke-width:1}`,
    `.pl-region-exclude{fill:var(--pl-exclude-fill);stroke:var(--pl-exclude);stroke-width:1}`,
    `.pl-permit-text{font-size:10.5px;fill:var(--pl-permit);font-weight:600}`,
    `.pl-exclude-text{font-size:10.5px;fill:var(--pl-exclude);font-weight:600}`,
    `.pl-box{fill:var(--pl-panel);stroke:var(--pl-border);stroke-width:1}`,
    `.pl-box-primary{fill:var(--pl-accent-soft);stroke:var(--pl-accent);stroke-width:1.5}`,
    `.pl-box-unused{fill:var(--pl-bg);stroke:var(--pl-unused);stroke-width:1;stroke-dasharray:5 3}`,
    `.pl-unused-text{fill:var(--pl-unused)}`,
    `.pl-badge{font-size:9px;fill:var(--pl-unused);letter-spacing:.09em;font-weight:600}`,
    `.pl-edge{stroke:var(--pl-edge);stroke-width:1.4;fill:none}`,
    `.pl-edge-used{stroke:var(--pl-accent);stroke-width:1.75;fill:none}`,
    `.pl-curve{stroke:var(--pl-accent);stroke-width:2.25;fill:none;stroke-linecap:round}`,
    // The asymptote of a convergent limit. Deliberately a fine dot pattern
    // rather than the 5/3 of `pl-weak-stroke` or the 7/4 of `pl-schematic`, so
    // that "this is the limit" and "this is illustrative" stay distinguishable
    // at a glance. When the limit itself is weak, `pl-weak-stroke` is declared
    // later and wins — the epistemic encoding outranks the drawing convention.
    `.pl-asymptote{stroke:var(--pl-accent);stroke-width:1.5;fill:none;stroke-dasharray:1 4;stroke-linecap:round}`,
    `.pl-rule{stroke:var(--pl-border);stroke-width:1}`,
    `.pl-rationale-bar{fill:var(--pl-accent);stroke:none}`,
    `.pl-rationale{font-size:12px;fill:var(--pl-fg)}`,
    `.pl-warn-box{fill:var(--pl-warn-bg);stroke:var(--pl-warn-border);stroke-width:1.25}`,
    `.pl-warn-text{font-size:11.5px;fill:var(--pl-warn);font-weight:600}`,
    `.pl-note{font-size:10.5px;fill:var(--pl-muted)}`,
    // The epistemic encoding. Anything weaker than `derived` is drawn with a
    // broken stroke and a lightened fill, and the legend says so in words.
    `.pl-weak-stroke{stroke-dasharray:5 3}`,
    `.pl-weak-fill{fill-opacity:.55}`,
    `.pl-weak-text{fill:var(--pl-muted)}`
  ].join("");
  return palette + rules;
}

// packages/renderer-svg/src/index.ts
var DEFAULT_WIDTH = 720;
var PAD = 24;
var BLOCK_GAP = 18;
function renderSvg(spec, options = {}) {
  const width = clampWidth(options.width ?? DEFAULT_WIDTH);
  const theme = options.theme ?? "auto";
  const prefix = sanitizeId(options.idPrefix ?? "pl");
  const ctx = createContext(spec, width, PAD, prefix, options.animate === true);
  const body = safeLayout(spec, ctx);
  const animated = ctx.animTargets.length > 0;
  const legendRows = [...body.legend];
  if (specHasWeakElement(spec)) legendRows.push(WEAK_LEGEND_ROW);
  legendRows.push({
    swatch: "none",
    text: `This figure as a whole is ${spec.epistemic}. ${EPISTEMIC_GLOSS[spec.epistemic]}`
  });
  if (animated) legendRows.push(ANIMATION_LEGEND_ROW);
  let chromeAnim = "";
  if (animated) {
    const settled = ctx.animTargets.reduce((t, a) => Math.max(t, a.delay + a.duration), 0);
    chromeAnim = ctx.anim({ kind: "fade", delay: settled, duration: CHROME_FADE_DURATION });
  }
  const header = renderHeader(spec, ctx);
  const headerH = headerHeight(spec);
  const legend = renderLegend(legendRows, ctx, ctx.id("arrow"));
  const annotations = renderAnnotations(spec, ctx);
  let y = headerH;
  let content = header;
  content += group(y, body.svg);
  y += body.height + BLOCK_GAP;
  if (legend.height > 0) {
    content += group(y, legend.svg, chromeAnim);
    y += legend.height + BLOCK_GAP;
  }
  if (annotations.height > 0) {
    content += group(y, annotations.svg, chromeAnim);
    y += annotations.height;
  }
  const totalHeight = Math.max(120, Math.ceil(y + PAD));
  const titleId = ctx.id("title");
  const descId = ctx.id("desc");
  const rootAttrs = attrs({
    xmlns: "http://www.w3.org/2000/svg",
    "xmlns:xlink": void 0,
    viewBox: `0 0 ${num(width)} ${num(totalHeight)}`,
    width: options.width !== void 0 ? options.width : "100%",
    height: options.height !== void 0 ? options.height : void 0,
    // Letterboxing rather than stretching: a distorted figure would misstate
    // relative positions, which on a number line is a factual error.
    preserveAspectRatio: "xMidYMin meet",
    role: "img",
    "aria-labelledby": `${titleId} ${descId}`,
    class: "prooflens-figure",
    "data-prooflens-type": spec.type,
    "data-prooflens-epistemic": spec.epistemic
  });
  const style = `<style>${buildStylesheet(theme, options.fontFamily ?? DEFAULT_FONT_FAMILY)}${buildAnimationStylesheet(ctx)}</style>`;
  const label = `<title id="${titleId}">${escapeXml(spec.title)}</title><desc id="${descId}">${escapeXml(describe(spec))}</desc>`;
  const background = `<rect x="0" y="0" width="${num(width)}" height="${num(totalHeight)}" class="pl-bg"/>`;
  return `<svg${rootAttrs}>${label}${style}${arrowDefs(ctx)}${background}${content}</svg>`;
}
function group(y, inner, animClass = "") {
  if (inner === "") return "";
  const cls = animClass === "" ? "" : ` class="${animClass.trimStart()}"`;
  return `<g transform="translate(0 ${num(y)})"${cls}>${inner}</g>`;
}
function clampWidth(width) {
  if (!Number.isFinite(width)) return DEFAULT_WIDTH;
  return Math.min(4e3, Math.max(320, Math.round(width)));
}
function safeLayout(spec, ctx) {
  try {
    return dispatch(spec.type, spec, ctx);
  } catch {
    ctx.resetAnim();
    try {
      return layoutGeneric(spec, ctx);
    } catch {
      ctx.resetAnim();
      return { svg: "", height: 40, legend: [] };
    }
  }
}
function dispatch(type, spec, ctx) {
  switch (type) {
    case "upper-bound-plot":
    case "lower-bound-plot":
    case "number-line":
      return layoutNumberLine(spec, ctx);
    case "monotonicity-plot":
      return layoutMonotonicity(spec, ctx);
    case "limit-plot":
      return layoutLimit(spec, ctx);
    case "assumption-sensitivity":
      return layoutAssumptionSensitivity(spec, ctx);
    case "dependency-graph":
    case "implication-graph":
    case "relationship-diagram":
      return layoutLayeredGraph(spec, ctx);
    case "expression-tree":
      return layoutExpressionTree(spec, ctx);
    default:
      return layoutGeneric(spec, ctx);
  }
}
var TYPE_DESCRIPTION = {
  "upper-bound-plot": "a horizontal number line showing which values the bounded quantity is permitted to take and which the theorem rules out",
  "lower-bound-plot": "a horizontal number line showing which values the bounded quantity is permitted to take and which the theorem rules out",
  "number-line": "a horizontal number line with the marked value and the regions on either side of it",
  "monotonicity-plot": "a schematic curve illustrating the direction in which the function's output moves as its input increases",
  "limit-plot": "a schematic plot of what the function's values do along the given filter: either a dotted line at the limit value with a curve that closes on it without ever meeting it, or, for a divergence, a curve leaving the frame under an arrowhead",
  "assumption-sensitivity": "two columns of boxes: the theorem's stated hypotheses on the left, the conclusion on the right, with a connector drawn for each hypothesis the proof term actually uses",
  "dependency-graph": "a layered graph of the declarations this proof references, with arrows pointing from each declaration to what it depends on",
  "implication-graph": "a layered graph showing which statement follows from which",
  "relationship-diagram": "a layered graph of the elements and how they relate",
  "expression-tree": "the theorem's formal structure: the conclusion, with the hypotheses that lead to it listed beneath"
};
function describe(spec) {
  const what = TYPE_DESCRIPTION[spec.type] ?? `a listing of this figure's ${spec.entities.length} elements`;
  const parts = [`${spec.title}. This figure shows ${what}.`];
  const weakElements = spec.entities.filter((e) => e.epistemic !== spec.epistemic);
  const overall = weakest(spec.epistemic, ...spec.entities.map((e) => e.epistemic));
  parts.push(`Epistemic status: ${spec.epistemic}. ${EPISTEMIC_GLOSS[spec.epistemic]}`);
  if (overall !== spec.epistemic) {
    parts.push(`Some elements are weaker still (${overall}). ${EPISTEMIC_GLOSS[overall]}`);
  } else if (weakElements.length > 0) {
    parts.push(
      "Elements drawn with dashed strokes are illustrative and make no mathematical claim."
    );
  }
  const unused = spec.entities.filter((e) => e.state === "unused");
  if (unused.length > 0) {
    parts.push(
      `${unused.length} of the stated hypotheses (${unused.map((e) => e.label).join(", ")}) are never used by the proof.`
    );
  }
  parts.push(`Why this figure: ${spec.rationale}`);
  return parts.join(" ");
}

// packages/epistemics/src/index.ts
var EPISTEMIC_GLOSS2 = {
  verified: "Checked by the Lean kernel.",
  derived: "Computed from the verified statement by a deterministic rule.",
  interpreted: "A reading of the formal statement, not part of what was proved.",
  heuristic: "A rule-of-thumb guess that may be wrong.",
  illustrative: "A display choice. It makes no mathematical claim.",
  speculative: "Generated by a model. Not verified by anything."
};
var WITNESS_BRAND2 = Symbol("prooflens.kernel-witness");

// lean/widget/src/index.tsx
var v = (name, fallback) => `var(${name}, ${fallback})`;
var colors = {
  fg: v("--vscode-editor-foreground", "#1f2328"),
  bg: v("--vscode-editor-background", "#ffffff"),
  muted: v("--vscode-descriptionForeground", "#57606a"),
  border: v("--vscode-panel-border", "#d0d7de"),
  accent: v("--vscode-textLink-foreground", "#0969da"),
  warnBg: v("--vscode-inputValidation-warningBackground", "#fff8c5"),
  warnFg: v("--vscode-inputValidation-warningForeground", "#4d2d00"),
  errBg: v("--vscode-inputValidation-errorBackground", "#ffebe9"),
  errFg: v("--vscode-inputValidation-errorForeground", "#82071e"),
  code: v("--vscode-textCodeBlock-background", "#f6f8fa")
};
var STATUS_STYLE = {
  verified: { glyph: "\u2714", color: "#1a7f37" },
  derived: { glyph: "\u27F9", color: "#0969da" },
  interpreted: { glyph: "\u2248", color: "#8250df" },
  heuristic: { glyph: "?", color: "#9a6700" },
  illustrative: { glyph: "\u25C7", color: "#57606a" },
  speculative: { glyph: "\u2726", color: "#bf3989" }
};
function StatusChip({ status }) {
  const style = STATUS_STYLE[status];
  return /* @__PURE__ */ React.createElement(
    "span",
    {
      title: EPISTEMIC_GLOSS2[status],
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: "0.3em",
        border: `1px solid ${style.color}`,
        borderStyle: status === "illustrative" || status === "speculative" ? "dashed" : "solid",
        borderRadius: "999px",
        padding: "0 0.55em",
        fontSize: "0.78em",
        lineHeight: "1.6",
        color: style.color,
        whiteSpace: "nowrap"
      }
    },
    /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true" }, style.glyph),
    status
  );
}
function Section({
  title,
  children,
  right
}) {
  return /* @__PURE__ */ React.createElement("section", { style: { marginBottom: "1.1em" } }, /* @__PURE__ */ React.createElement(
    "h3",
    {
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        gap: "0.6em",
        margin: "0 0 0.4em",
        fontSize: "0.82em",
        letterSpacing: "0.06em",
        textTransform: "uppercase",
        color: colors.muted,
        fontWeight: 600
      }
    },
    /* @__PURE__ */ React.createElement("span", null, title),
    right
  ), children);
}
function Code({ children }) {
  return /* @__PURE__ */ React.createElement(
    "pre",
    {
      style: {
        margin: 0,
        padding: "0.6em 0.7em",
        background: colors.code,
        border: `1px solid ${colors.border}`,
        borderRadius: "4px",
        overflowX: "auto",
        fontSize: "0.92em",
        whiteSpace: "pre-wrap",
        wordBreak: "break-word"
      }
    },
    children
  );
}
function TrustBanner({ analysis }) {
  const { usesSorry, unusualAxioms: unusualAxioms2 } = analysis.math.trust;
  if (!usesSorry && unusualAxioms2.length === 0) return null;
  const isError = usesSorry;
  return /* @__PURE__ */ React.createElement(
    "div",
    {
      role: "alert",
      style: {
        background: isError ? colors.errBg : colors.warnBg,
        color: isError ? colors.errFg : colors.warnFg,
        border: `1px solid ${isError ? "#cf222e" : "#bf8700"}`,
        borderRadius: "4px",
        padding: "0.5em 0.7em",
        marginBottom: "0.9em",
        fontSize: "0.92em"
      }
    },
    /* @__PURE__ */ React.createElement("strong", null, isError ? "Not proved." : "Extra axioms."),
    " ",
    isError ? "This declaration's proof reaches `sorryAx`. Nothing below describes a theorem \u2014 only a statement." : `This proof depends on axioms beyond Lean's standard three: ${unusualAxioms2.join(", ")}.`
  );
}
function Figure({ svg }) {
  return /* @__PURE__ */ React.createElement("div", { style: { margin: "0.2em 0 0.5em" }, dangerouslySetInnerHTML: { __html: svg } });
}
function Figures({ analysis }) {
  const [active, setActive] = React.useState(0);
  const visuals = analysis.visuals;
  const current = visuals[Math.min(active, visuals.length - 1)];
  if (!current) return /* @__PURE__ */ React.createElement("p", { style: { color: colors.muted } }, "No figure was planned.");
  const svg = React.useMemo(
    () => renderSvg(current, { theme: "auto", idPrefix: `pl-${active}` }),
    [current, active]
  );
  return /* @__PURE__ */ React.createElement("div", null, visuals.length > 1 ? /* @__PURE__ */ React.createElement(
    "div",
    {
      role: "tablist",
      style: { display: "flex", flexWrap: "wrap", gap: "0.35em", marginBottom: "0.5em" }
    },
    visuals.map((visual, index) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: visual.id,
        role: "tab",
        "aria-selected": index === active,
        onClick: () => setActive(index),
        style: {
          border: `1px solid ${index === active ? colors.accent : colors.border}`,
          background: index === active ? colors.accent : "transparent",
          color: index === active ? colors.bg : colors.fg,
          borderRadius: "999px",
          padding: "0.1em 0.7em",
          fontSize: "0.8em",
          cursor: "pointer"
        }
      },
      visual.type
    ))
  ) : null, /* @__PURE__ */ React.createElement(Figure, { svg }), /* @__PURE__ */ React.createElement("p", { style: { margin: "0.2em 0", fontSize: "0.88em", color: colors.muted } }, /* @__PURE__ */ React.createElement("strong", { style: { color: colors.fg } }, "Why this figure: "), current.rationale), /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.35em" } }, /* @__PURE__ */ React.createElement(StatusChip, { status: current.epistemic })));
}
function Explanation({ analysis }) {
  return /* @__PURE__ */ React.createElement("div", null, analysis.explanations.map((layer2) => /* @__PURE__ */ React.createElement("div", { key: layer2.id, style: { marginBottom: "0.7em" } }, /* @__PURE__ */ React.createElement(
    "div",
    {
      style: { display: "flex", alignItems: "center", gap: "0.5em", marginBottom: "0.2em" }
    },
    /* @__PURE__ */ React.createElement("strong", { style: { fontSize: "0.93em" } }, layer2.title),
    /* @__PURE__ */ React.createElement(StatusChip, { status: layer2.claim.status })
  ), layer2.id === "formal" ? /* @__PURE__ */ React.createElement(Code, null, layer2.claim.value) : /* @__PURE__ */ React.createElement("p", { style: { margin: 0, fontSize: "0.93em", lineHeight: 1.5 } }, layer2.claim.value))));
}
function Provenance({ analysis }) {
  return /* @__PURE__ */ React.createElement("div", { style: { fontSize: "0.88em" } }, analysis.classifications.map((c) => /* @__PURE__ */ React.createElement(
    "div",
    {
      key: c.rule.id,
      style: {
        borderLeft: `2px solid ${colors.border}`,
        paddingLeft: "0.7em",
        marginBottom: "0.7em"
      }
    },
    /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "center", gap: "0.5em", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("code", { style: { color: colors.accent } }, c.rule.id), /* @__PURE__ */ React.createElement(StatusChip, { status: c.claim.status })),
    /* @__PURE__ */ React.createElement("div", { style: { marginTop: "0.2em" } }, c.rationale),
    c.claim.provenance.sources[0]?.path ? /* @__PURE__ */ React.createElement("div", { style: { color: colors.muted, marginTop: "0.15em" } }, "evidence at ", /* @__PURE__ */ React.createElement("code", null, c.claim.provenance.sources[0].path)) : null
  )), /* @__PURE__ */ React.createElement("div", { style: { color: colors.muted } }, "trust base: ", analysis.math.trust.axioms.join(", ") || "none recorded"));
}
function Stages({ analysis }) {
  const stages = [
    ["Formal IR", analysis.formal],
    ["MathIR", analysis.math],
    ["Classifier output", analysis.classifications],
    ["VisualIR", analysis.visuals]
  ];
  return /* @__PURE__ */ React.createElement("div", null, stages.map(([label, value]) => /* @__PURE__ */ React.createElement("details", { key: label, style: { marginBottom: "0.4em" } }, /* @__PURE__ */ React.createElement("summary", { style: { cursor: "pointer", fontSize: "0.9em" } }, label), /* @__PURE__ */ React.createElement(Code, null, JSON.stringify(value, null, 2)))));
}
var TABS = ["Figures", "Explanation", "Provenance", "Stages"];
function ProofLensWidget(props) {
  const [tab, setTab] = React.useState("Figures");
  const result = React.useMemo(() => {
    try {
      const bundle = runPipelineOnValue(props);
      const focus = typeof props.focus === "string" ? props.focus : void 0;
      const analysis2 = focus ? findAnalysis(bundle, focus) : bundle.analyses[0];
      return { bundle, analysis: analysis2, error: null };
    } catch (error) {
      return {
        bundle: null,
        analysis: void 0,
        error: error instanceof Error ? error.message : String(error)
      };
    }
  }, [props]);
  if (result.error !== null) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        style: {
          color: colors.errFg,
          background: colors.errBg,
          padding: "0.7em",
          borderRadius: "4px"
        }
      },
      /* @__PURE__ */ React.createElement("strong", null, "ProofLens could not read this declaration."),
      /* @__PURE__ */ React.createElement(Code, null, result.error)
    );
  }
  const analysis = result.analysis;
  if (!analysis) {
    return /* @__PURE__ */ React.createElement("p", { style: { color: colors.muted } }, "ProofLens found no declaration to analyse.");
  }
  const shortName2 = analysis.math.name.split(".").pop() ?? analysis.math.name;
  const kinds = Array.from(new Set(analysis.classifications.map((c) => c.payload.kind)));
  return /* @__PURE__ */ React.createElement("div", { style: { color: colors.fg, fontFamily: "inherit", fontSize: "1em", lineHeight: 1.45 } }, /* @__PURE__ */ React.createElement(
    "header",
    {
      style: {
        borderBottom: `1px solid ${colors.border}`,
        paddingBottom: "0.5em",
        marginBottom: "0.8em"
      }
    },
    /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "baseline", gap: "0.5em", flexWrap: "wrap" } }, /* @__PURE__ */ React.createElement("strong", { style: { fontSize: "1.05em" } }, shortName2), /* @__PURE__ */ React.createElement("span", { style: { color: colors.muted, fontSize: "0.85em" } }, analysis.math.kind)),
    /* @__PURE__ */ React.createElement("div", { style: { display: "flex", gap: "0.3em", flexWrap: "wrap", marginTop: "0.35em" } }, kinds.map((kind) => /* @__PURE__ */ React.createElement(
      "span",
      {
        key: kind,
        style: {
          border: `1px solid ${colors.border}`,
          borderRadius: "3px",
          padding: "0 0.45em",
          fontSize: "0.78em",
          color: colors.muted
        }
      },
      kind
    )))
  ), /* @__PURE__ */ React.createElement(TrustBanner, { analysis }), /* @__PURE__ */ React.createElement(Section, { title: "Formal statement" }, /* @__PURE__ */ React.createElement(Code, null, analysis.math.statementDisplay)), /* @__PURE__ */ React.createElement(
    "div",
    {
      role: "tablist",
      style: { display: "flex", gap: "0.35em", marginBottom: "0.8em", flexWrap: "wrap" }
    },
    TABS.map((name) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: name,
        role: "tab",
        "aria-selected": tab === name,
        onClick: () => setTab(name),
        style: {
          border: "none",
          borderBottom: `2px solid ${tab === name ? colors.accent : "transparent"}`,
          background: "transparent",
          color: tab === name ? colors.fg : colors.muted,
          padding: "0.2em 0.4em",
          fontSize: "0.9em",
          cursor: "pointer"
        }
      },
      name
    ))
  ), tab === "Figures" ? /* @__PURE__ */ React.createElement(Figures, { analysis }) : null, tab === "Explanation" ? /* @__PURE__ */ React.createElement(Explanation, { analysis }) : null, tab === "Provenance" ? /* @__PURE__ */ React.createElement(Provenance, { analysis }) : null, tab === "Stages" ? /* @__PURE__ */ React.createElement(Stages, { analysis }) : null, /* @__PURE__ */ React.createElement(
    "footer",
    {
      style: {
        marginTop: "1.2em",
        paddingTop: "0.5em",
        borderTop: `1px solid ${colors.border}`,
        fontSize: "0.8em",
        color: colors.muted
      }
    },
    "ProofLens ",
    result.bundle?.prooflensVersion,
    " \xB7 Lean determines what was proved; ProofLens helps you read it."
  ));
}
export {
  ProofLensWidget as default
};
